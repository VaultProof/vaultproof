#!/usr/bin/env node
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// node_modules/cli-spinners/spinners.json
var require_spinners = __commonJS({
  "node_modules/cli-spinners/spinners.json"(exports, module) {
    module.exports = {
      dots: {
        interval: 80,
        frames: [
          "\u280B",
          "\u2819",
          "\u2839",
          "\u2838",
          "\u283C",
          "\u2834",
          "\u2826",
          "\u2827",
          "\u2807",
          "\u280F"
        ]
      },
      dots2: {
        interval: 80,
        frames: [
          "\u28FE",
          "\u28FD",
          "\u28FB",
          "\u28BF",
          "\u287F",
          "\u28DF",
          "\u28EF",
          "\u28F7"
        ]
      },
      dots3: {
        interval: 80,
        frames: [
          "\u280B",
          "\u2819",
          "\u281A",
          "\u281E",
          "\u2816",
          "\u2826",
          "\u2834",
          "\u2832",
          "\u2833",
          "\u2813"
        ]
      },
      dots4: {
        interval: 80,
        frames: [
          "\u2804",
          "\u2806",
          "\u2807",
          "\u280B",
          "\u2819",
          "\u2838",
          "\u2830",
          "\u2820",
          "\u2830",
          "\u2838",
          "\u2819",
          "\u280B",
          "\u2807",
          "\u2806"
        ]
      },
      dots5: {
        interval: 80,
        frames: [
          "\u280B",
          "\u2819",
          "\u281A",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u2832",
          "\u2834",
          "\u2826",
          "\u2816",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2813",
          "\u280B"
        ]
      },
      dots6: {
        interval: 80,
        frames: [
          "\u2801",
          "\u2809",
          "\u2819",
          "\u281A",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u2832",
          "\u2834",
          "\u2824",
          "\u2804",
          "\u2804",
          "\u2824",
          "\u2834",
          "\u2832",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u281A",
          "\u2819",
          "\u2809",
          "\u2801"
        ]
      },
      dots7: {
        interval: 80,
        frames: [
          "\u2808",
          "\u2809",
          "\u280B",
          "\u2813",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2816",
          "\u2826",
          "\u2824",
          "\u2820",
          "\u2820",
          "\u2824",
          "\u2826",
          "\u2816",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2813",
          "\u280B",
          "\u2809",
          "\u2808"
        ]
      },
      dots8: {
        interval: 80,
        frames: [
          "\u2801",
          "\u2801",
          "\u2809",
          "\u2819",
          "\u281A",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u2832",
          "\u2834",
          "\u2824",
          "\u2804",
          "\u2804",
          "\u2824",
          "\u2820",
          "\u2820",
          "\u2824",
          "\u2826",
          "\u2816",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2813",
          "\u280B",
          "\u2809",
          "\u2808",
          "\u2808"
        ]
      },
      dots9: {
        interval: 80,
        frames: [
          "\u28B9",
          "\u28BA",
          "\u28BC",
          "\u28F8",
          "\u28C7",
          "\u2867",
          "\u2857",
          "\u284F"
        ]
      },
      dots10: {
        interval: 80,
        frames: [
          "\u2884",
          "\u2882",
          "\u2881",
          "\u2841",
          "\u2848",
          "\u2850",
          "\u2860"
        ]
      },
      dots11: {
        interval: 100,
        frames: [
          "\u2801",
          "\u2802",
          "\u2804",
          "\u2840",
          "\u2880",
          "\u2820",
          "\u2810",
          "\u2808"
        ]
      },
      dots12: {
        interval: 80,
        frames: [
          "\u2880\u2800",
          "\u2840\u2800",
          "\u2804\u2800",
          "\u2882\u2800",
          "\u2842\u2800",
          "\u2805\u2800",
          "\u2883\u2800",
          "\u2843\u2800",
          "\u280D\u2800",
          "\u288B\u2800",
          "\u284B\u2800",
          "\u280D\u2801",
          "\u288B\u2801",
          "\u284B\u2801",
          "\u280D\u2809",
          "\u280B\u2809",
          "\u280B\u2809",
          "\u2809\u2819",
          "\u2809\u2819",
          "\u2809\u2829",
          "\u2808\u2899",
          "\u2808\u2859",
          "\u2888\u2829",
          "\u2840\u2899",
          "\u2804\u2859",
          "\u2882\u2829",
          "\u2842\u2898",
          "\u2805\u2858",
          "\u2883\u2828",
          "\u2843\u2890",
          "\u280D\u2850",
          "\u288B\u2820",
          "\u284B\u2880",
          "\u280D\u2841",
          "\u288B\u2801",
          "\u284B\u2801",
          "\u280D\u2809",
          "\u280B\u2809",
          "\u280B\u2809",
          "\u2809\u2819",
          "\u2809\u2819",
          "\u2809\u2829",
          "\u2808\u2899",
          "\u2808\u2859",
          "\u2808\u2829",
          "\u2800\u2899",
          "\u2800\u2859",
          "\u2800\u2829",
          "\u2800\u2898",
          "\u2800\u2858",
          "\u2800\u2828",
          "\u2800\u2890",
          "\u2800\u2850",
          "\u2800\u2820",
          "\u2800\u2880",
          "\u2800\u2840"
        ]
      },
      dots13: {
        interval: 80,
        frames: [
          "\u28FC",
          "\u28F9",
          "\u28BB",
          "\u283F",
          "\u285F",
          "\u28CF",
          "\u28E7",
          "\u28F6"
        ]
      },
      dots8Bit: {
        interval: 80,
        frames: [
          "\u2800",
          "\u2801",
          "\u2802",
          "\u2803",
          "\u2804",
          "\u2805",
          "\u2806",
          "\u2807",
          "\u2840",
          "\u2841",
          "\u2842",
          "\u2843",
          "\u2844",
          "\u2845",
          "\u2846",
          "\u2847",
          "\u2808",
          "\u2809",
          "\u280A",
          "\u280B",
          "\u280C",
          "\u280D",
          "\u280E",
          "\u280F",
          "\u2848",
          "\u2849",
          "\u284A",
          "\u284B",
          "\u284C",
          "\u284D",
          "\u284E",
          "\u284F",
          "\u2810",
          "\u2811",
          "\u2812",
          "\u2813",
          "\u2814",
          "\u2815",
          "\u2816",
          "\u2817",
          "\u2850",
          "\u2851",
          "\u2852",
          "\u2853",
          "\u2854",
          "\u2855",
          "\u2856",
          "\u2857",
          "\u2818",
          "\u2819",
          "\u281A",
          "\u281B",
          "\u281C",
          "\u281D",
          "\u281E",
          "\u281F",
          "\u2858",
          "\u2859",
          "\u285A",
          "\u285B",
          "\u285C",
          "\u285D",
          "\u285E",
          "\u285F",
          "\u2820",
          "\u2821",
          "\u2822",
          "\u2823",
          "\u2824",
          "\u2825",
          "\u2826",
          "\u2827",
          "\u2860",
          "\u2861",
          "\u2862",
          "\u2863",
          "\u2864",
          "\u2865",
          "\u2866",
          "\u2867",
          "\u2828",
          "\u2829",
          "\u282A",
          "\u282B",
          "\u282C",
          "\u282D",
          "\u282E",
          "\u282F",
          "\u2868",
          "\u2869",
          "\u286A",
          "\u286B",
          "\u286C",
          "\u286D",
          "\u286E",
          "\u286F",
          "\u2830",
          "\u2831",
          "\u2832",
          "\u2833",
          "\u2834",
          "\u2835",
          "\u2836",
          "\u2837",
          "\u2870",
          "\u2871",
          "\u2872",
          "\u2873",
          "\u2874",
          "\u2875",
          "\u2876",
          "\u2877",
          "\u2838",
          "\u2839",
          "\u283A",
          "\u283B",
          "\u283C",
          "\u283D",
          "\u283E",
          "\u283F",
          "\u2878",
          "\u2879",
          "\u287A",
          "\u287B",
          "\u287C",
          "\u287D",
          "\u287E",
          "\u287F",
          "\u2880",
          "\u2881",
          "\u2882",
          "\u2883",
          "\u2884",
          "\u2885",
          "\u2886",
          "\u2887",
          "\u28C0",
          "\u28C1",
          "\u28C2",
          "\u28C3",
          "\u28C4",
          "\u28C5",
          "\u28C6",
          "\u28C7",
          "\u2888",
          "\u2889",
          "\u288A",
          "\u288B",
          "\u288C",
          "\u288D",
          "\u288E",
          "\u288F",
          "\u28C8",
          "\u28C9",
          "\u28CA",
          "\u28CB",
          "\u28CC",
          "\u28CD",
          "\u28CE",
          "\u28CF",
          "\u2890",
          "\u2891",
          "\u2892",
          "\u2893",
          "\u2894",
          "\u2895",
          "\u2896",
          "\u2897",
          "\u28D0",
          "\u28D1",
          "\u28D2",
          "\u28D3",
          "\u28D4",
          "\u28D5",
          "\u28D6",
          "\u28D7",
          "\u2898",
          "\u2899",
          "\u289A",
          "\u289B",
          "\u289C",
          "\u289D",
          "\u289E",
          "\u289F",
          "\u28D8",
          "\u28D9",
          "\u28DA",
          "\u28DB",
          "\u28DC",
          "\u28DD",
          "\u28DE",
          "\u28DF",
          "\u28A0",
          "\u28A1",
          "\u28A2",
          "\u28A3",
          "\u28A4",
          "\u28A5",
          "\u28A6",
          "\u28A7",
          "\u28E0",
          "\u28E1",
          "\u28E2",
          "\u28E3",
          "\u28E4",
          "\u28E5",
          "\u28E6",
          "\u28E7",
          "\u28A8",
          "\u28A9",
          "\u28AA",
          "\u28AB",
          "\u28AC",
          "\u28AD",
          "\u28AE",
          "\u28AF",
          "\u28E8",
          "\u28E9",
          "\u28EA",
          "\u28EB",
          "\u28EC",
          "\u28ED",
          "\u28EE",
          "\u28EF",
          "\u28B0",
          "\u28B1",
          "\u28B2",
          "\u28B3",
          "\u28B4",
          "\u28B5",
          "\u28B6",
          "\u28B7",
          "\u28F0",
          "\u28F1",
          "\u28F2",
          "\u28F3",
          "\u28F4",
          "\u28F5",
          "\u28F6",
          "\u28F7",
          "\u28B8",
          "\u28B9",
          "\u28BA",
          "\u28BB",
          "\u28BC",
          "\u28BD",
          "\u28BE",
          "\u28BF",
          "\u28F8",
          "\u28F9",
          "\u28FA",
          "\u28FB",
          "\u28FC",
          "\u28FD",
          "\u28FE",
          "\u28FF"
        ]
      },
      sand: {
        interval: 80,
        frames: [
          "\u2801",
          "\u2802",
          "\u2804",
          "\u2840",
          "\u2848",
          "\u2850",
          "\u2860",
          "\u28C0",
          "\u28C1",
          "\u28C2",
          "\u28C4",
          "\u28CC",
          "\u28D4",
          "\u28E4",
          "\u28E5",
          "\u28E6",
          "\u28EE",
          "\u28F6",
          "\u28F7",
          "\u28FF",
          "\u287F",
          "\u283F",
          "\u289F",
          "\u281F",
          "\u285B",
          "\u281B",
          "\u282B",
          "\u288B",
          "\u280B",
          "\u280D",
          "\u2849",
          "\u2809",
          "\u2811",
          "\u2821",
          "\u2881"
        ]
      },
      line: {
        interval: 130,
        frames: [
          "-",
          "\\",
          "|",
          "/"
        ]
      },
      line2: {
        interval: 100,
        frames: [
          "\u2802",
          "-",
          "\u2013",
          "\u2014",
          "\u2013",
          "-"
        ]
      },
      pipe: {
        interval: 100,
        frames: [
          "\u2524",
          "\u2518",
          "\u2534",
          "\u2514",
          "\u251C",
          "\u250C",
          "\u252C",
          "\u2510"
        ]
      },
      simpleDots: {
        interval: 400,
        frames: [
          ".  ",
          ".. ",
          "...",
          "   "
        ]
      },
      simpleDotsScrolling: {
        interval: 200,
        frames: [
          ".  ",
          ".. ",
          "...",
          " ..",
          "  .",
          "   "
        ]
      },
      star: {
        interval: 70,
        frames: [
          "\u2736",
          "\u2738",
          "\u2739",
          "\u273A",
          "\u2739",
          "\u2737"
        ]
      },
      star2: {
        interval: 80,
        frames: [
          "+",
          "x",
          "*"
        ]
      },
      flip: {
        interval: 70,
        frames: [
          "_",
          "_",
          "_",
          "-",
          "`",
          "`",
          "'",
          "\xB4",
          "-",
          "_",
          "_",
          "_"
        ]
      },
      hamburger: {
        interval: 100,
        frames: [
          "\u2631",
          "\u2632",
          "\u2634"
        ]
      },
      growVertical: {
        interval: 120,
        frames: [
          "\u2581",
          "\u2583",
          "\u2584",
          "\u2585",
          "\u2586",
          "\u2587",
          "\u2586",
          "\u2585",
          "\u2584",
          "\u2583"
        ]
      },
      growHorizontal: {
        interval: 120,
        frames: [
          "\u258F",
          "\u258E",
          "\u258D",
          "\u258C",
          "\u258B",
          "\u258A",
          "\u2589",
          "\u258A",
          "\u258B",
          "\u258C",
          "\u258D",
          "\u258E"
        ]
      },
      balloon: {
        interval: 140,
        frames: [
          " ",
          ".",
          "o",
          "O",
          "@",
          "*",
          " "
        ]
      },
      balloon2: {
        interval: 120,
        frames: [
          ".",
          "o",
          "O",
          "\xB0",
          "O",
          "o",
          "."
        ]
      },
      noise: {
        interval: 100,
        frames: [
          "\u2593",
          "\u2592",
          "\u2591"
        ]
      },
      bounce: {
        interval: 120,
        frames: [
          "\u2801",
          "\u2802",
          "\u2804",
          "\u2802"
        ]
      },
      boxBounce: {
        interval: 120,
        frames: [
          "\u2596",
          "\u2598",
          "\u259D",
          "\u2597"
        ]
      },
      boxBounce2: {
        interval: 100,
        frames: [
          "\u258C",
          "\u2580",
          "\u2590",
          "\u2584"
        ]
      },
      triangle: {
        interval: 50,
        frames: [
          "\u25E2",
          "\u25E3",
          "\u25E4",
          "\u25E5"
        ]
      },
      binary: {
        interval: 80,
        frames: [
          "010010",
          "001100",
          "100101",
          "111010",
          "111101",
          "010111",
          "101011",
          "111000",
          "110011",
          "110101"
        ]
      },
      arc: {
        interval: 100,
        frames: [
          "\u25DC",
          "\u25E0",
          "\u25DD",
          "\u25DE",
          "\u25E1",
          "\u25DF"
        ]
      },
      circle: {
        interval: 120,
        frames: [
          "\u25E1",
          "\u2299",
          "\u25E0"
        ]
      },
      squareCorners: {
        interval: 180,
        frames: [
          "\u25F0",
          "\u25F3",
          "\u25F2",
          "\u25F1"
        ]
      },
      circleQuarters: {
        interval: 120,
        frames: [
          "\u25F4",
          "\u25F7",
          "\u25F6",
          "\u25F5"
        ]
      },
      circleHalves: {
        interval: 50,
        frames: [
          "\u25D0",
          "\u25D3",
          "\u25D1",
          "\u25D2"
        ]
      },
      squish: {
        interval: 100,
        frames: [
          "\u256B",
          "\u256A"
        ]
      },
      toggle: {
        interval: 250,
        frames: [
          "\u22B6",
          "\u22B7"
        ]
      },
      toggle2: {
        interval: 80,
        frames: [
          "\u25AB",
          "\u25AA"
        ]
      },
      toggle3: {
        interval: 120,
        frames: [
          "\u25A1",
          "\u25A0"
        ]
      },
      toggle4: {
        interval: 100,
        frames: [
          "\u25A0",
          "\u25A1",
          "\u25AA",
          "\u25AB"
        ]
      },
      toggle5: {
        interval: 100,
        frames: [
          "\u25AE",
          "\u25AF"
        ]
      },
      toggle6: {
        interval: 300,
        frames: [
          "\u101D",
          "\u1040"
        ]
      },
      toggle7: {
        interval: 80,
        frames: [
          "\u29BE",
          "\u29BF"
        ]
      },
      toggle8: {
        interval: 100,
        frames: [
          "\u25CD",
          "\u25CC"
        ]
      },
      toggle9: {
        interval: 100,
        frames: [
          "\u25C9",
          "\u25CE"
        ]
      },
      toggle10: {
        interval: 100,
        frames: [
          "\u3282",
          "\u3280",
          "\u3281"
        ]
      },
      toggle11: {
        interval: 50,
        frames: [
          "\u29C7",
          "\u29C6"
        ]
      },
      toggle12: {
        interval: 120,
        frames: [
          "\u2617",
          "\u2616"
        ]
      },
      toggle13: {
        interval: 80,
        frames: [
          "=",
          "*",
          "-"
        ]
      },
      arrow: {
        interval: 100,
        frames: [
          "\u2190",
          "\u2196",
          "\u2191",
          "\u2197",
          "\u2192",
          "\u2198",
          "\u2193",
          "\u2199"
        ]
      },
      arrow2: {
        interval: 80,
        frames: [
          "\u2B06\uFE0F ",
          "\u2197\uFE0F ",
          "\u27A1\uFE0F ",
          "\u2198\uFE0F ",
          "\u2B07\uFE0F ",
          "\u2199\uFE0F ",
          "\u2B05\uFE0F ",
          "\u2196\uFE0F "
        ]
      },
      arrow3: {
        interval: 120,
        frames: [
          "\u25B9\u25B9\u25B9\u25B9\u25B9",
          "\u25B8\u25B9\u25B9\u25B9\u25B9",
          "\u25B9\u25B8\u25B9\u25B9\u25B9",
          "\u25B9\u25B9\u25B8\u25B9\u25B9",
          "\u25B9\u25B9\u25B9\u25B8\u25B9",
          "\u25B9\u25B9\u25B9\u25B9\u25B8"
        ]
      },
      bouncingBar: {
        interval: 80,
        frames: [
          "[    ]",
          "[=   ]",
          "[==  ]",
          "[=== ]",
          "[====]",
          "[ ===]",
          "[  ==]",
          "[   =]",
          "[    ]",
          "[   =]",
          "[  ==]",
          "[ ===]",
          "[====]",
          "[=== ]",
          "[==  ]",
          "[=   ]"
        ]
      },
      bouncingBall: {
        interval: 80,
        frames: [
          "( \u25CF    )",
          "(  \u25CF   )",
          "(   \u25CF  )",
          "(    \u25CF )",
          "(     \u25CF)",
          "(    \u25CF )",
          "(   \u25CF  )",
          "(  \u25CF   )",
          "( \u25CF    )",
          "(\u25CF     )"
        ]
      },
      smiley: {
        interval: 200,
        frames: [
          "\u{1F604} ",
          "\u{1F61D} "
        ]
      },
      monkey: {
        interval: 300,
        frames: [
          "\u{1F648} ",
          "\u{1F648} ",
          "\u{1F649} ",
          "\u{1F64A} "
        ]
      },
      hearts: {
        interval: 100,
        frames: [
          "\u{1F49B} ",
          "\u{1F499} ",
          "\u{1F49C} ",
          "\u{1F49A} ",
          "\u2764\uFE0F "
        ]
      },
      clock: {
        interval: 100,
        frames: [
          "\u{1F55B} ",
          "\u{1F550} ",
          "\u{1F551} ",
          "\u{1F552} ",
          "\u{1F553} ",
          "\u{1F554} ",
          "\u{1F555} ",
          "\u{1F556} ",
          "\u{1F557} ",
          "\u{1F558} ",
          "\u{1F559} ",
          "\u{1F55A} "
        ]
      },
      earth: {
        interval: 180,
        frames: [
          "\u{1F30D} ",
          "\u{1F30E} ",
          "\u{1F30F} "
        ]
      },
      material: {
        interval: 17,
        frames: [
          "\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588",
          "\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588",
          "\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588",
          "\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588",
          "\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588",
          "\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588",
          "\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588",
          "\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2588",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581",
          "\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581\u2581"
        ]
      },
      moon: {
        interval: 80,
        frames: [
          "\u{1F311} ",
          "\u{1F312} ",
          "\u{1F313} ",
          "\u{1F314} ",
          "\u{1F315} ",
          "\u{1F316} ",
          "\u{1F317} ",
          "\u{1F318} "
        ]
      },
      runner: {
        interval: 140,
        frames: [
          "\u{1F6B6} ",
          "\u{1F3C3} "
        ]
      },
      pong: {
        interval: 80,
        frames: [
          "\u2590\u2802       \u258C",
          "\u2590\u2808       \u258C",
          "\u2590 \u2802      \u258C",
          "\u2590 \u2820      \u258C",
          "\u2590  \u2840     \u258C",
          "\u2590  \u2820     \u258C",
          "\u2590   \u2802    \u258C",
          "\u2590   \u2808    \u258C",
          "\u2590    \u2802   \u258C",
          "\u2590    \u2820   \u258C",
          "\u2590     \u2840  \u258C",
          "\u2590     \u2820  \u258C",
          "\u2590      \u2802 \u258C",
          "\u2590      \u2808 \u258C",
          "\u2590       \u2802\u258C",
          "\u2590       \u2820\u258C",
          "\u2590       \u2840\u258C",
          "\u2590      \u2820 \u258C",
          "\u2590      \u2802 \u258C",
          "\u2590     \u2808  \u258C",
          "\u2590     \u2802  \u258C",
          "\u2590    \u2820   \u258C",
          "\u2590    \u2840   \u258C",
          "\u2590   \u2820    \u258C",
          "\u2590   \u2802    \u258C",
          "\u2590  \u2808     \u258C",
          "\u2590  \u2802     \u258C",
          "\u2590 \u2820      \u258C",
          "\u2590 \u2840      \u258C",
          "\u2590\u2820       \u258C"
        ]
      },
      shark: {
        interval: 120,
        frames: [
          "\u2590|\\____________\u258C",
          "\u2590_|\\___________\u258C",
          "\u2590__|\\__________\u258C",
          "\u2590___|\\_________\u258C",
          "\u2590____|\\________\u258C",
          "\u2590_____|\\_______\u258C",
          "\u2590______|\\______\u258C",
          "\u2590_______|\\_____\u258C",
          "\u2590________|\\____\u258C",
          "\u2590_________|\\___\u258C",
          "\u2590__________|\\__\u258C",
          "\u2590___________|\\_\u258C",
          "\u2590____________|\\\u258C",
          "\u2590____________/|\u258C",
          "\u2590___________/|_\u258C",
          "\u2590__________/|__\u258C",
          "\u2590_________/|___\u258C",
          "\u2590________/|____\u258C",
          "\u2590_______/|_____\u258C",
          "\u2590______/|______\u258C",
          "\u2590_____/|_______\u258C",
          "\u2590____/|________\u258C",
          "\u2590___/|_________\u258C",
          "\u2590__/|__________\u258C",
          "\u2590_/|___________\u258C",
          "\u2590/|____________\u258C"
        ]
      },
      dqpb: {
        interval: 100,
        frames: [
          "d",
          "q",
          "p",
          "b"
        ]
      },
      weather: {
        interval: 100,
        frames: [
          "\u2600\uFE0F ",
          "\u2600\uFE0F ",
          "\u2600\uFE0F ",
          "\u{1F324} ",
          "\u26C5\uFE0F ",
          "\u{1F325} ",
          "\u2601\uFE0F ",
          "\u{1F327} ",
          "\u{1F328} ",
          "\u{1F327} ",
          "\u{1F328} ",
          "\u{1F327} ",
          "\u{1F328} ",
          "\u26C8 ",
          "\u{1F328} ",
          "\u{1F327} ",
          "\u{1F328} ",
          "\u2601\uFE0F ",
          "\u{1F325} ",
          "\u26C5\uFE0F ",
          "\u{1F324} ",
          "\u2600\uFE0F ",
          "\u2600\uFE0F "
        ]
      },
      christmas: {
        interval: 400,
        frames: [
          "\u{1F332}",
          "\u{1F384}"
        ]
      },
      grenade: {
        interval: 80,
        frames: [
          "\u060C  ",
          "\u2032  ",
          " \xB4 ",
          " \u203E ",
          "  \u2E0C",
          "  \u2E0A",
          "  |",
          "  \u204E",
          "  \u2055",
          " \u0DF4 ",
          "  \u2053",
          "   ",
          "   ",
          "   "
        ]
      },
      point: {
        interval: 125,
        frames: [
          "\u2219\u2219\u2219",
          "\u25CF\u2219\u2219",
          "\u2219\u25CF\u2219",
          "\u2219\u2219\u25CF",
          "\u2219\u2219\u2219"
        ]
      },
      layer: {
        interval: 150,
        frames: [
          "-",
          "=",
          "\u2261"
        ]
      },
      betaWave: {
        interval: 80,
        frames: [
          "\u03C1\u03B2\u03B2\u03B2\u03B2\u03B2\u03B2",
          "\u03B2\u03C1\u03B2\u03B2\u03B2\u03B2\u03B2",
          "\u03B2\u03B2\u03C1\u03B2\u03B2\u03B2\u03B2",
          "\u03B2\u03B2\u03B2\u03C1\u03B2\u03B2\u03B2",
          "\u03B2\u03B2\u03B2\u03B2\u03C1\u03B2\u03B2",
          "\u03B2\u03B2\u03B2\u03B2\u03B2\u03C1\u03B2",
          "\u03B2\u03B2\u03B2\u03B2\u03B2\u03B2\u03C1"
        ]
      },
      fingerDance: {
        interval: 160,
        frames: [
          "\u{1F918} ",
          "\u{1F91F} ",
          "\u{1F596} ",
          "\u270B ",
          "\u{1F91A} ",
          "\u{1F446} "
        ]
      },
      fistBump: {
        interval: 80,
        frames: [
          "\u{1F91C}\u3000\u3000\u3000\u3000\u{1F91B} ",
          "\u{1F91C}\u3000\u3000\u3000\u3000\u{1F91B} ",
          "\u{1F91C}\u3000\u3000\u3000\u3000\u{1F91B} ",
          "\u3000\u{1F91C}\u3000\u3000\u{1F91B}\u3000 ",
          "\u3000\u3000\u{1F91C}\u{1F91B}\u3000\u3000 ",
          "\u3000\u{1F91C}\u2728\u{1F91B}\u3000\u3000 ",
          "\u{1F91C}\u3000\u2728\u3000\u{1F91B}\u3000 "
        ]
      },
      soccerHeader: {
        interval: 80,
        frames: [
          " \u{1F9D1}\u26BD\uFE0F       \u{1F9D1} ",
          "\u{1F9D1}  \u26BD\uFE0F      \u{1F9D1} ",
          "\u{1F9D1}   \u26BD\uFE0F     \u{1F9D1} ",
          "\u{1F9D1}    \u26BD\uFE0F    \u{1F9D1} ",
          "\u{1F9D1}     \u26BD\uFE0F   \u{1F9D1} ",
          "\u{1F9D1}      \u26BD\uFE0F  \u{1F9D1} ",
          "\u{1F9D1}       \u26BD\uFE0F\u{1F9D1}  ",
          "\u{1F9D1}      \u26BD\uFE0F  \u{1F9D1} ",
          "\u{1F9D1}     \u26BD\uFE0F   \u{1F9D1} ",
          "\u{1F9D1}    \u26BD\uFE0F    \u{1F9D1} ",
          "\u{1F9D1}   \u26BD\uFE0F     \u{1F9D1} ",
          "\u{1F9D1}  \u26BD\uFE0F      \u{1F9D1} "
        ]
      },
      mindblown: {
        interval: 160,
        frames: [
          "\u{1F610} ",
          "\u{1F610} ",
          "\u{1F62E} ",
          "\u{1F62E} ",
          "\u{1F626} ",
          "\u{1F626} ",
          "\u{1F627} ",
          "\u{1F627} ",
          "\u{1F92F} ",
          "\u{1F4A5} ",
          "\u2728 ",
          "\u3000 ",
          "\u3000 ",
          "\u3000 "
        ]
      },
      speaker: {
        interval: 160,
        frames: [
          "\u{1F508} ",
          "\u{1F509} ",
          "\u{1F50A} ",
          "\u{1F509} "
        ]
      },
      orangePulse: {
        interval: 100,
        frames: [
          "\u{1F538} ",
          "\u{1F536} ",
          "\u{1F7E0} ",
          "\u{1F7E0} ",
          "\u{1F536} "
        ]
      },
      bluePulse: {
        interval: 100,
        frames: [
          "\u{1F539} ",
          "\u{1F537} ",
          "\u{1F535} ",
          "\u{1F535} ",
          "\u{1F537} "
        ]
      },
      orangeBluePulse: {
        interval: 100,
        frames: [
          "\u{1F538} ",
          "\u{1F536} ",
          "\u{1F7E0} ",
          "\u{1F7E0} ",
          "\u{1F536} ",
          "\u{1F539} ",
          "\u{1F537} ",
          "\u{1F535} ",
          "\u{1F535} ",
          "\u{1F537} "
        ]
      },
      timeTravel: {
        interval: 100,
        frames: [
          "\u{1F55B} ",
          "\u{1F55A} ",
          "\u{1F559} ",
          "\u{1F558} ",
          "\u{1F557} ",
          "\u{1F556} ",
          "\u{1F555} ",
          "\u{1F554} ",
          "\u{1F553} ",
          "\u{1F552} ",
          "\u{1F551} ",
          "\u{1F550} "
        ]
      },
      aesthetic: {
        interval: 80,
        frames: [
          "\u25B0\u25B1\u25B1\u25B1\u25B1\u25B1\u25B1",
          "\u25B0\u25B0\u25B1\u25B1\u25B1\u25B1\u25B1",
          "\u25B0\u25B0\u25B0\u25B1\u25B1\u25B1\u25B1",
          "\u25B0\u25B0\u25B0\u25B0\u25B1\u25B1\u25B1",
          "\u25B0\u25B0\u25B0\u25B0\u25B0\u25B1\u25B1",
          "\u25B0\u25B0\u25B0\u25B0\u25B0\u25B0\u25B1",
          "\u25B0\u25B0\u25B0\u25B0\u25B0\u25B0\u25B0",
          "\u25B0\u25B1\u25B1\u25B1\u25B1\u25B1\u25B1"
        ]
      },
      dwarfFortress: {
        interval: 80,
        frames: [
          " \u2588\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2588\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2588\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2593\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2593\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2592\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2592\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2591\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A\u2591\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "\u263A \u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2593\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2593\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2592\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2592\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2591\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A\u2591\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u263A \u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2593\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2593\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2592\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2592\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2591\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A\u2591\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u263A \u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2593\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2593\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2592\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2592\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2591\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A\u2591\u2588\u2588\xA3\xA3\xA3  ",
          "   \u263A \u2588\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2588\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2588\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2593\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2593\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2592\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2592\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2591\u2588\xA3\xA3\xA3  ",
          "    \u263A\u2591\u2588\xA3\xA3\xA3  ",
          "    \u263A \u2588\xA3\xA3\xA3  ",
          "     \u263A\u2588\xA3\xA3\xA3  ",
          "     \u263A\u2588\xA3\xA3\xA3  ",
          "     \u263A\u2593\xA3\xA3\xA3  ",
          "     \u263A\u2593\xA3\xA3\xA3  ",
          "     \u263A\u2592\xA3\xA3\xA3  ",
          "     \u263A\u2592\xA3\xA3\xA3  ",
          "     \u263A\u2591\xA3\xA3\xA3  ",
          "     \u263A\u2591\xA3\xA3\xA3  ",
          "     \u263A \xA3\xA3\xA3  ",
          "      \u263A\xA3\xA3\xA3  ",
          "      \u263A\xA3\xA3\xA3  ",
          "      \u263A\u2593\xA3\xA3  ",
          "      \u263A\u2593\xA3\xA3  ",
          "      \u263A\u2592\xA3\xA3  ",
          "      \u263A\u2592\xA3\xA3  ",
          "      \u263A\u2591\xA3\xA3  ",
          "      \u263A\u2591\xA3\xA3  ",
          "      \u263A \xA3\xA3  ",
          "       \u263A\xA3\xA3  ",
          "       \u263A\xA3\xA3  ",
          "       \u263A\u2593\xA3  ",
          "       \u263A\u2593\xA3  ",
          "       \u263A\u2592\xA3  ",
          "       \u263A\u2592\xA3  ",
          "       \u263A\u2591\xA3  ",
          "       \u263A\u2591\xA3  ",
          "       \u263A \xA3  ",
          "        \u263A\xA3  ",
          "        \u263A\xA3  ",
          "        \u263A\u2593  ",
          "        \u263A\u2593  ",
          "        \u263A\u2592  ",
          "        \u263A\u2592  ",
          "        \u263A\u2591  ",
          "        \u263A\u2591  ",
          "        \u263A   ",
          "        \u263A  &",
          "        \u263A \u263C&",
          "       \u263A \u263C &",
          "       \u263A\u263C  &",
          "      \u263A\u263C  & ",
          "      \u203C   & ",
          "     \u263A   &  ",
          "    \u203C    &  ",
          "   \u263A    &   ",
          "  \u203C     &   ",
          " \u263A     &    ",
          "\u203C      &    ",
          "      &     ",
          "      &     ",
          "     &   \u2591  ",
          "     &   \u2592  ",
          "    &    \u2593  ",
          "    &    \xA3  ",
          "   &    \u2591\xA3  ",
          "   &    \u2592\xA3  ",
          "  &     \u2593\xA3  ",
          "  &     \xA3\xA3  ",
          " &     \u2591\xA3\xA3  ",
          " &     \u2592\xA3\xA3  ",
          "&      \u2593\xA3\xA3  ",
          "&      \xA3\xA3\xA3  ",
          "      \u2591\xA3\xA3\xA3  ",
          "      \u2592\xA3\xA3\xA3  ",
          "      \u2593\xA3\xA3\xA3  ",
          "      \u2588\xA3\xA3\xA3  ",
          "     \u2591\u2588\xA3\xA3\xA3  ",
          "     \u2592\u2588\xA3\xA3\xA3  ",
          "     \u2593\u2588\xA3\xA3\xA3  ",
          "     \u2588\u2588\xA3\xA3\xA3  ",
          "    \u2591\u2588\u2588\xA3\xA3\xA3  ",
          "    \u2592\u2588\u2588\xA3\xA3\xA3  ",
          "    \u2593\u2588\u2588\xA3\xA3\xA3  ",
          "    \u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u2591\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u2592\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u2593\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "   \u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u2591\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u2592\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u2593\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          "  \u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u2591\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u2592\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u2593\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u2588\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  ",
          " \u2588\u2588\u2588\u2588\u2588\u2588\xA3\xA3\xA3  "
        ]
      }
    };
  }
});

// node_modules/cli-spinners/index.js
var require_cli_spinners = __commonJS({
  "node_modules/cli-spinners/index.js"(exports, module) {
    "use strict";
    var spinners = Object.assign({}, require_spinners());
    var spinnersList = Object.keys(spinners);
    Object.defineProperty(spinners, "random", {
      get() {
        const randomIndex = Math.floor(Math.random() * spinnersList.length);
        const spinnerName = spinnersList[randomIndex];
        return spinners[spinnerName];
      }
    });
    module.exports = spinners;
  }
});

// node_modules/string-width/node_modules/emoji-regex/index.js
var require_emoji_regex = __commonJS({
  "node_modules/string-width/node_modules/emoji-regex/index.js"(exports, module) {
    module.exports = () => {
      return /[#*0-9]\uFE0F?\u20E3|[\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23ED-\u23EF\u23F1\u23F2\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB\u25FC\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692\u2694-\u2697\u2699\u269B\u269C\u26A0\u26A7\u26AA\u26B0\u26B1\u26BD\u26BE\u26C4\u26C8\u26CF\u26D1\u26E9\u26F0-\u26F5\u26F7\u26F8\u26FA\u2702\u2708\u2709\u270F\u2712\u2714\u2716\u271D\u2721\u2733\u2734\u2744\u2747\u2757\u2763\u27A1\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B55\u3030\u303D\u3297\u3299]\uFE0F?|[\u261D\u270C\u270D](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\u270A\u270B](?:\uD83C[\uDFFB-\uDFFF])?|[\u23E9-\u23EC\u23F0\u23F3\u25FD\u2693\u26A1\u26AB\u26C5\u26CE\u26D4\u26EA\u26FD\u2705\u2728\u274C\u274E\u2753-\u2755\u2795-\u2797\u27B0\u27BF\u2B50]|\u26D3\uFE0F?(?:\u200D\uD83D\uDCA5)?|\u26F9(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\u2764\uFE0F?(?:\u200D(?:\uD83D\uDD25|\uD83E\uDE79))?|\uD83C(?:[\uDC04\uDD70\uDD71\uDD7E\uDD7F\uDE02\uDE37\uDF21\uDF24-\uDF2C\uDF36\uDF7D\uDF96\uDF97\uDF99-\uDF9B\uDF9E\uDF9F\uDFCD\uDFCE\uDFD4-\uDFDF\uDFF5\uDFF7]\uFE0F?|[\uDF85\uDFC2\uDFC7](?:\uD83C[\uDFFB-\uDFFF])?|[\uDFC4\uDFCA](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDFCB\uDFCC](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDCCF\uDD8E\uDD91-\uDD9A\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF43\uDF45-\uDF4A\uDF4C-\uDF7C\uDF7E-\uDF84\uDF86-\uDF93\uDFA0-\uDFC1\uDFC5\uDFC6\uDFC8\uDFC9\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF8-\uDFFF]|\uDDE6\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF]|\uDDE7\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF]|\uDDE8\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF7\uDDFA-\uDDFF]|\uDDE9\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF]|\uDDEA\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA]|\uDDEB\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7]|\uDDEC\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE]|\uDDED\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA]|\uDDEE\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9]|\uDDEF\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5]|\uDDF0\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF]|\uDDF1\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE]|\uDDF2\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF]|\uDDF3\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF]|\uDDF4\uD83C\uDDF2|\uDDF5\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE]|\uDDF6\uD83C\uDDE6|\uDDF7\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC]|\uDDF8\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF]|\uDDF9\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF]|\uDDFA\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF]|\uDDFB\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA]|\uDDFC\uD83C[\uDDEB\uDDF8]|\uDDFD\uD83C\uDDF0|\uDDFE\uD83C[\uDDEA\uDDF9]|\uDDFF\uD83C[\uDDE6\uDDF2\uDDFC]|\uDF44(?:\u200D\uD83D\uDFEB)?|\uDF4B(?:\u200D\uD83D\uDFE9)?|\uDFC3(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDFF3\uFE0F?(?:\u200D(?:\u26A7\uFE0F?|\uD83C\uDF08))?|\uDFF4(?:\u200D\u2620\uFE0F?|\uDB40\uDC67\uDB40\uDC62\uDB40(?:\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDC73\uDB40\uDC63\uDB40\uDC74|\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F)?)|\uD83D(?:[\uDC3F\uDCFD\uDD49\uDD4A\uDD6F\uDD70\uDD73\uDD76-\uDD79\uDD87\uDD8A-\uDD8D\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA\uDECB\uDECD-\uDECF\uDEE0-\uDEE5\uDEE9\uDEF0\uDEF3]\uFE0F?|[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC](?:\uD83C[\uDFFB-\uDFFF])?|[\uDC6E-\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4\uDEB5](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD74\uDD90](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\uDC00-\uDC07\uDC09-\uDC14\uDC16-\uDC25\uDC27-\uDC3A\uDC3C-\uDC3E\uDC40\uDC44\uDC45\uDC51-\uDC65\uDC6A\uDC79-\uDC7B\uDC7D-\uDC80\uDC84\uDC88-\uDC8E\uDC90\uDC92-\uDCA9\uDCAB-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDDA4\uDDFB-\uDE2D\uDE2F-\uDE34\uDE37-\uDE41\uDE43\uDE44\uDE48-\uDE4A\uDE80-\uDEA2\uDEA4-\uDEB3\uDEB7-\uDEBF\uDEC1-\uDEC5\uDED0-\uDED2\uDED5-\uDED8\uDEDC-\uDEDF\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB\uDFF0]|\uDC08(?:\u200D\u2B1B)?|\uDC15(?:\u200D\uD83E\uDDBA)?|\uDC26(?:\u200D(?:\u2B1B|\uD83D\uDD25))?|\uDC3B(?:\u200D\u2744\uFE0F?)?|\uDC41\uFE0F?(?:\u200D\uD83D\uDDE8\uFE0F?)?|\uDC68(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDC68\uDC69]\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?))?|\uDC69(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?[\uDC68\uDC69]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?|\uDC69\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?))|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFC-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFD-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFD\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFE]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])))?))?|\uDD75(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\uDE2E(?:\u200D\uD83D\uDCA8)?|\uDE35(?:\u200D\uD83D\uDCAB)?|\uDE36(?:\u200D\uD83C\uDF2B\uFE0F?)?|\uDE42(?:\u200D[\u2194\u2195]\uFE0F?)?|\uDEB6(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?)|\uD83E(?:[\uDD0C\uDD0F\uDD18-\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5\uDEC3-\uDEC5\uDEF0\uDEF2-\uDEF8](?:\uD83C[\uDFFB-\uDFFF])?|[\uDD26\uDD35\uDD37-\uDD39\uDD3C-\uDD3E\uDDB8\uDDB9\uDDCD\uDDCF\uDDD4\uDDD6-\uDDDD](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDDDE\uDDDF](?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD0D\uDD0E\uDD10-\uDD17\uDD20-\uDD25\uDD27-\uDD2F\uDD3A\uDD3F-\uDD45\uDD47-\uDD76\uDD78-\uDDB4\uDDB7\uDDBA\uDDBC-\uDDCC\uDDD0\uDDE0-\uDDFF\uDE70-\uDE7C\uDE80-\uDE8A\uDE8E-\uDEC2\uDEC6\uDEC8\uDECD-\uDEDC\uDEDF-\uDEEA\uDEEF]|\uDDCE(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDDD1(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1|\uDDD1\u200D\uD83E\uDDD2(?:\u200D\uD83E\uDDD2)?|\uDDD2(?:\u200D\uD83E\uDDD2)?))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])))?))?|\uDEF1(?:\uD83C(?:\uDFFB(?:\u200D\uD83E\uDEF2\uD83C[\uDFFC-\uDFFF])?|\uDFFC(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFD-\uDFFF])?|\uDFFD(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])?|\uDFFE(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFD\uDFFF])?|\uDFFF(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFE])?))?)/g;
    };
  }
});

// packages/init-cli/src/prompts.ts
var prompts_exports = {};
__export(prompts_exports, {
  confirm: () => confirm,
  prompt: () => prompt,
  promptHidden: () => promptHidden
});
import readline from "node:readline";
function createInterface() {
  return readline.createInterface({
    input: process.stdin,
    output: process.stderr
  });
}
function prompt(question) {
  return new Promise((resolve) => {
    const rl = createInterface();
    rl.question(question, (answer) => {
      rl.close();
      resolve(answer.trim());
    });
  });
}
function promptHidden(question) {
  return new Promise((resolve) => {
    process.stderr.write(question);
    const stdin = process.stdin;
    const wasRaw = stdin.isRaw;
    if (stdin.isTTY) stdin.setRawMode(true);
    let input = "";
    const onData = (chunk) => {
      const str = chunk.toString("utf-8");
      for (const c of str) {
        const code = c.charCodeAt(0);
        if (c === "\n" || c === "\r" || code === 4) {
          if (stdin.isTTY) stdin.setRawMode(wasRaw ?? false);
          stdin.removeListener("data", onData);
          process.stderr.write("\n");
          resolve(input);
          return;
        }
        if (code === 3) {
          if (stdin.isTTY) stdin.setRawMode(wasRaw ?? false);
          stdin.removeListener("data", onData);
          process.stderr.write("\n");
          process.exit(130);
        }
        if (c === "\x7F" || c === "\b") {
          input = input.slice(0, -1);
          continue;
        }
        input += c;
      }
    };
    stdin.on("data", onData);
  });
}
async function confirm(question, defaultYes = false) {
  const hint = defaultYes ? "(Y/n)" : "(y/N)";
  const answer = (await prompt(`${question} ${hint} `)).toLowerCase();
  if (answer === "") return defaultYes;
  return answer === "y" || answer === "yes";
}
var init_prompts = __esm({
  "packages/init-cli/src/prompts.ts"() {
    "use strict";
  }
});

// packages/init-cli/node_modules/chalk/source/vendor/ansi-styles/index.js
var ANSI_BACKGROUND_OFFSET = 10;
var wrapAnsi16 = (offset = 0) => (code) => `\x1B[${code + offset}m`;
var wrapAnsi256 = (offset = 0) => (code) => `\x1B[${38 + offset};5;${code}m`;
var wrapAnsi16m = (offset = 0) => (red, green, blue) => `\x1B[${38 + offset};2;${red};${green};${blue}m`;
var styles = {
  modifier: {
    reset: [0, 0],
    // 21 isn't widely supported and 22 does the same thing
    bold: [1, 22],
    dim: [2, 22],
    italic: [3, 23],
    underline: [4, 24],
    overline: [53, 55],
    inverse: [7, 27],
    hidden: [8, 28],
    strikethrough: [9, 29]
  },
  color: {
    black: [30, 39],
    red: [31, 39],
    green: [32, 39],
    yellow: [33, 39],
    blue: [34, 39],
    magenta: [35, 39],
    cyan: [36, 39],
    white: [37, 39],
    // Bright color
    blackBright: [90, 39],
    gray: [90, 39],
    // Alias of `blackBright`
    grey: [90, 39],
    // Alias of `blackBright`
    redBright: [91, 39],
    greenBright: [92, 39],
    yellowBright: [93, 39],
    blueBright: [94, 39],
    magentaBright: [95, 39],
    cyanBright: [96, 39],
    whiteBright: [97, 39]
  },
  bgColor: {
    bgBlack: [40, 49],
    bgRed: [41, 49],
    bgGreen: [42, 49],
    bgYellow: [43, 49],
    bgBlue: [44, 49],
    bgMagenta: [45, 49],
    bgCyan: [46, 49],
    bgWhite: [47, 49],
    // Bright color
    bgBlackBright: [100, 49],
    bgGray: [100, 49],
    // Alias of `bgBlackBright`
    bgGrey: [100, 49],
    // Alias of `bgBlackBright`
    bgRedBright: [101, 49],
    bgGreenBright: [102, 49],
    bgYellowBright: [103, 49],
    bgBlueBright: [104, 49],
    bgMagentaBright: [105, 49],
    bgCyanBright: [106, 49],
    bgWhiteBright: [107, 49]
  }
};
var modifierNames = Object.keys(styles.modifier);
var foregroundColorNames = Object.keys(styles.color);
var backgroundColorNames = Object.keys(styles.bgColor);
var colorNames = [...foregroundColorNames, ...backgroundColorNames];
function assembleStyles() {
  const codes = /* @__PURE__ */ new Map();
  for (const [groupName, group] of Object.entries(styles)) {
    for (const [styleName, style] of Object.entries(group)) {
      styles[styleName] = {
        open: `\x1B[${style[0]}m`,
        close: `\x1B[${style[1]}m`
      };
      group[styleName] = styles[styleName];
      codes.set(style[0], style[1]);
    }
    Object.defineProperty(styles, groupName, {
      value: group,
      enumerable: false
    });
  }
  Object.defineProperty(styles, "codes", {
    value: codes,
    enumerable: false
  });
  styles.color.close = "\x1B[39m";
  styles.bgColor.close = "\x1B[49m";
  styles.color.ansi = wrapAnsi16();
  styles.color.ansi256 = wrapAnsi256();
  styles.color.ansi16m = wrapAnsi16m();
  styles.bgColor.ansi = wrapAnsi16(ANSI_BACKGROUND_OFFSET);
  styles.bgColor.ansi256 = wrapAnsi256(ANSI_BACKGROUND_OFFSET);
  styles.bgColor.ansi16m = wrapAnsi16m(ANSI_BACKGROUND_OFFSET);
  Object.defineProperties(styles, {
    rgbToAnsi256: {
      value(red, green, blue) {
        if (red === green && green === blue) {
          if (red < 8) {
            return 16;
          }
          if (red > 248) {
            return 231;
          }
          return Math.round((red - 8) / 247 * 24) + 232;
        }
        return 16 + 36 * Math.round(red / 255 * 5) + 6 * Math.round(green / 255 * 5) + Math.round(blue / 255 * 5);
      },
      enumerable: false
    },
    hexToRgb: {
      value(hex) {
        const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
        if (!matches) {
          return [0, 0, 0];
        }
        let [colorString] = matches;
        if (colorString.length === 3) {
          colorString = [...colorString].map((character) => character + character).join("");
        }
        const integer = Number.parseInt(colorString, 16);
        return [
          /* eslint-disable no-bitwise */
          integer >> 16 & 255,
          integer >> 8 & 255,
          integer & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: false
    },
    hexToAnsi256: {
      value: (hex) => styles.rgbToAnsi256(...styles.hexToRgb(hex)),
      enumerable: false
    },
    ansi256ToAnsi: {
      value(code) {
        if (code < 8) {
          return 30 + code;
        }
        if (code < 16) {
          return 90 + (code - 8);
        }
        let red;
        let green;
        let blue;
        if (code >= 232) {
          red = ((code - 232) * 10 + 8) / 255;
          green = red;
          blue = red;
        } else {
          code -= 16;
          const remainder = code % 36;
          red = Math.floor(code / 36) / 5;
          green = Math.floor(remainder / 6) / 5;
          blue = remainder % 6 / 5;
        }
        const value = Math.max(red, green, blue) * 2;
        if (value === 0) {
          return 30;
        }
        let result = 30 + (Math.round(blue) << 2 | Math.round(green) << 1 | Math.round(red));
        if (value === 2) {
          result += 60;
        }
        return result;
      },
      enumerable: false
    },
    rgbToAnsi: {
      value: (red, green, blue) => styles.ansi256ToAnsi(styles.rgbToAnsi256(red, green, blue)),
      enumerable: false
    },
    hexToAnsi: {
      value: (hex) => styles.ansi256ToAnsi(styles.hexToAnsi256(hex)),
      enumerable: false
    }
  });
  return styles;
}
var ansiStyles = assembleStyles();
var ansi_styles_default = ansiStyles;

// packages/init-cli/node_modules/chalk/source/vendor/supports-color/index.js
import process2 from "node:process";
import os from "node:os";
import tty from "node:tty";
function hasFlag(flag, argv = globalThis.Deno ? globalThis.Deno.args : process2.argv) {
  const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
  const position = argv.indexOf(prefix + flag);
  const terminatorPosition = argv.indexOf("--");
  return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
}
var { env } = process2;
var flagForceColor;
if (hasFlag("no-color") || hasFlag("no-colors") || hasFlag("color=false") || hasFlag("color=never")) {
  flagForceColor = 0;
} else if (hasFlag("color") || hasFlag("colors") || hasFlag("color=true") || hasFlag("color=always")) {
  flagForceColor = 1;
}
function envForceColor() {
  if ("FORCE_COLOR" in env) {
    if (env.FORCE_COLOR === "true") {
      return 1;
    }
    if (env.FORCE_COLOR === "false") {
      return 0;
    }
    return env.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env.FORCE_COLOR, 10), 3);
  }
}
function translateLevel(level) {
  if (level === 0) {
    return false;
  }
  return {
    level,
    hasBasic: true,
    has256: level >= 2,
    has16m: level >= 3
  };
}
function _supportsColor(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
  const noFlagForceColor = envForceColor();
  if (noFlagForceColor !== void 0) {
    flagForceColor = noFlagForceColor;
  }
  const forceColor = sniffFlags ? flagForceColor : noFlagForceColor;
  if (forceColor === 0) {
    return 0;
  }
  if (sniffFlags) {
    if (hasFlag("color=16m") || hasFlag("color=full") || hasFlag("color=truecolor")) {
      return 3;
    }
    if (hasFlag("color=256")) {
      return 2;
    }
  }
  if ("TF_BUILD" in env && "AGENT_NAME" in env) {
    return 1;
  }
  if (haveStream && !streamIsTTY && forceColor === void 0) {
    return 0;
  }
  const min = forceColor || 0;
  if (env.TERM === "dumb") {
    return min;
  }
  if (process2.platform === "win32") {
    const osRelease = os.release().split(".");
    if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
      return Number(osRelease[2]) >= 14931 ? 3 : 2;
    }
    return 1;
  }
  if ("CI" in env) {
    if (["GITHUB_ACTIONS", "GITEA_ACTIONS", "CIRCLECI"].some((key) => key in env)) {
      return 3;
    }
    if (["TRAVIS", "APPVEYOR", "GITLAB_CI", "BUILDKITE", "DRONE"].some((sign) => sign in env) || env.CI_NAME === "codeship") {
      return 1;
    }
    return min;
  }
  if ("TEAMCITY_VERSION" in env) {
    return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env.TEAMCITY_VERSION) ? 1 : 0;
  }
  if (env.COLORTERM === "truecolor") {
    return 3;
  }
  if (env.TERM === "xterm-kitty") {
    return 3;
  }
  if (env.TERM === "xterm-ghostty") {
    return 3;
  }
  if (env.TERM === "wezterm") {
    return 3;
  }
  if ("TERM_PROGRAM" in env) {
    const version = Number.parseInt((env.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
    switch (env.TERM_PROGRAM) {
      case "iTerm.app": {
        return version >= 3 ? 3 : 2;
      }
      case "Apple_Terminal": {
        return 2;
      }
    }
  }
  if (/-256(color)?$/i.test(env.TERM)) {
    return 2;
  }
  if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env.TERM)) {
    return 1;
  }
  if ("COLORTERM" in env) {
    return 1;
  }
  return min;
}
function createSupportsColor(stream, options = {}) {
  const level = _supportsColor(stream, {
    streamIsTTY: stream && stream.isTTY,
    ...options
  });
  return translateLevel(level);
}
var supportsColor = {
  stdout: createSupportsColor({ isTTY: tty.isatty(1) }),
  stderr: createSupportsColor({ isTTY: tty.isatty(2) })
};
var supports_color_default = supportsColor;

// packages/init-cli/node_modules/chalk/source/utilities.js
function stringReplaceAll(string, substring, replacer) {
  let index = string.indexOf(substring);
  if (index === -1) {
    return string;
  }
  const substringLength = substring.length;
  let endIndex = 0;
  let returnValue = "";
  do {
    returnValue += string.slice(endIndex, index) + substring + replacer;
    endIndex = index + substringLength;
    index = string.indexOf(substring, endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
function stringEncaseCRLFWithFirstIndex(string, prefix, postfix, index) {
  let endIndex = 0;
  let returnValue = "";
  do {
    const gotCR = string[index - 1] === "\r";
    returnValue += string.slice(endIndex, gotCR ? index - 1 : index) + prefix + (gotCR ? "\r\n" : "\n") + postfix;
    endIndex = index + 1;
    index = string.indexOf("\n", endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}

// packages/init-cli/node_modules/chalk/source/index.js
var { stdout: stdoutColor, stderr: stderrColor } = supports_color_default;
var GENERATOR = /* @__PURE__ */ Symbol("GENERATOR");
var STYLER = /* @__PURE__ */ Symbol("STYLER");
var IS_EMPTY = /* @__PURE__ */ Symbol("IS_EMPTY");
var levelMapping = [
  "ansi",
  "ansi",
  "ansi256",
  "ansi16m"
];
var styles2 = /* @__PURE__ */ Object.create(null);
var applyOptions = (object, options = {}) => {
  if (options.level && !(Number.isInteger(options.level) && options.level >= 0 && options.level <= 3)) {
    throw new Error("The `level` option should be an integer from 0 to 3");
  }
  const colorLevel = stdoutColor ? stdoutColor.level : 0;
  object.level = options.level === void 0 ? colorLevel : options.level;
};
var chalkFactory = (options) => {
  const chalk4 = (...strings) => strings.join(" ");
  applyOptions(chalk4, options);
  Object.setPrototypeOf(chalk4, createChalk.prototype);
  return chalk4;
};
function createChalk(options) {
  return chalkFactory(options);
}
Object.setPrototypeOf(createChalk.prototype, Function.prototype);
for (const [styleName, style] of Object.entries(ansi_styles_default)) {
  styles2[styleName] = {
    get() {
      const builder = createBuilder(this, createStyler(style.open, style.close, this[STYLER]), this[IS_EMPTY]);
      Object.defineProperty(this, styleName, { value: builder });
      return builder;
    }
  };
}
styles2.visible = {
  get() {
    const builder = createBuilder(this, this[STYLER], true);
    Object.defineProperty(this, "visible", { value: builder });
    return builder;
  }
};
var getModelAnsi = (model, level, type, ...arguments_) => {
  if (model === "rgb") {
    if (level === "ansi16m") {
      return ansi_styles_default[type].ansi16m(...arguments_);
    }
    if (level === "ansi256") {
      return ansi_styles_default[type].ansi256(ansi_styles_default.rgbToAnsi256(...arguments_));
    }
    return ansi_styles_default[type].ansi(ansi_styles_default.rgbToAnsi(...arguments_));
  }
  if (model === "hex") {
    return getModelAnsi("rgb", level, type, ...ansi_styles_default.hexToRgb(...arguments_));
  }
  return ansi_styles_default[type][model](...arguments_);
};
var usedModels = ["rgb", "hex", "ansi256"];
for (const model of usedModels) {
  styles2[model] = {
    get() {
      const { level } = this;
      return function(...arguments_) {
        const styler = createStyler(getModelAnsi(model, levelMapping[level], "color", ...arguments_), ansi_styles_default.color.close, this[STYLER]);
        return createBuilder(this, styler, this[IS_EMPTY]);
      };
    }
  };
  const bgModel = "bg" + model[0].toUpperCase() + model.slice(1);
  styles2[bgModel] = {
    get() {
      const { level } = this;
      return function(...arguments_) {
        const styler = createStyler(getModelAnsi(model, levelMapping[level], "bgColor", ...arguments_), ansi_styles_default.bgColor.close, this[STYLER]);
        return createBuilder(this, styler, this[IS_EMPTY]);
      };
    }
  };
}
var proto = Object.defineProperties(() => {
}, {
  ...styles2,
  level: {
    enumerable: true,
    get() {
      return this[GENERATOR].level;
    },
    set(level) {
      this[GENERATOR].level = level;
    }
  }
});
var createStyler = (open, close, parent) => {
  let openAll;
  let closeAll;
  if (parent === void 0) {
    openAll = open;
    closeAll = close;
  } else {
    openAll = parent.openAll + open;
    closeAll = close + parent.closeAll;
  }
  return {
    open,
    close,
    openAll,
    closeAll,
    parent
  };
};
var createBuilder = (self, _styler, _isEmpty) => {
  const builder = (...arguments_) => applyStyle(builder, arguments_.length === 1 ? "" + arguments_[0] : arguments_.join(" "));
  Object.setPrototypeOf(builder, proto);
  builder[GENERATOR] = self;
  builder[STYLER] = _styler;
  builder[IS_EMPTY] = _isEmpty;
  return builder;
};
var applyStyle = (self, string) => {
  if (self.level <= 0 || !string) {
    return self[IS_EMPTY] ? "" : string;
  }
  let styler = self[STYLER];
  if (styler === void 0) {
    return string;
  }
  const { openAll, closeAll } = styler;
  if (string.includes("\x1B")) {
    while (styler !== void 0) {
      string = stringReplaceAll(string, styler.close, styler.open);
      styler = styler.parent;
    }
  }
  const lfIndex = string.indexOf("\n");
  if (lfIndex !== -1) {
    string = stringEncaseCRLFWithFirstIndex(string, closeAll, openAll, lfIndex);
  }
  return openAll + string + closeAll;
};
Object.defineProperties(createChalk.prototype, styles2);
var chalk = createChalk();
var chalkStderr = createChalk({ level: stderrColor ? stderrColor.level : 0 });
var source_default = chalk;

// node_modules/ora/index.js
import process11 from "node:process";

// node_modules/ora/node_modules/chalk/source/vendor/ansi-styles/index.js
var ANSI_BACKGROUND_OFFSET2 = 10;
var wrapAnsi162 = (offset = 0) => (code) => `\x1B[${code + offset}m`;
var wrapAnsi2562 = (offset = 0) => (code) => `\x1B[${38 + offset};5;${code}m`;
var wrapAnsi16m2 = (offset = 0) => (red, green, blue) => `\x1B[${38 + offset};2;${red};${green};${blue}m`;
var styles3 = {
  modifier: {
    reset: [0, 0],
    // 21 isn't widely supported and 22 does the same thing
    bold: [1, 22],
    dim: [2, 22],
    italic: [3, 23],
    underline: [4, 24],
    overline: [53, 55],
    inverse: [7, 27],
    hidden: [8, 28],
    strikethrough: [9, 29]
  },
  color: {
    black: [30, 39],
    red: [31, 39],
    green: [32, 39],
    yellow: [33, 39],
    blue: [34, 39],
    magenta: [35, 39],
    cyan: [36, 39],
    white: [37, 39],
    // Bright color
    blackBright: [90, 39],
    gray: [90, 39],
    // Alias of `blackBright`
    grey: [90, 39],
    // Alias of `blackBright`
    redBright: [91, 39],
    greenBright: [92, 39],
    yellowBright: [93, 39],
    blueBright: [94, 39],
    magentaBright: [95, 39],
    cyanBright: [96, 39],
    whiteBright: [97, 39]
  },
  bgColor: {
    bgBlack: [40, 49],
    bgRed: [41, 49],
    bgGreen: [42, 49],
    bgYellow: [43, 49],
    bgBlue: [44, 49],
    bgMagenta: [45, 49],
    bgCyan: [46, 49],
    bgWhite: [47, 49],
    // Bright color
    bgBlackBright: [100, 49],
    bgGray: [100, 49],
    // Alias of `bgBlackBright`
    bgGrey: [100, 49],
    // Alias of `bgBlackBright`
    bgRedBright: [101, 49],
    bgGreenBright: [102, 49],
    bgYellowBright: [103, 49],
    bgBlueBright: [104, 49],
    bgMagentaBright: [105, 49],
    bgCyanBright: [106, 49],
    bgWhiteBright: [107, 49]
  }
};
var modifierNames2 = Object.keys(styles3.modifier);
var foregroundColorNames2 = Object.keys(styles3.color);
var backgroundColorNames2 = Object.keys(styles3.bgColor);
var colorNames2 = [...foregroundColorNames2, ...backgroundColorNames2];
function assembleStyles2() {
  const codes = /* @__PURE__ */ new Map();
  for (const [groupName, group] of Object.entries(styles3)) {
    for (const [styleName, style] of Object.entries(group)) {
      styles3[styleName] = {
        open: `\x1B[${style[0]}m`,
        close: `\x1B[${style[1]}m`
      };
      group[styleName] = styles3[styleName];
      codes.set(style[0], style[1]);
    }
    Object.defineProperty(styles3, groupName, {
      value: group,
      enumerable: false
    });
  }
  Object.defineProperty(styles3, "codes", {
    value: codes,
    enumerable: false
  });
  styles3.color.close = "\x1B[39m";
  styles3.bgColor.close = "\x1B[49m";
  styles3.color.ansi = wrapAnsi162();
  styles3.color.ansi256 = wrapAnsi2562();
  styles3.color.ansi16m = wrapAnsi16m2();
  styles3.bgColor.ansi = wrapAnsi162(ANSI_BACKGROUND_OFFSET2);
  styles3.bgColor.ansi256 = wrapAnsi2562(ANSI_BACKGROUND_OFFSET2);
  styles3.bgColor.ansi16m = wrapAnsi16m2(ANSI_BACKGROUND_OFFSET2);
  Object.defineProperties(styles3, {
    rgbToAnsi256: {
      value(red, green, blue) {
        if (red === green && green === blue) {
          if (red < 8) {
            return 16;
          }
          if (red > 248) {
            return 231;
          }
          return Math.round((red - 8) / 247 * 24) + 232;
        }
        return 16 + 36 * Math.round(red / 255 * 5) + 6 * Math.round(green / 255 * 5) + Math.round(blue / 255 * 5);
      },
      enumerable: false
    },
    hexToRgb: {
      value(hex) {
        const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
        if (!matches) {
          return [0, 0, 0];
        }
        let [colorString] = matches;
        if (colorString.length === 3) {
          colorString = [...colorString].map((character) => character + character).join("");
        }
        const integer = Number.parseInt(colorString, 16);
        return [
          /* eslint-disable no-bitwise */
          integer >> 16 & 255,
          integer >> 8 & 255,
          integer & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: false
    },
    hexToAnsi256: {
      value: (hex) => styles3.rgbToAnsi256(...styles3.hexToRgb(hex)),
      enumerable: false
    },
    ansi256ToAnsi: {
      value(code) {
        if (code < 8) {
          return 30 + code;
        }
        if (code < 16) {
          return 90 + (code - 8);
        }
        let red;
        let green;
        let blue;
        if (code >= 232) {
          red = ((code - 232) * 10 + 8) / 255;
          green = red;
          blue = red;
        } else {
          code -= 16;
          const remainder = code % 36;
          red = Math.floor(code / 36) / 5;
          green = Math.floor(remainder / 6) / 5;
          blue = remainder % 6 / 5;
        }
        const value = Math.max(red, green, blue) * 2;
        if (value === 0) {
          return 30;
        }
        let result = 30 + (Math.round(blue) << 2 | Math.round(green) << 1 | Math.round(red));
        if (value === 2) {
          result += 60;
        }
        return result;
      },
      enumerable: false
    },
    rgbToAnsi: {
      value: (red, green, blue) => styles3.ansi256ToAnsi(styles3.rgbToAnsi256(red, green, blue)),
      enumerable: false
    },
    hexToAnsi: {
      value: (hex) => styles3.ansi256ToAnsi(styles3.hexToAnsi256(hex)),
      enumerable: false
    }
  });
  return styles3;
}
var ansiStyles2 = assembleStyles2();
var ansi_styles_default2 = ansiStyles2;

// node_modules/ora/node_modules/chalk/source/vendor/supports-color/index.js
import process3 from "node:process";
import os2 from "node:os";
import tty2 from "node:tty";
function hasFlag2(flag, argv = globalThis.Deno ? globalThis.Deno.args : process3.argv) {
  const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
  const position = argv.indexOf(prefix + flag);
  const terminatorPosition = argv.indexOf("--");
  return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
}
var { env: env2 } = process3;
var flagForceColor2;
if (hasFlag2("no-color") || hasFlag2("no-colors") || hasFlag2("color=false") || hasFlag2("color=never")) {
  flagForceColor2 = 0;
} else if (hasFlag2("color") || hasFlag2("colors") || hasFlag2("color=true") || hasFlag2("color=always")) {
  flagForceColor2 = 1;
}
function envForceColor2() {
  if ("FORCE_COLOR" in env2) {
    if (env2.FORCE_COLOR === "true") {
      return 1;
    }
    if (env2.FORCE_COLOR === "false") {
      return 0;
    }
    return env2.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env2.FORCE_COLOR, 10), 3);
  }
}
function translateLevel2(level) {
  if (level === 0) {
    return false;
  }
  return {
    level,
    hasBasic: true,
    has256: level >= 2,
    has16m: level >= 3
  };
}
function _supportsColor2(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
  const noFlagForceColor = envForceColor2();
  if (noFlagForceColor !== void 0) {
    flagForceColor2 = noFlagForceColor;
  }
  const forceColor = sniffFlags ? flagForceColor2 : noFlagForceColor;
  if (forceColor === 0) {
    return 0;
  }
  if (sniffFlags) {
    if (hasFlag2("color=16m") || hasFlag2("color=full") || hasFlag2("color=truecolor")) {
      return 3;
    }
    if (hasFlag2("color=256")) {
      return 2;
    }
  }
  if ("TF_BUILD" in env2 && "AGENT_NAME" in env2) {
    return 1;
  }
  if (haveStream && !streamIsTTY && forceColor === void 0) {
    return 0;
  }
  const min = forceColor || 0;
  if (env2.TERM === "dumb") {
    return min;
  }
  if (process3.platform === "win32") {
    const osRelease = os2.release().split(".");
    if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
      return Number(osRelease[2]) >= 14931 ? 3 : 2;
    }
    return 1;
  }
  if ("CI" in env2) {
    if (["GITHUB_ACTIONS", "GITEA_ACTIONS", "CIRCLECI"].some((key) => key in env2)) {
      return 3;
    }
    if (["TRAVIS", "APPVEYOR", "GITLAB_CI", "BUILDKITE", "DRONE"].some((sign) => sign in env2) || env2.CI_NAME === "codeship") {
      return 1;
    }
    return min;
  }
  if ("TEAMCITY_VERSION" in env2) {
    return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env2.TEAMCITY_VERSION) ? 1 : 0;
  }
  if (env2.COLORTERM === "truecolor") {
    return 3;
  }
  if (env2.TERM === "xterm-kitty") {
    return 3;
  }
  if (env2.TERM === "xterm-ghostty") {
    return 3;
  }
  if (env2.TERM === "wezterm") {
    return 3;
  }
  if ("TERM_PROGRAM" in env2) {
    const version = Number.parseInt((env2.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
    switch (env2.TERM_PROGRAM) {
      case "iTerm.app": {
        return version >= 3 ? 3 : 2;
      }
      case "Apple_Terminal": {
        return 2;
      }
    }
  }
  if (/-256(color)?$/i.test(env2.TERM)) {
    return 2;
  }
  if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env2.TERM)) {
    return 1;
  }
  if ("COLORTERM" in env2) {
    return 1;
  }
  return min;
}
function createSupportsColor2(stream, options = {}) {
  const level = _supportsColor2(stream, {
    streamIsTTY: stream && stream.isTTY,
    ...options
  });
  return translateLevel2(level);
}
var supportsColor2 = {
  stdout: createSupportsColor2({ isTTY: tty2.isatty(1) }),
  stderr: createSupportsColor2({ isTTY: tty2.isatty(2) })
};
var supports_color_default2 = supportsColor2;

// node_modules/ora/node_modules/chalk/source/utilities.js
function stringReplaceAll2(string, substring, replacer) {
  let index = string.indexOf(substring);
  if (index === -1) {
    return string;
  }
  const substringLength = substring.length;
  let endIndex = 0;
  let returnValue = "";
  do {
    returnValue += string.slice(endIndex, index) + substring + replacer;
    endIndex = index + substringLength;
    index = string.indexOf(substring, endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
function stringEncaseCRLFWithFirstIndex2(string, prefix, postfix, index) {
  let endIndex = 0;
  let returnValue = "";
  do {
    const gotCR = string[index - 1] === "\r";
    returnValue += string.slice(endIndex, gotCR ? index - 1 : index) + prefix + (gotCR ? "\r\n" : "\n") + postfix;
    endIndex = index + 1;
    index = string.indexOf("\n", endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}

// node_modules/ora/node_modules/chalk/source/index.js
var { stdout: stdoutColor2, stderr: stderrColor2 } = supports_color_default2;
var GENERATOR2 = /* @__PURE__ */ Symbol("GENERATOR");
var STYLER2 = /* @__PURE__ */ Symbol("STYLER");
var IS_EMPTY2 = /* @__PURE__ */ Symbol("IS_EMPTY");
var levelMapping2 = [
  "ansi",
  "ansi",
  "ansi256",
  "ansi16m"
];
var styles4 = /* @__PURE__ */ Object.create(null);
var applyOptions2 = (object, options = {}) => {
  if (options.level && !(Number.isInteger(options.level) && options.level >= 0 && options.level <= 3)) {
    throw new Error("The `level` option should be an integer from 0 to 3");
  }
  const colorLevel = stdoutColor2 ? stdoutColor2.level : 0;
  object.level = options.level === void 0 ? colorLevel : options.level;
};
var chalkFactory2 = (options) => {
  const chalk4 = (...strings) => strings.join(" ");
  applyOptions2(chalk4, options);
  Object.setPrototypeOf(chalk4, createChalk2.prototype);
  return chalk4;
};
function createChalk2(options) {
  return chalkFactory2(options);
}
Object.setPrototypeOf(createChalk2.prototype, Function.prototype);
for (const [styleName, style] of Object.entries(ansi_styles_default2)) {
  styles4[styleName] = {
    get() {
      const builder = createBuilder2(this, createStyler2(style.open, style.close, this[STYLER2]), this[IS_EMPTY2]);
      Object.defineProperty(this, styleName, { value: builder });
      return builder;
    }
  };
}
styles4.visible = {
  get() {
    const builder = createBuilder2(this, this[STYLER2], true);
    Object.defineProperty(this, "visible", { value: builder });
    return builder;
  }
};
var getModelAnsi2 = (model, level, type, ...arguments_) => {
  if (model === "rgb") {
    if (level === "ansi16m") {
      return ansi_styles_default2[type].ansi16m(...arguments_);
    }
    if (level === "ansi256") {
      return ansi_styles_default2[type].ansi256(ansi_styles_default2.rgbToAnsi256(...arguments_));
    }
    return ansi_styles_default2[type].ansi(ansi_styles_default2.rgbToAnsi(...arguments_));
  }
  if (model === "hex") {
    return getModelAnsi2("rgb", level, type, ...ansi_styles_default2.hexToRgb(...arguments_));
  }
  return ansi_styles_default2[type][model](...arguments_);
};
var usedModels2 = ["rgb", "hex", "ansi256"];
for (const model of usedModels2) {
  styles4[model] = {
    get() {
      const { level } = this;
      return function(...arguments_) {
        const styler = createStyler2(getModelAnsi2(model, levelMapping2[level], "color", ...arguments_), ansi_styles_default2.color.close, this[STYLER2]);
        return createBuilder2(this, styler, this[IS_EMPTY2]);
      };
    }
  };
  const bgModel = "bg" + model[0].toUpperCase() + model.slice(1);
  styles4[bgModel] = {
    get() {
      const { level } = this;
      return function(...arguments_) {
        const styler = createStyler2(getModelAnsi2(model, levelMapping2[level], "bgColor", ...arguments_), ansi_styles_default2.bgColor.close, this[STYLER2]);
        return createBuilder2(this, styler, this[IS_EMPTY2]);
      };
    }
  };
}
var proto2 = Object.defineProperties(() => {
}, {
  ...styles4,
  level: {
    enumerable: true,
    get() {
      return this[GENERATOR2].level;
    },
    set(level) {
      this[GENERATOR2].level = level;
    }
  }
});
var createStyler2 = (open, close, parent) => {
  let openAll;
  let closeAll;
  if (parent === void 0) {
    openAll = open;
    closeAll = close;
  } else {
    openAll = parent.openAll + open;
    closeAll = close + parent.closeAll;
  }
  return {
    open,
    close,
    openAll,
    closeAll,
    parent
  };
};
var createBuilder2 = (self, _styler, _isEmpty) => {
  const builder = (...arguments_) => applyStyle2(builder, arguments_.length === 1 ? "" + arguments_[0] : arguments_.join(" "));
  Object.setPrototypeOf(builder, proto2);
  builder[GENERATOR2] = self;
  builder[STYLER2] = _styler;
  builder[IS_EMPTY2] = _isEmpty;
  return builder;
};
var applyStyle2 = (self, string) => {
  if (self.level <= 0 || !string) {
    return self[IS_EMPTY2] ? "" : string;
  }
  let styler = self[STYLER2];
  if (styler === void 0) {
    return string;
  }
  const { openAll, closeAll } = styler;
  if (string.includes("\x1B")) {
    while (styler !== void 0) {
      string = stringReplaceAll2(string, styler.close, styler.open);
      styler = styler.parent;
    }
  }
  const lfIndex = string.indexOf("\n");
  if (lfIndex !== -1) {
    string = stringEncaseCRLFWithFirstIndex2(string, closeAll, openAll, lfIndex);
  }
  return openAll + string + closeAll;
};
Object.defineProperties(createChalk2.prototype, styles4);
var chalk2 = createChalk2();
var chalkStderr2 = createChalk2({ level: stderrColor2 ? stderrColor2.level : 0 });
var source_default2 = chalk2;

// node_modules/cli-cursor/index.js
import process6 from "node:process";

// node_modules/restore-cursor/index.js
import process5 from "node:process";

// node_modules/mimic-function/index.js
var copyProperty = (to, from, property, ignoreNonConfigurable) => {
  if (property === "length" || property === "prototype") {
    return;
  }
  if (property === "arguments" || property === "caller") {
    return;
  }
  const toDescriptor = Object.getOwnPropertyDescriptor(to, property);
  const fromDescriptor = Object.getOwnPropertyDescriptor(from, property);
  if (!canCopyProperty(toDescriptor, fromDescriptor) && ignoreNonConfigurable) {
    return;
  }
  Object.defineProperty(to, property, fromDescriptor);
};
var canCopyProperty = function(toDescriptor, fromDescriptor) {
  return toDescriptor === void 0 || toDescriptor.configurable || toDescriptor.writable === fromDescriptor.writable && toDescriptor.enumerable === fromDescriptor.enumerable && toDescriptor.configurable === fromDescriptor.configurable && (toDescriptor.writable || toDescriptor.value === fromDescriptor.value);
};
var changePrototype = (to, from) => {
  const fromPrototype = Object.getPrototypeOf(from);
  if (fromPrototype === Object.getPrototypeOf(to)) {
    return;
  }
  Object.setPrototypeOf(to, fromPrototype);
};
var wrappedToString = (withName, fromBody) => `/* Wrapped ${withName}*/
${fromBody}`;
var toStringDescriptor = Object.getOwnPropertyDescriptor(Function.prototype, "toString");
var toStringName = Object.getOwnPropertyDescriptor(Function.prototype.toString, "name");
var changeToString = (to, from, name) => {
  const withName = name === "" ? "" : `with ${name.trim()}() `;
  const newToString = wrappedToString.bind(null, withName, from.toString());
  Object.defineProperty(newToString, "name", toStringName);
  const { writable, enumerable, configurable } = toStringDescriptor;
  Object.defineProperty(to, "toString", { value: newToString, writable, enumerable, configurable });
};
function mimicFunction(to, from, { ignoreNonConfigurable = false } = {}) {
  const { name } = to;
  for (const property of Reflect.ownKeys(from)) {
    copyProperty(to, from, property, ignoreNonConfigurable);
  }
  changePrototype(to, from);
  changeToString(to, from, name);
  return to;
}

// node_modules/onetime/index.js
var calledFunctions = /* @__PURE__ */ new WeakMap();
var onetime = (function_, options = {}) => {
  if (typeof function_ !== "function") {
    throw new TypeError("Expected a function");
  }
  let returnValue;
  let callCount = 0;
  const functionName = function_.displayName || function_.name || "<anonymous>";
  const onetime2 = function(...arguments_) {
    calledFunctions.set(onetime2, ++callCount);
    if (callCount === 1) {
      returnValue = function_.apply(this, arguments_);
      function_ = void 0;
    } else if (options.throw === true) {
      throw new Error(`Function \`${functionName}\` can only be called once`);
    }
    return returnValue;
  };
  mimicFunction(onetime2, function_);
  calledFunctions.set(onetime2, callCount);
  return onetime2;
};
onetime.callCount = (function_) => {
  if (!calledFunctions.has(function_)) {
    throw new Error(`The given function \`${function_.name}\` is not wrapped by the \`onetime\` package`);
  }
  return calledFunctions.get(function_);
};
var onetime_default = onetime;

// node_modules/signal-exit/dist/mjs/signals.js
var signals = [];
signals.push("SIGHUP", "SIGINT", "SIGTERM");
if (process.platform !== "win32") {
  signals.push(
    "SIGALRM",
    "SIGABRT",
    "SIGVTALRM",
    "SIGXCPU",
    "SIGXFSZ",
    "SIGUSR2",
    "SIGTRAP",
    "SIGSYS",
    "SIGQUIT",
    "SIGIOT"
    // should detect profiler and enable/disable accordingly.
    // see #21
    // 'SIGPROF'
  );
}
if (process.platform === "linux") {
  signals.push("SIGIO", "SIGPOLL", "SIGPWR", "SIGSTKFLT");
}

// node_modules/signal-exit/dist/mjs/index.js
var processOk = (process12) => !!process12 && typeof process12 === "object" && typeof process12.removeListener === "function" && typeof process12.emit === "function" && typeof process12.reallyExit === "function" && typeof process12.listeners === "function" && typeof process12.kill === "function" && typeof process12.pid === "number" && typeof process12.on === "function";
var kExitEmitter = /* @__PURE__ */ Symbol.for("signal-exit emitter");
var global = globalThis;
var ObjectDefineProperty = Object.defineProperty.bind(Object);
var Emitter = class {
  emitted = {
    afterExit: false,
    exit: false
  };
  listeners = {
    afterExit: [],
    exit: []
  };
  count = 0;
  id = Math.random();
  constructor() {
    if (global[kExitEmitter]) {
      return global[kExitEmitter];
    }
    ObjectDefineProperty(global, kExitEmitter, {
      value: this,
      writable: false,
      enumerable: false,
      configurable: false
    });
  }
  on(ev, fn) {
    this.listeners[ev].push(fn);
  }
  removeListener(ev, fn) {
    const list = this.listeners[ev];
    const i = list.indexOf(fn);
    if (i === -1) {
      return;
    }
    if (i === 0 && list.length === 1) {
      list.length = 0;
    } else {
      list.splice(i, 1);
    }
  }
  emit(ev, code, signal) {
    if (this.emitted[ev]) {
      return false;
    }
    this.emitted[ev] = true;
    let ret = false;
    for (const fn of this.listeners[ev]) {
      ret = fn(code, signal) === true || ret;
    }
    if (ev === "exit") {
      ret = this.emit("afterExit", code, signal) || ret;
    }
    return ret;
  }
};
var SignalExitBase = class {
};
var signalExitWrap = (handler) => {
  return {
    onExit(cb, opts) {
      return handler.onExit(cb, opts);
    },
    load() {
      return handler.load();
    },
    unload() {
      return handler.unload();
    }
  };
};
var SignalExitFallback = class extends SignalExitBase {
  onExit() {
    return () => {
    };
  }
  load() {
  }
  unload() {
  }
};
var SignalExit = class extends SignalExitBase {
  // "SIGHUP" throws an `ENOSYS` error on Windows,
  // so use a supported signal instead
  /* c8 ignore start */
  #hupSig = process4.platform === "win32" ? "SIGINT" : "SIGHUP";
  /* c8 ignore stop */
  #emitter = new Emitter();
  #process;
  #originalProcessEmit;
  #originalProcessReallyExit;
  #sigListeners = {};
  #loaded = false;
  constructor(process12) {
    super();
    this.#process = process12;
    this.#sigListeners = {};
    for (const sig of signals) {
      this.#sigListeners[sig] = () => {
        const listeners = this.#process.listeners(sig);
        let { count } = this.#emitter;
        const p = process12;
        if (typeof p.__signal_exit_emitter__ === "object" && typeof p.__signal_exit_emitter__.count === "number") {
          count += p.__signal_exit_emitter__.count;
        }
        if (listeners.length === count) {
          this.unload();
          const ret = this.#emitter.emit("exit", null, sig);
          const s = sig === "SIGHUP" ? this.#hupSig : sig;
          if (!ret)
            process12.kill(process12.pid, s);
        }
      };
    }
    this.#originalProcessReallyExit = process12.reallyExit;
    this.#originalProcessEmit = process12.emit;
  }
  onExit(cb, opts) {
    if (!processOk(this.#process)) {
      return () => {
      };
    }
    if (this.#loaded === false) {
      this.load();
    }
    const ev = opts?.alwaysLast ? "afterExit" : "exit";
    this.#emitter.on(ev, cb);
    return () => {
      this.#emitter.removeListener(ev, cb);
      if (this.#emitter.listeners["exit"].length === 0 && this.#emitter.listeners["afterExit"].length === 0) {
        this.unload();
      }
    };
  }
  load() {
    if (this.#loaded) {
      return;
    }
    this.#loaded = true;
    this.#emitter.count += 1;
    for (const sig of signals) {
      try {
        const fn = this.#sigListeners[sig];
        if (fn)
          this.#process.on(sig, fn);
      } catch (_) {
      }
    }
    this.#process.emit = (ev, ...a) => {
      return this.#processEmit(ev, ...a);
    };
    this.#process.reallyExit = (code) => {
      return this.#processReallyExit(code);
    };
  }
  unload() {
    if (!this.#loaded) {
      return;
    }
    this.#loaded = false;
    signals.forEach((sig) => {
      const listener = this.#sigListeners[sig];
      if (!listener) {
        throw new Error("Listener not defined for signal: " + sig);
      }
      try {
        this.#process.removeListener(sig, listener);
      } catch (_) {
      }
    });
    this.#process.emit = this.#originalProcessEmit;
    this.#process.reallyExit = this.#originalProcessReallyExit;
    this.#emitter.count -= 1;
  }
  #processReallyExit(code) {
    if (!processOk(this.#process)) {
      return 0;
    }
    this.#process.exitCode = code || 0;
    this.#emitter.emit("exit", this.#process.exitCode, null);
    return this.#originalProcessReallyExit.call(this.#process, this.#process.exitCode);
  }
  #processEmit(ev, ...args) {
    const og = this.#originalProcessEmit;
    if (ev === "exit" && processOk(this.#process)) {
      if (typeof args[0] === "number") {
        this.#process.exitCode = args[0];
      }
      const ret = og.call(this.#process, ev, ...args);
      this.#emitter.emit("exit", this.#process.exitCode, null);
      return ret;
    } else {
      return og.call(this.#process, ev, ...args);
    }
  }
};
var process4 = globalThis.process;
var {
  /**
   * Called when the process is exiting, whether via signal, explicit
   * exit, or running out of stuff to do.
   *
   * If the global process object is not suitable for instrumentation,
   * then this will be a no-op.
   *
   * Returns a function that may be used to unload signal-exit.
   */
  onExit,
  /**
   * Load the listeners.  Likely you never need to call this, unless
   * doing a rather deep integration with signal-exit functionality.
   * Mostly exposed for the benefit of testing.
   *
   * @internal
   */
  load,
  /**
   * Unload the listeners.  Likely you never need to call this, unless
   * doing a rather deep integration with signal-exit functionality.
   * Mostly exposed for the benefit of testing.
   *
   * @internal
   */
  unload
} = signalExitWrap(processOk(process4) ? new SignalExit(process4) : new SignalExitFallback());

// node_modules/restore-cursor/index.js
var terminal = process5.stderr.isTTY ? process5.stderr : process5.stdout.isTTY ? process5.stdout : void 0;
var restoreCursor = terminal ? onetime_default(() => {
  onExit(() => {
    terminal.write("\x1B[?25h");
  }, { alwaysLast: true });
}) : () => {
};
var restore_cursor_default = restoreCursor;

// node_modules/cli-cursor/index.js
var isHidden = false;
var cliCursor = {};
cliCursor.show = (writableStream = process6.stderr) => {
  if (!writableStream.isTTY) {
    return;
  }
  isHidden = false;
  writableStream.write("\x1B[?25h");
};
cliCursor.hide = (writableStream = process6.stderr) => {
  if (!writableStream.isTTY) {
    return;
  }
  restore_cursor_default();
  isHidden = true;
  writableStream.write("\x1B[?25l");
};
cliCursor.toggle = (force, writableStream) => {
  if (force !== void 0) {
    isHidden = force;
  }
  if (isHidden) {
    cliCursor.show(writableStream);
  } else {
    cliCursor.hide(writableStream);
  }
};
var cli_cursor_default = cliCursor;

// node_modules/ora/index.js
var import_cli_spinners = __toESM(require_cli_spinners(), 1);

// node_modules/log-symbols/node_modules/chalk/source/vendor/ansi-styles/index.js
var ANSI_BACKGROUND_OFFSET3 = 10;
var wrapAnsi163 = (offset = 0) => (code) => `\x1B[${code + offset}m`;
var wrapAnsi2563 = (offset = 0) => (code) => `\x1B[${38 + offset};5;${code}m`;
var wrapAnsi16m3 = (offset = 0) => (red, green, blue) => `\x1B[${38 + offset};2;${red};${green};${blue}m`;
var styles5 = {
  modifier: {
    reset: [0, 0],
    // 21 isn't widely supported and 22 does the same thing
    bold: [1, 22],
    dim: [2, 22],
    italic: [3, 23],
    underline: [4, 24],
    overline: [53, 55],
    inverse: [7, 27],
    hidden: [8, 28],
    strikethrough: [9, 29]
  },
  color: {
    black: [30, 39],
    red: [31, 39],
    green: [32, 39],
    yellow: [33, 39],
    blue: [34, 39],
    magenta: [35, 39],
    cyan: [36, 39],
    white: [37, 39],
    // Bright color
    blackBright: [90, 39],
    gray: [90, 39],
    // Alias of `blackBright`
    grey: [90, 39],
    // Alias of `blackBright`
    redBright: [91, 39],
    greenBright: [92, 39],
    yellowBright: [93, 39],
    blueBright: [94, 39],
    magentaBright: [95, 39],
    cyanBright: [96, 39],
    whiteBright: [97, 39]
  },
  bgColor: {
    bgBlack: [40, 49],
    bgRed: [41, 49],
    bgGreen: [42, 49],
    bgYellow: [43, 49],
    bgBlue: [44, 49],
    bgMagenta: [45, 49],
    bgCyan: [46, 49],
    bgWhite: [47, 49],
    // Bright color
    bgBlackBright: [100, 49],
    bgGray: [100, 49],
    // Alias of `bgBlackBright`
    bgGrey: [100, 49],
    // Alias of `bgBlackBright`
    bgRedBright: [101, 49],
    bgGreenBright: [102, 49],
    bgYellowBright: [103, 49],
    bgBlueBright: [104, 49],
    bgMagentaBright: [105, 49],
    bgCyanBright: [106, 49],
    bgWhiteBright: [107, 49]
  }
};
var modifierNames3 = Object.keys(styles5.modifier);
var foregroundColorNames3 = Object.keys(styles5.color);
var backgroundColorNames3 = Object.keys(styles5.bgColor);
var colorNames3 = [...foregroundColorNames3, ...backgroundColorNames3];
function assembleStyles3() {
  const codes = /* @__PURE__ */ new Map();
  for (const [groupName, group] of Object.entries(styles5)) {
    for (const [styleName, style] of Object.entries(group)) {
      styles5[styleName] = {
        open: `\x1B[${style[0]}m`,
        close: `\x1B[${style[1]}m`
      };
      group[styleName] = styles5[styleName];
      codes.set(style[0], style[1]);
    }
    Object.defineProperty(styles5, groupName, {
      value: group,
      enumerable: false
    });
  }
  Object.defineProperty(styles5, "codes", {
    value: codes,
    enumerable: false
  });
  styles5.color.close = "\x1B[39m";
  styles5.bgColor.close = "\x1B[49m";
  styles5.color.ansi = wrapAnsi163();
  styles5.color.ansi256 = wrapAnsi2563();
  styles5.color.ansi16m = wrapAnsi16m3();
  styles5.bgColor.ansi = wrapAnsi163(ANSI_BACKGROUND_OFFSET3);
  styles5.bgColor.ansi256 = wrapAnsi2563(ANSI_BACKGROUND_OFFSET3);
  styles5.bgColor.ansi16m = wrapAnsi16m3(ANSI_BACKGROUND_OFFSET3);
  Object.defineProperties(styles5, {
    rgbToAnsi256: {
      value(red, green, blue) {
        if (red === green && green === blue) {
          if (red < 8) {
            return 16;
          }
          if (red > 248) {
            return 231;
          }
          return Math.round((red - 8) / 247 * 24) + 232;
        }
        return 16 + 36 * Math.round(red / 255 * 5) + 6 * Math.round(green / 255 * 5) + Math.round(blue / 255 * 5);
      },
      enumerable: false
    },
    hexToRgb: {
      value(hex) {
        const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
        if (!matches) {
          return [0, 0, 0];
        }
        let [colorString] = matches;
        if (colorString.length === 3) {
          colorString = [...colorString].map((character) => character + character).join("");
        }
        const integer = Number.parseInt(colorString, 16);
        return [
          /* eslint-disable no-bitwise */
          integer >> 16 & 255,
          integer >> 8 & 255,
          integer & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: false
    },
    hexToAnsi256: {
      value: (hex) => styles5.rgbToAnsi256(...styles5.hexToRgb(hex)),
      enumerable: false
    },
    ansi256ToAnsi: {
      value(code) {
        if (code < 8) {
          return 30 + code;
        }
        if (code < 16) {
          return 90 + (code - 8);
        }
        let red;
        let green;
        let blue;
        if (code >= 232) {
          red = ((code - 232) * 10 + 8) / 255;
          green = red;
          blue = red;
        } else {
          code -= 16;
          const remainder = code % 36;
          red = Math.floor(code / 36) / 5;
          green = Math.floor(remainder / 6) / 5;
          blue = remainder % 6 / 5;
        }
        const value = Math.max(red, green, blue) * 2;
        if (value === 0) {
          return 30;
        }
        let result = 30 + (Math.round(blue) << 2 | Math.round(green) << 1 | Math.round(red));
        if (value === 2) {
          result += 60;
        }
        return result;
      },
      enumerable: false
    },
    rgbToAnsi: {
      value: (red, green, blue) => styles5.ansi256ToAnsi(styles5.rgbToAnsi256(red, green, blue)),
      enumerable: false
    },
    hexToAnsi: {
      value: (hex) => styles5.ansi256ToAnsi(styles5.hexToAnsi256(hex)),
      enumerable: false
    }
  });
  return styles5;
}
var ansiStyles3 = assembleStyles3();
var ansi_styles_default3 = ansiStyles3;

// node_modules/log-symbols/node_modules/chalk/source/vendor/supports-color/index.js
import process7 from "node:process";
import os3 from "node:os";
import tty3 from "node:tty";
function hasFlag3(flag, argv = globalThis.Deno ? globalThis.Deno.args : process7.argv) {
  const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
  const position = argv.indexOf(prefix + flag);
  const terminatorPosition = argv.indexOf("--");
  return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
}
var { env: env3 } = process7;
var flagForceColor3;
if (hasFlag3("no-color") || hasFlag3("no-colors") || hasFlag3("color=false") || hasFlag3("color=never")) {
  flagForceColor3 = 0;
} else if (hasFlag3("color") || hasFlag3("colors") || hasFlag3("color=true") || hasFlag3("color=always")) {
  flagForceColor3 = 1;
}
function envForceColor3() {
  if ("FORCE_COLOR" in env3) {
    if (env3.FORCE_COLOR === "true") {
      return 1;
    }
    if (env3.FORCE_COLOR === "false") {
      return 0;
    }
    return env3.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env3.FORCE_COLOR, 10), 3);
  }
}
function translateLevel3(level) {
  if (level === 0) {
    return false;
  }
  return {
    level,
    hasBasic: true,
    has256: level >= 2,
    has16m: level >= 3
  };
}
function _supportsColor3(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
  const noFlagForceColor = envForceColor3();
  if (noFlagForceColor !== void 0) {
    flagForceColor3 = noFlagForceColor;
  }
  const forceColor = sniffFlags ? flagForceColor3 : noFlagForceColor;
  if (forceColor === 0) {
    return 0;
  }
  if (sniffFlags) {
    if (hasFlag3("color=16m") || hasFlag3("color=full") || hasFlag3("color=truecolor")) {
      return 3;
    }
    if (hasFlag3("color=256")) {
      return 2;
    }
  }
  if ("TF_BUILD" in env3 && "AGENT_NAME" in env3) {
    return 1;
  }
  if (haveStream && !streamIsTTY && forceColor === void 0) {
    return 0;
  }
  const min = forceColor || 0;
  if (env3.TERM === "dumb") {
    return min;
  }
  if (process7.platform === "win32") {
    const osRelease = os3.release().split(".");
    if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
      return Number(osRelease[2]) >= 14931 ? 3 : 2;
    }
    return 1;
  }
  if ("CI" in env3) {
    if (["GITHUB_ACTIONS", "GITEA_ACTIONS", "CIRCLECI"].some((key) => key in env3)) {
      return 3;
    }
    if (["TRAVIS", "APPVEYOR", "GITLAB_CI", "BUILDKITE", "DRONE"].some((sign) => sign in env3) || env3.CI_NAME === "codeship") {
      return 1;
    }
    return min;
  }
  if ("TEAMCITY_VERSION" in env3) {
    return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env3.TEAMCITY_VERSION) ? 1 : 0;
  }
  if (env3.COLORTERM === "truecolor") {
    return 3;
  }
  if (env3.TERM === "xterm-kitty") {
    return 3;
  }
  if (env3.TERM === "xterm-ghostty") {
    return 3;
  }
  if (env3.TERM === "wezterm") {
    return 3;
  }
  if ("TERM_PROGRAM" in env3) {
    const version = Number.parseInt((env3.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
    switch (env3.TERM_PROGRAM) {
      case "iTerm.app": {
        return version >= 3 ? 3 : 2;
      }
      case "Apple_Terminal": {
        return 2;
      }
    }
  }
  if (/-256(color)?$/i.test(env3.TERM)) {
    return 2;
  }
  if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env3.TERM)) {
    return 1;
  }
  if ("COLORTERM" in env3) {
    return 1;
  }
  return min;
}
function createSupportsColor3(stream, options = {}) {
  const level = _supportsColor3(stream, {
    streamIsTTY: stream && stream.isTTY,
    ...options
  });
  return translateLevel3(level);
}
var supportsColor3 = {
  stdout: createSupportsColor3({ isTTY: tty3.isatty(1) }),
  stderr: createSupportsColor3({ isTTY: tty3.isatty(2) })
};
var supports_color_default3 = supportsColor3;

// node_modules/log-symbols/node_modules/chalk/source/utilities.js
function stringReplaceAll3(string, substring, replacer) {
  let index = string.indexOf(substring);
  if (index === -1) {
    return string;
  }
  const substringLength = substring.length;
  let endIndex = 0;
  let returnValue = "";
  do {
    returnValue += string.slice(endIndex, index) + substring + replacer;
    endIndex = index + substringLength;
    index = string.indexOf(substring, endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
function stringEncaseCRLFWithFirstIndex3(string, prefix, postfix, index) {
  let endIndex = 0;
  let returnValue = "";
  do {
    const gotCR = string[index - 1] === "\r";
    returnValue += string.slice(endIndex, gotCR ? index - 1 : index) + prefix + (gotCR ? "\r\n" : "\n") + postfix;
    endIndex = index + 1;
    index = string.indexOf("\n", endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}

// node_modules/log-symbols/node_modules/chalk/source/index.js
var { stdout: stdoutColor3, stderr: stderrColor3 } = supports_color_default3;
var GENERATOR3 = /* @__PURE__ */ Symbol("GENERATOR");
var STYLER3 = /* @__PURE__ */ Symbol("STYLER");
var IS_EMPTY3 = /* @__PURE__ */ Symbol("IS_EMPTY");
var levelMapping3 = [
  "ansi",
  "ansi",
  "ansi256",
  "ansi16m"
];
var styles6 = /* @__PURE__ */ Object.create(null);
var applyOptions3 = (object, options = {}) => {
  if (options.level && !(Number.isInteger(options.level) && options.level >= 0 && options.level <= 3)) {
    throw new Error("The `level` option should be an integer from 0 to 3");
  }
  const colorLevel = stdoutColor3 ? stdoutColor3.level : 0;
  object.level = options.level === void 0 ? colorLevel : options.level;
};
var chalkFactory3 = (options) => {
  const chalk4 = (...strings) => strings.join(" ");
  applyOptions3(chalk4, options);
  Object.setPrototypeOf(chalk4, createChalk3.prototype);
  return chalk4;
};
function createChalk3(options) {
  return chalkFactory3(options);
}
Object.setPrototypeOf(createChalk3.prototype, Function.prototype);
for (const [styleName, style] of Object.entries(ansi_styles_default3)) {
  styles6[styleName] = {
    get() {
      const builder = createBuilder3(this, createStyler3(style.open, style.close, this[STYLER3]), this[IS_EMPTY3]);
      Object.defineProperty(this, styleName, { value: builder });
      return builder;
    }
  };
}
styles6.visible = {
  get() {
    const builder = createBuilder3(this, this[STYLER3], true);
    Object.defineProperty(this, "visible", { value: builder });
    return builder;
  }
};
var getModelAnsi3 = (model, level, type, ...arguments_) => {
  if (model === "rgb") {
    if (level === "ansi16m") {
      return ansi_styles_default3[type].ansi16m(...arguments_);
    }
    if (level === "ansi256") {
      return ansi_styles_default3[type].ansi256(ansi_styles_default3.rgbToAnsi256(...arguments_));
    }
    return ansi_styles_default3[type].ansi(ansi_styles_default3.rgbToAnsi(...arguments_));
  }
  if (model === "hex") {
    return getModelAnsi3("rgb", level, type, ...ansi_styles_default3.hexToRgb(...arguments_));
  }
  return ansi_styles_default3[type][model](...arguments_);
};
var usedModels3 = ["rgb", "hex", "ansi256"];
for (const model of usedModels3) {
  styles6[model] = {
    get() {
      const { level } = this;
      return function(...arguments_) {
        const styler = createStyler3(getModelAnsi3(model, levelMapping3[level], "color", ...arguments_), ansi_styles_default3.color.close, this[STYLER3]);
        return createBuilder3(this, styler, this[IS_EMPTY3]);
      };
    }
  };
  const bgModel = "bg" + model[0].toUpperCase() + model.slice(1);
  styles6[bgModel] = {
    get() {
      const { level } = this;
      return function(...arguments_) {
        const styler = createStyler3(getModelAnsi3(model, levelMapping3[level], "bgColor", ...arguments_), ansi_styles_default3.bgColor.close, this[STYLER3]);
        return createBuilder3(this, styler, this[IS_EMPTY3]);
      };
    }
  };
}
var proto3 = Object.defineProperties(() => {
}, {
  ...styles6,
  level: {
    enumerable: true,
    get() {
      return this[GENERATOR3].level;
    },
    set(level) {
      this[GENERATOR3].level = level;
    }
  }
});
var createStyler3 = (open, close, parent) => {
  let openAll;
  let closeAll;
  if (parent === void 0) {
    openAll = open;
    closeAll = close;
  } else {
    openAll = parent.openAll + open;
    closeAll = close + parent.closeAll;
  }
  return {
    open,
    close,
    openAll,
    closeAll,
    parent
  };
};
var createBuilder3 = (self, _styler, _isEmpty) => {
  const builder = (...arguments_) => applyStyle3(builder, arguments_.length === 1 ? "" + arguments_[0] : arguments_.join(" "));
  Object.setPrototypeOf(builder, proto3);
  builder[GENERATOR3] = self;
  builder[STYLER3] = _styler;
  builder[IS_EMPTY3] = _isEmpty;
  return builder;
};
var applyStyle3 = (self, string) => {
  if (self.level <= 0 || !string) {
    return self[IS_EMPTY3] ? "" : string;
  }
  let styler = self[STYLER3];
  if (styler === void 0) {
    return string;
  }
  const { openAll, closeAll } = styler;
  if (string.includes("\x1B")) {
    while (styler !== void 0) {
      string = stringReplaceAll3(string, styler.close, styler.open);
      styler = styler.parent;
    }
  }
  const lfIndex = string.indexOf("\n");
  if (lfIndex !== -1) {
    string = stringEncaseCRLFWithFirstIndex3(string, closeAll, openAll, lfIndex);
  }
  return openAll + string + closeAll;
};
Object.defineProperties(createChalk3.prototype, styles6);
var chalk3 = createChalk3();
var chalkStderr3 = createChalk3({ level: stderrColor3 ? stderrColor3.level : 0 });
var source_default3 = chalk3;

// node_modules/log-symbols/node_modules/is-unicode-supported/index.js
import process8 from "node:process";
function isUnicodeSupported() {
  if (process8.platform !== "win32") {
    return process8.env.TERM !== "linux";
  }
  return Boolean(process8.env.CI) || Boolean(process8.env.WT_SESSION) || Boolean(process8.env.TERMINUS_SUBLIME) || process8.env.ConEmuTask === "{cmd::Cmder}" || process8.env.TERM_PROGRAM === "Terminus-Sublime" || process8.env.TERM_PROGRAM === "vscode" || process8.env.TERM === "xterm-256color" || process8.env.TERM === "alacritty" || process8.env.TERMINAL_EMULATOR === "JetBrains-JediTerm";
}

// node_modules/log-symbols/index.js
var main = {
  info: source_default3.blue("\u2139"),
  success: source_default3.green("\u2714"),
  warning: source_default3.yellow("\u26A0"),
  error: source_default3.red("\u2716")
};
var fallback = {
  info: source_default3.blue("i"),
  success: source_default3.green("\u221A"),
  warning: source_default3.yellow("\u203C"),
  error: source_default3.red("\xD7")
};
var logSymbols = isUnicodeSupported() ? main : fallback;
var log_symbols_default = logSymbols;

// node_modules/ansi-regex/index.js
function ansiRegex({ onlyFirst = false } = {}) {
  const ST = "(?:\\u0007|\\u001B\\u005C|\\u009C)";
  const osc = `(?:\\u001B\\][\\s\\S]*?${ST})`;
  const csi = "[\\u001B\\u009B][[\\]()#;?]*(?:\\d{1,4}(?:[;:]\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]";
  const pattern = `${osc}|${csi}`;
  return new RegExp(pattern, onlyFirst ? void 0 : "g");
}

// node_modules/strip-ansi/index.js
var regex = ansiRegex();
function stripAnsi(string) {
  if (typeof string !== "string") {
    throw new TypeError(`Expected a \`string\`, got \`${typeof string}\``);
  }
  if (!string.includes("\x1B") && !string.includes("\x9B")) {
    return string;
  }
  return string.replace(regex, "");
}

// node_modules/get-east-asian-width/lookup-data.js
var ambiguousRanges = [161, 161, 164, 164, 167, 168, 170, 170, 173, 174, 176, 180, 182, 186, 188, 191, 198, 198, 208, 208, 215, 216, 222, 225, 230, 230, 232, 234, 236, 237, 240, 240, 242, 243, 247, 250, 252, 252, 254, 254, 257, 257, 273, 273, 275, 275, 283, 283, 294, 295, 299, 299, 305, 307, 312, 312, 319, 322, 324, 324, 328, 331, 333, 333, 338, 339, 358, 359, 363, 363, 462, 462, 464, 464, 466, 466, 468, 468, 470, 470, 472, 472, 474, 474, 476, 476, 593, 593, 609, 609, 708, 708, 711, 711, 713, 715, 717, 717, 720, 720, 728, 731, 733, 733, 735, 735, 768, 879, 913, 929, 931, 937, 945, 961, 963, 969, 1025, 1025, 1040, 1103, 1105, 1105, 8208, 8208, 8211, 8214, 8216, 8217, 8220, 8221, 8224, 8226, 8228, 8231, 8240, 8240, 8242, 8243, 8245, 8245, 8251, 8251, 8254, 8254, 8308, 8308, 8319, 8319, 8321, 8324, 8364, 8364, 8451, 8451, 8453, 8453, 8457, 8457, 8467, 8467, 8470, 8470, 8481, 8482, 8486, 8486, 8491, 8491, 8531, 8532, 8539, 8542, 8544, 8555, 8560, 8569, 8585, 8585, 8592, 8601, 8632, 8633, 8658, 8658, 8660, 8660, 8679, 8679, 8704, 8704, 8706, 8707, 8711, 8712, 8715, 8715, 8719, 8719, 8721, 8721, 8725, 8725, 8730, 8730, 8733, 8736, 8739, 8739, 8741, 8741, 8743, 8748, 8750, 8750, 8756, 8759, 8764, 8765, 8776, 8776, 8780, 8780, 8786, 8786, 8800, 8801, 8804, 8807, 8810, 8811, 8814, 8815, 8834, 8835, 8838, 8839, 8853, 8853, 8857, 8857, 8869, 8869, 8895, 8895, 8978, 8978, 9312, 9449, 9451, 9547, 9552, 9587, 9600, 9615, 9618, 9621, 9632, 9633, 9635, 9641, 9650, 9651, 9654, 9655, 9660, 9661, 9664, 9665, 9670, 9672, 9675, 9675, 9678, 9681, 9698, 9701, 9711, 9711, 9733, 9734, 9737, 9737, 9742, 9743, 9756, 9756, 9758, 9758, 9792, 9792, 9794, 9794, 9824, 9825, 9827, 9829, 9831, 9834, 9836, 9837, 9839, 9839, 9886, 9887, 9919, 9919, 9926, 9933, 9935, 9939, 9941, 9953, 9955, 9955, 9960, 9961, 9963, 9969, 9972, 9972, 9974, 9977, 9979, 9980, 9982, 9983, 10045, 10045, 10102, 10111, 11094, 11097, 12872, 12879, 57344, 63743, 65024, 65039, 65533, 65533, 127232, 127242, 127248, 127277, 127280, 127337, 127344, 127373, 127375, 127376, 127387, 127404, 917760, 917999, 983040, 1048573, 1048576, 1114109];
var fullwidthRanges = [12288, 12288, 65281, 65376, 65504, 65510];
var halfwidthRanges = [8361, 8361, 65377, 65470, 65474, 65479, 65482, 65487, 65490, 65495, 65498, 65500, 65512, 65518];
var narrowRanges = [32, 126, 162, 163, 165, 166, 172, 172, 175, 175, 10214, 10221, 10629, 10630];
var wideRanges = [4352, 4447, 8986, 8987, 9001, 9002, 9193, 9196, 9200, 9200, 9203, 9203, 9725, 9726, 9748, 9749, 9776, 9783, 9800, 9811, 9855, 9855, 9866, 9871, 9875, 9875, 9889, 9889, 9898, 9899, 9917, 9918, 9924, 9925, 9934, 9934, 9940, 9940, 9962, 9962, 9970, 9971, 9973, 9973, 9978, 9978, 9981, 9981, 9989, 9989, 9994, 9995, 10024, 10024, 10060, 10060, 10062, 10062, 10067, 10069, 10071, 10071, 10133, 10135, 10160, 10160, 10175, 10175, 11035, 11036, 11088, 11088, 11093, 11093, 11904, 11929, 11931, 12019, 12032, 12245, 12272, 12287, 12289, 12350, 12353, 12438, 12441, 12543, 12549, 12591, 12593, 12686, 12688, 12773, 12783, 12830, 12832, 12871, 12880, 42124, 42128, 42182, 43360, 43388, 44032, 55203, 63744, 64255, 65040, 65049, 65072, 65106, 65108, 65126, 65128, 65131, 94176, 94180, 94192, 94198, 94208, 101589, 101631, 101662, 101760, 101874, 110576, 110579, 110581, 110587, 110589, 110590, 110592, 110882, 110898, 110898, 110928, 110930, 110933, 110933, 110948, 110951, 110960, 111355, 119552, 119638, 119648, 119670, 126980, 126980, 127183, 127183, 127374, 127374, 127377, 127386, 127488, 127490, 127504, 127547, 127552, 127560, 127568, 127569, 127584, 127589, 127744, 127776, 127789, 127797, 127799, 127868, 127870, 127891, 127904, 127946, 127951, 127955, 127968, 127984, 127988, 127988, 127992, 128062, 128064, 128064, 128066, 128252, 128255, 128317, 128331, 128334, 128336, 128359, 128378, 128378, 128405, 128406, 128420, 128420, 128507, 128591, 128640, 128709, 128716, 128716, 128720, 128722, 128725, 128728, 128732, 128735, 128747, 128748, 128756, 128764, 128992, 129003, 129008, 129008, 129292, 129338, 129340, 129349, 129351, 129535, 129648, 129660, 129664, 129674, 129678, 129734, 129736, 129736, 129741, 129756, 129759, 129770, 129775, 129784, 131072, 196605, 196608, 262141];

// node_modules/get-east-asian-width/utilities.js
var isInRange = (ranges, codePoint) => {
  let low = 0;
  let high = Math.floor(ranges.length / 2) - 1;
  while (low <= high) {
    const mid = Math.floor((low + high) / 2);
    const i = mid * 2;
    if (codePoint < ranges[i]) {
      high = mid - 1;
    } else if (codePoint > ranges[i + 1]) {
      low = mid + 1;
    } else {
      return true;
    }
  }
  return false;
};

// node_modules/get-east-asian-width/lookup.js
var minimumAmbiguousCodePoint = ambiguousRanges[0];
var maximumAmbiguousCodePoint = ambiguousRanges.at(-1);
var minimumFullWidthCodePoint = fullwidthRanges[0];
var maximumFullWidthCodePoint = fullwidthRanges.at(-1);
var minimumHalfWidthCodePoint = halfwidthRanges[0];
var maximumHalfWidthCodePoint = halfwidthRanges.at(-1);
var minimumNarrowCodePoint = narrowRanges[0];
var maximumNarrowCodePoint = narrowRanges.at(-1);
var minimumWideCodePoint = wideRanges[0];
var maximumWideCodePoint = wideRanges.at(-1);
var commonCjkCodePoint = 19968;
var [wideFastPathStart, wideFastPathEnd] = findWideFastPathRange(wideRanges);
function findWideFastPathRange(ranges) {
  let fastPathStart = ranges[0];
  let fastPathEnd = ranges[1];
  for (let index = 0; index < ranges.length; index += 2) {
    const start = ranges[index];
    const end = ranges[index + 1];
    if (commonCjkCodePoint >= start && commonCjkCodePoint <= end) {
      return [start, end];
    }
    if (end - start > fastPathEnd - fastPathStart) {
      fastPathStart = start;
      fastPathEnd = end;
    }
  }
  return [fastPathStart, fastPathEnd];
}
var isAmbiguous = (codePoint) => {
  if (codePoint < minimumAmbiguousCodePoint || codePoint > maximumAmbiguousCodePoint) {
    return false;
  }
  return isInRange(ambiguousRanges, codePoint);
};
var isFullWidth = (codePoint) => {
  if (codePoint < minimumFullWidthCodePoint || codePoint > maximumFullWidthCodePoint) {
    return false;
  }
  return isInRange(fullwidthRanges, codePoint);
};
var isWide = (codePoint) => {
  if (codePoint >= wideFastPathStart && codePoint <= wideFastPathEnd) {
    return true;
  }
  if (codePoint < minimumWideCodePoint || codePoint > maximumWideCodePoint) {
    return false;
  }
  return isInRange(wideRanges, codePoint);
};

// node_modules/get-east-asian-width/index.js
function validate(codePoint) {
  if (!Number.isSafeInteger(codePoint)) {
    throw new TypeError(`Expected a code point, got \`${typeof codePoint}\`.`);
  }
}
function eastAsianWidth(codePoint, { ambiguousAsWide = false } = {}) {
  validate(codePoint);
  if (isFullWidth(codePoint) || isWide(codePoint) || ambiguousAsWide && isAmbiguous(codePoint)) {
    return 2;
  }
  return 1;
}

// node_modules/string-width/index.js
var import_emoji_regex = __toESM(require_emoji_regex(), 1);
var segmenter = new Intl.Segmenter();
var defaultIgnorableCodePointRegex = new RegExp("^\\p{Default_Ignorable_Code_Point}$", "u");
function stringWidth(string, options = {}) {
  if (typeof string !== "string" || string.length === 0) {
    return 0;
  }
  const {
    ambiguousIsNarrow = true,
    countAnsiEscapeCodes = false
  } = options;
  if (!countAnsiEscapeCodes) {
    string = stripAnsi(string);
  }
  if (string.length === 0) {
    return 0;
  }
  let width = 0;
  const eastAsianWidthOptions = { ambiguousAsWide: !ambiguousIsNarrow };
  for (const { segment: character } of segmenter.segment(string)) {
    const codePoint = character.codePointAt(0);
    if (codePoint <= 31 || codePoint >= 127 && codePoint <= 159) {
      continue;
    }
    if (codePoint >= 8203 && codePoint <= 8207 || codePoint === 65279) {
      continue;
    }
    if (codePoint >= 768 && codePoint <= 879 || codePoint >= 6832 && codePoint <= 6911 || codePoint >= 7616 && codePoint <= 7679 || codePoint >= 8400 && codePoint <= 8447 || codePoint >= 65056 && codePoint <= 65071) {
      continue;
    }
    if (codePoint >= 55296 && codePoint <= 57343) {
      continue;
    }
    if (codePoint >= 65024 && codePoint <= 65039) {
      continue;
    }
    if (defaultIgnorableCodePointRegex.test(character)) {
      continue;
    }
    if ((0, import_emoji_regex.default)().test(character)) {
      width += 2;
      continue;
    }
    width += eastAsianWidth(codePoint, eastAsianWidthOptions);
  }
  return width;
}

// node_modules/is-interactive/index.js
function isInteractive({ stream = process.stdout } = {}) {
  return Boolean(
    stream && stream.isTTY && process.env.TERM !== "dumb" && !("CI" in process.env)
  );
}

// node_modules/is-unicode-supported/index.js
import process9 from "node:process";
function isUnicodeSupported2() {
  const { env: env4 } = process9;
  const { TERM, TERM_PROGRAM } = env4;
  if (process9.platform !== "win32") {
    return TERM !== "linux";
  }
  return Boolean(env4.WT_SESSION) || Boolean(env4.TERMINUS_SUBLIME) || env4.ConEmuTask === "{cmd::Cmder}" || TERM_PROGRAM === "Terminus-Sublime" || TERM_PROGRAM === "vscode" || TERM === "xterm-256color" || TERM === "alacritty" || TERM === "rxvt-unicode" || TERM === "rxvt-unicode-256color" || env4.TERMINAL_EMULATOR === "JetBrains-JediTerm";
}

// node_modules/stdin-discarder/index.js
import process10 from "node:process";
var ASCII_ETX_CODE = 3;
var StdinDiscarder = class {
  #activeCount = 0;
  start() {
    this.#activeCount++;
    if (this.#activeCount === 1) {
      this.#realStart();
    }
  }
  stop() {
    if (this.#activeCount <= 0) {
      throw new Error("`stop` called more times than `start`");
    }
    this.#activeCount--;
    if (this.#activeCount === 0) {
      this.#realStop();
    }
  }
  #realStart() {
    if (process10.platform === "win32" || !process10.stdin.isTTY) {
      return;
    }
    process10.stdin.setRawMode(true);
    process10.stdin.on("data", this.#handleInput);
    process10.stdin.resume();
  }
  #realStop() {
    if (!process10.stdin.isTTY) {
      return;
    }
    process10.stdin.off("data", this.#handleInput);
    process10.stdin.pause();
    process10.stdin.setRawMode(false);
  }
  #handleInput(chunk) {
    if (chunk[0] === ASCII_ETX_CODE) {
      process10.emit("SIGINT");
    }
  }
};
var stdinDiscarder = new StdinDiscarder();
var stdin_discarder_default = stdinDiscarder;

// node_modules/ora/index.js
var import_cli_spinners2 = __toESM(require_cli_spinners(), 1);
var Ora = class {
  #linesToClear = 0;
  #isDiscardingStdin = false;
  #lineCount = 0;
  #frameIndex = -1;
  #lastSpinnerFrameTime = 0;
  #options;
  #spinner;
  #stream;
  #id;
  #initialInterval;
  #isEnabled;
  #isSilent;
  #indent;
  #text;
  #prefixText;
  #suffixText;
  color;
  constructor(options) {
    if (typeof options === "string") {
      options = {
        text: options
      };
    }
    this.#options = {
      color: "cyan",
      stream: process11.stderr,
      discardStdin: true,
      hideCursor: true,
      ...options
    };
    this.color = this.#options.color;
    this.spinner = this.#options.spinner;
    this.#initialInterval = this.#options.interval;
    this.#stream = this.#options.stream;
    this.#isEnabled = typeof this.#options.isEnabled === "boolean" ? this.#options.isEnabled : isInteractive({ stream: this.#stream });
    this.#isSilent = typeof this.#options.isSilent === "boolean" ? this.#options.isSilent : false;
    this.text = this.#options.text;
    this.prefixText = this.#options.prefixText;
    this.suffixText = this.#options.suffixText;
    this.indent = this.#options.indent;
    if (process11.env.NODE_ENV === "test") {
      this._stream = this.#stream;
      this._isEnabled = this.#isEnabled;
      Object.defineProperty(this, "_linesToClear", {
        get() {
          return this.#linesToClear;
        },
        set(newValue) {
          this.#linesToClear = newValue;
        }
      });
      Object.defineProperty(this, "_frameIndex", {
        get() {
          return this.#frameIndex;
        }
      });
      Object.defineProperty(this, "_lineCount", {
        get() {
          return this.#lineCount;
        }
      });
    }
  }
  get indent() {
    return this.#indent;
  }
  set indent(indent = 0) {
    if (!(indent >= 0 && Number.isInteger(indent))) {
      throw new Error("The `indent` option must be an integer from 0 and up");
    }
    this.#indent = indent;
    this.#updateLineCount();
  }
  get interval() {
    return this.#initialInterval ?? this.#spinner.interval ?? 100;
  }
  get spinner() {
    return this.#spinner;
  }
  set spinner(spinner) {
    this.#frameIndex = -1;
    this.#initialInterval = void 0;
    if (typeof spinner === "object") {
      if (spinner.frames === void 0) {
        throw new Error("The given spinner must have a `frames` property");
      }
      this.#spinner = spinner;
    } else if (!isUnicodeSupported2()) {
      this.#spinner = import_cli_spinners.default.line;
    } else if (spinner === void 0) {
      this.#spinner = import_cli_spinners.default.dots;
    } else if (spinner !== "default" && import_cli_spinners.default[spinner]) {
      this.#spinner = import_cli_spinners.default[spinner];
    } else {
      throw new Error(`There is no built-in spinner named '${spinner}'. See https://github.com/sindresorhus/cli-spinners/blob/main/spinners.json for a full list.`);
    }
  }
  get text() {
    return this.#text;
  }
  set text(value = "") {
    this.#text = value;
    this.#updateLineCount();
  }
  get prefixText() {
    return this.#prefixText;
  }
  set prefixText(value = "") {
    this.#prefixText = value;
    this.#updateLineCount();
  }
  get suffixText() {
    return this.#suffixText;
  }
  set suffixText(value = "") {
    this.#suffixText = value;
    this.#updateLineCount();
  }
  get isSpinning() {
    return this.#id !== void 0;
  }
  #getFullPrefixText(prefixText = this.#prefixText, postfix = " ") {
    if (typeof prefixText === "string" && prefixText !== "") {
      return prefixText + postfix;
    }
    if (typeof prefixText === "function") {
      return prefixText() + postfix;
    }
    return "";
  }
  #getFullSuffixText(suffixText = this.#suffixText, prefix = " ") {
    if (typeof suffixText === "string" && suffixText !== "") {
      return prefix + suffixText;
    }
    if (typeof suffixText === "function") {
      return prefix + suffixText();
    }
    return "";
  }
  #updateLineCount() {
    const columns = this.#stream.columns ?? 80;
    const fullPrefixText = this.#getFullPrefixText(this.#prefixText, "-");
    const fullSuffixText = this.#getFullSuffixText(this.#suffixText, "-");
    const fullText = " ".repeat(this.#indent) + fullPrefixText + "--" + this.#text + "--" + fullSuffixText;
    this.#lineCount = 0;
    for (const line of stripAnsi(fullText).split("\n")) {
      this.#lineCount += Math.max(1, Math.ceil(stringWidth(line, { countAnsiEscapeCodes: true }) / columns));
    }
  }
  get isEnabled() {
    return this.#isEnabled && !this.#isSilent;
  }
  set isEnabled(value) {
    if (typeof value !== "boolean") {
      throw new TypeError("The `isEnabled` option must be a boolean");
    }
    this.#isEnabled = value;
  }
  get isSilent() {
    return this.#isSilent;
  }
  set isSilent(value) {
    if (typeof value !== "boolean") {
      throw new TypeError("The `isSilent` option must be a boolean");
    }
    this.#isSilent = value;
  }
  frame() {
    const now = Date.now();
    if (this.#frameIndex === -1 || now - this.#lastSpinnerFrameTime >= this.interval) {
      this.#frameIndex = ++this.#frameIndex % this.#spinner.frames.length;
      this.#lastSpinnerFrameTime = now;
    }
    const { frames } = this.#spinner;
    let frame = frames[this.#frameIndex];
    if (this.color) {
      frame = source_default2[this.color](frame);
    }
    const fullPrefixText = typeof this.#prefixText === "string" && this.#prefixText !== "" ? this.#prefixText + " " : "";
    const fullText = typeof this.text === "string" ? " " + this.text : "";
    const fullSuffixText = typeof this.#suffixText === "string" && this.#suffixText !== "" ? " " + this.#suffixText : "";
    return fullPrefixText + frame + fullText + fullSuffixText;
  }
  clear() {
    if (!this.#isEnabled || !this.#stream.isTTY) {
      return this;
    }
    this.#stream.cursorTo(0);
    for (let index = 0; index < this.#linesToClear; index++) {
      if (index > 0) {
        this.#stream.moveCursor(0, -1);
      }
      this.#stream.clearLine(1);
    }
    if (this.#indent || this.lastIndent !== this.#indent) {
      this.#stream.cursorTo(this.#indent);
    }
    this.lastIndent = this.#indent;
    this.#linesToClear = 0;
    return this;
  }
  render() {
    if (this.#isSilent) {
      return this;
    }
    this.clear();
    this.#stream.write(this.frame());
    this.#linesToClear = this.#lineCount;
    return this;
  }
  start(text) {
    if (text) {
      this.text = text;
    }
    if (this.#isSilent) {
      return this;
    }
    if (!this.#isEnabled) {
      if (this.text) {
        this.#stream.write(`- ${this.text}
`);
      }
      return this;
    }
    if (this.isSpinning) {
      return this;
    }
    if (this.#options.hideCursor) {
      cli_cursor_default.hide(this.#stream);
    }
    if (this.#options.discardStdin && process11.stdin.isTTY) {
      this.#isDiscardingStdin = true;
      stdin_discarder_default.start();
    }
    this.render();
    this.#id = setInterval(this.render.bind(this), this.interval);
    return this;
  }
  stop() {
    if (!this.#isEnabled) {
      return this;
    }
    clearInterval(this.#id);
    this.#id = void 0;
    this.#frameIndex = 0;
    this.clear();
    if (this.#options.hideCursor) {
      cli_cursor_default.show(this.#stream);
    }
    if (this.#options.discardStdin && process11.stdin.isTTY && this.#isDiscardingStdin) {
      stdin_discarder_default.stop();
      this.#isDiscardingStdin = false;
    }
    return this;
  }
  succeed(text) {
    return this.stopAndPersist({ symbol: log_symbols_default.success, text });
  }
  fail(text) {
    return this.stopAndPersist({ symbol: log_symbols_default.error, text });
  }
  warn(text) {
    return this.stopAndPersist({ symbol: log_symbols_default.warning, text });
  }
  info(text) {
    return this.stopAndPersist({ symbol: log_symbols_default.info, text });
  }
  stopAndPersist(options = {}) {
    if (this.#isSilent) {
      return this;
    }
    const prefixText = options.prefixText ?? this.#prefixText;
    const fullPrefixText = this.#getFullPrefixText(prefixText, " ");
    const symbolText = options.symbol ?? " ";
    const text = options.text ?? this.text;
    const separatorText = symbolText ? " " : "";
    const fullText = typeof text === "string" ? separatorText + text : "";
    const suffixText = options.suffixText ?? this.#suffixText;
    const fullSuffixText = this.#getFullSuffixText(suffixText, " ");
    const textToWrite = fullPrefixText + symbolText + fullText + fullSuffixText + "\n";
    this.stop();
    this.#stream.write(textToWrite);
    return this;
  }
};
function ora(options) {
  return new Ora(options);
}

// packages/init-cli/src/index.ts
import fs7 from "node:fs";
import path6 from "node:path";
import os6 from "node:os";
import { spawn } from "node:child_process";

// packages/shamir/dist/index.js
import { randomBytes } from "crypto";
var EXP_TABLE = new Uint8Array(512);
var LOG_TABLE = new Uint8Array(256);
(() => {
  let x = 1;
  for (let i = 0; i < 255; i++) {
    EXP_TABLE[i] = x;
    LOG_TABLE[x] = i;
    x = x ^ x << 1 ^ (x >= 128 ? 283 : 0);
    x &= 255;
  }
  for (let i = 255; i < 512; i++) {
    EXP_TABLE[i] = EXP_TABLE[i - 255];
  }
})();
function gf256Add(a, b) {
  return a ^ b;
}
function gf256Mul(a, b) {
  if (a === 0 || b === 0)
    return 0;
  return EXP_TABLE[LOG_TABLE[a] + LOG_TABLE[b]];
}
function evalPoly(coeffs, x) {
  let result = 0;
  for (let i = coeffs.length - 1; i >= 0; i--) {
    result = gf256Add(gf256Mul(result, x), coeffs[i]);
  }
  return result;
}
function split(secret, n, k) {
  if (k < 2)
    throw new Error("Threshold k must be >= 2");
  if (n < k)
    throw new Error("Total shares n must be >= threshold k");
  if (n > 255)
    throw new Error("Maximum 255 shares");
  if (secret.length === 0)
    throw new Error("Secret must not be empty");
  const shares = Array.from({ length: n }, (_, i) => ({
    x: i + 1,
    y: new Uint8Array(secret.length)
  }));
  for (let byteIdx = 0; byteIdx < secret.length; byteIdx++) {
    const coeffs = new Uint8Array(k);
    coeffs[0] = secret[byteIdx];
    const rand = randomBytes(k - 1);
    for (let i = 1; i < k; i++) {
      coeffs[i] = rand[i - 1];
    }
    for (let i = 0; i < n; i++) {
      shares[i].y[byteIdx] = evalPoly(coeffs, shares[i].x);
    }
    coeffs.fill(0);
    rand.fill(0);
  }
  return shares;
}
function splitString(secret, n, k) {
  return split(new TextEncoder().encode(secret), n, k);
}
function serializeShare(share) {
  const buf = new Uint8Array(1 + share.y.length);
  buf[0] = share.x;
  buf.set(share.y, 1);
  return Buffer.from(buf).toString("base64");
}

// packages/init-cli/src/scan.ts
import fs from "node:fs";
import path from "node:path";
var DEFAULT_FILES = [".env", ".env.local", ".env.production", ".env.development"];
function stripQuotes(raw) {
  const t = raw.trim();
  if (t.startsWith('"') && t.endsWith('"') || t.startsWith("'") && t.endsWith("'")) {
    return t.slice(1, -1);
  }
  return t;
}
function detectProvider(varName, value, providers) {
  const normalizedVar = varName.toUpperCase();
  for (const p of providers) {
    let re;
    try {
      re = new RegExp(p.detect.regex);
    } catch {
      continue;
    }
    if (!re.test(value)) continue;
    if (p.detect.var_hint && !normalizedVar.includes(p.detect.var_hint.toUpperCase())) continue;
    return p;
  }
  return null;
}
function scanEnvFile(filePath, providers) {
  if (!fs.existsSync(filePath)) return [];
  const raw = fs.readFileSync(filePath, "utf-8");
  const lines = raw.split("\n");
  const findings = [];
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    if (!line || line.trim().startsWith("#")) continue;
    const m = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.+?)\s*$/);
    if (!m) continue;
    const [, varName, rawValue] = m;
    const value = stripQuotes(rawValue);
    const provider = detectProvider(varName, value, providers);
    if (provider) {
      findings.push({ file: filePath, line: i + 1, varName, value, provider });
    }
  }
  return findings;
}
function scanDirectory(dir, providers) {
  const findings = [];
  for (const name of DEFAULT_FILES) {
    findings.push(...scanEnvFile(path.join(dir, name), providers));
  }
  return findings;
}
function truncateKey(value) {
  if (value.length <= 12) return value;
  return value.slice(0, 8) + "..." + value.slice(-4);
}

// packages/init-cli/src/rewrite.ts
import fs2 from "node:fs";

// packages/init-cli/src/vault-secret.ts
var VAULT_SECRET_PROVIDER_PREFIX = "vaultenv-";
var PLACEHOLDER_PREFIX = "vaultproof://";
var MAX_PROVIDER_ID_LENGTH = 32;
var PUBLIC_NAME_PATTERNS = [
  /^NEXT_PUBLIC_/,
  /^VITE_/,
  /^PUBLIC_/,
  /(?:^|_)PUBLIC_KEY$/,
  /(?:^|_)PUBLISHABLE_KEY$/,
  /(?:^|_)ANON_KEY$/,
  /(?:^|_)PRICE_ID$/,
  /(?:^|_)CLIENT_ID$/
];
var PROXYABLE_NAME_PATTERNS = [
  /(?:^|_)API_KEY$/,
  /(?:^|_)ACCESS_TOKEN$/,
  /(?:^|_)AUTH_TOKEN$/,
  /(?:^|_)BEARER_TOKEN$/,
  /(?:^|_)SERVICE_TOKEN$/,
  /(?:^|_)PRIVATE_TOKEN$/,
  /(?:^|_)TOKEN$/
];
var VAULT_SECRET_NAME_PATTERNS = [
  /^DATABASE_URL$/,
  /^POSTGRES(?:QL)?_URL$/,
  /^POSTGRES_PRISMA_URL$/,
  /^MYSQL_URL$/,
  /^MARIADB_URL$/,
  /^MONGODB_URI$/,
  /^MONGO_URI$/,
  /^REDIS_URL$/,
  /^UPSTASH_REDIS_REST_TOKEN$/,
  /^JWT_SECRET$/,
  /^SESSION_SECRET$/,
  /^AUTH_SECRET$/,
  /^NEXTAUTH_SECRET$/,
  /^ENCRYPTION_KEY$/,
  /^SIGNING_KEY$/,
  /(?:^|_)DATABASE_URL$/,
  /(?:^|_)DB_URL$/,
  /(?:^|_)POSTGRES(?:QL)?_URL$/,
  /(?:^|_)MYSQL_URL$/,
  /(?:^|_)MONGODB_URI$/,
  /(?:^|_)MONGO_URI$/,
  /(?:^|_)REDIS_URL$/,
  /(?:^|_)JWT_SECRET$/,
  /(?:^|_)SESSION_SECRET$/,
  /(?:^|_)AUTH_SECRET$/,
  /(?:^|_)COOKIE_SECRET$/,
  /(?:^|_)CSRF_SECRET$/,
  /(?:^|_)WEBHOOK_SECRET$/,
  /(?:^|_)SIGNING_SECRET$/,
  /(?:^|_)CLIENT_SECRET$/,
  /(?:^|_)ENCRYPTION_KEY$/,
  /(?:^|_)SIGNING_KEY$/,
  /(?:^|_)PRIVATE_KEY$/,
  /(?:^|_)SECRET$/
];
var NON_SECRET_VALUES = /* @__PURE__ */ new Set([
  "true",
  "false",
  "null",
  "undefined",
  "development",
  "production",
  "staging",
  "test"
]);
function isVaultSecretPlaceholder(value) {
  return value.trim().startsWith(PLACEHOLDER_PREFIX);
}
function vaultSecretPlaceholder(envVar) {
  return `${PLACEHOLDER_PREFIX}${envVar}`;
}
function isLikelyVaultSecretEntry(entry) {
  const name = entry.name.toUpperCase();
  const value = entry.value.trim();
  if (!value || value.length < 8) return false;
  if (isVaultSecretPlaceholder(value)) return false;
  if (value.startsWith("vp-proj-")) return false;
  if (name.startsWith("VAULTPROOF_")) return false;
  if (NON_SECRET_VALUES.has(value.toLowerCase())) return false;
  const isVaultName = VAULT_SECRET_NAME_PATTERNS.some((pattern) => pattern.test(name));
  if (!isVaultName) return false;
  if (PUBLIC_NAME_PATTERNS.some((pattern) => pattern.test(name))) return false;
  if (PROXYABLE_NAME_PATTERNS.some((pattern) => pattern.test(name)) && name !== "UPSTASH_REDIS_REST_TOKEN") {
    return false;
  }
  return true;
}
function deriveVaultSecretProviderId(envVar) {
  const slug = envVar.toLowerCase().replace(/[^a-z0-9]+/gu, "-").replace(/^-+|-+$/gu, "") || "secret";
  const hash = stableHash(envVar).slice(0, 6);
  const maxSlugLength = MAX_PROVIDER_ID_LENGTH - VAULT_SECRET_PROVIDER_PREFIX.length - hash.length - 1;
  return `${VAULT_SECRET_PROVIDER_PREFIX}${slug.slice(0, maxSlugLength).replace(/-+$/u, "")}-${hash}`;
}
function formatDotEnvValue(value) {
  if (/^[A-Za-z0-9_./:@%+=,-]+$/u.test(value)) return value;
  return JSON.stringify(value);
}
function stableHash(value) {
  let hash = 2166136261;
  for (let i = 0; i < value.length; i++) {
    hash ^= value.charCodeAt(i);
    hash = Math.imul(hash, 16777619) >>> 0;
  }
  return hash.toString(36).padStart(6, "0");
}

// packages/init-cli/src/rewrite.ts
function rewriteEnvFile(envPath, findings, opts) {
  if (!fs2.existsSync(envPath)) return { rewritten: 0, manualNotes: [] };
  const relevant = findings.filter((f) => f.file === envPath);
  if (relevant.length === 0) return { rewritten: 0, manualNotes: [] };
  const original = fs2.readFileSync(envPath, "utf-8");
  const lines = original.split("\n");
  const byLine = /* @__PURE__ */ new Map();
  for (const f of relevant) byLine.set(f.line, f);
  const providersToConfigure = /* @__PURE__ */ new Map();
  for (const f of relevant) providersToConfigure.set(f.provider.id, f.provider);
  const managedBaseUrlVars = /* @__PURE__ */ new Set();
  for (const p of providersToConfigure.values()) {
    if (p.base_url_env_var) managedBaseUrlVars.add(p.base_url_env_var);
  }
  managedBaseUrlVars.add("VAULTPROOF_PROJECT_ID");
  const swapped = lines.map((line, idx) => {
    const lineNo = idx + 1;
    const finding = byLine.get(lineNo);
    if (!finding) return line;
    return `${finding.varName}=${opts.projectId}`;
  });
  const cleaned = [];
  let inManagedBlock = false;
  for (const line of swapped) {
    if (line.startsWith("# \u2500\u2500 VaultProof \u2500\u2500")) {
      inManagedBlock = true;
      continue;
    }
    if (inManagedBlock) {
      if (line.startsWith("# \u2500\u2500\u2500\u2500")) {
        inManagedBlock = false;
        continue;
      }
      if (line.startsWith("#") || line === "") continue;
      const m2 = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=/);
      if (m2 && managedBaseUrlVars.has(m2[1])) continue;
      inManagedBlock = false;
    }
    const m = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=/);
    if (m && managedBaseUrlVars.has(m[1])) continue;
    cleaned.push(line);
  }
  const manualNotes = [];
  const header = buildHeader(opts, providersToConfigure, manualNotes);
  const result = header + "\n" + cleaned.join("\n");
  fs2.writeFileSync(envPath, result);
  return { rewritten: relevant.length, manualNotes };
}
function rewriteEnvFileForMigration(envPath, entries, opts) {
  if (entries.length === 0) return { written: 0, manualNotes: [] };
  let original = "";
  if (fs2.existsSync(envPath)) {
    original = fs2.readFileSync(envPath, "utf-8");
  }
  const lines = original.split("\n");
  const providersToConfigure = /* @__PURE__ */ new Map();
  for (const e of entries) providersToConfigure.set(e.provider.id, e.provider);
  const managedVars = /* @__PURE__ */ new Set();
  for (const e of entries) managedVars.add(e.envVar);
  for (const p of providersToConfigure.values()) {
    if (p.base_url_env_var) managedVars.add(p.base_url_env_var);
  }
  managedVars.add("VAULTPROOF_PROJECT_ID");
  const cleaned = [];
  let inManagedBlock = false;
  for (const line of lines) {
    if (line.startsWith("# \u2500\u2500 VaultProof \u2500\u2500")) {
      inManagedBlock = true;
      continue;
    }
    if (inManagedBlock) {
      if (line.startsWith("# \u2500\u2500\u2500\u2500")) {
        inManagedBlock = false;
        continue;
      }
      if (line.startsWith("#") || line === "") continue;
      const m2 = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=/);
      if (m2 && managedVars.has(m2[1])) continue;
      inManagedBlock = false;
    }
    const m = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=/);
    if (m && managedVars.has(m[1])) continue;
    cleaned.push(line);
  }
  const manualNotes = [];
  const header = buildHeader(opts, providersToConfigure, manualNotes);
  const migrated = [];
  for (const e of entries) {
    migrated.push(`${e.envVar}=${opts.projectId}`);
  }
  const result = header + "\n" + migrated.join("\n") + "\n" + cleaned.join("\n");
  fs2.writeFileSync(envPath, result);
  return { written: entries.length, manualNotes };
}
function rewriteEnvFileForVaultSecrets(envPath, secrets, opts) {
  if (secrets.length === 0) return { rewritten: 0 };
  if (!fs2.existsSync(envPath)) return { rewritten: 0 };
  const relevant = secrets.filter((secret) => secret.file === envPath);
  if (relevant.length === 0) return { rewritten: 0 };
  const original = fs2.readFileSync(envPath, "utf-8");
  const lines = original.split("\n");
  const secretNames = new Set(relevant.map((secret) => secret.name));
  let projectIdSeen = false;
  const rewrittenNames = /* @__PURE__ */ new Set();
  const rewritten = lines.map((line) => {
    const m = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=/);
    if (!m) return line;
    if (m[1] === "VAULTPROOF_PROJECT_ID") {
      projectIdSeen = true;
      return `VAULTPROOF_PROJECT_ID=${opts.projectId}`;
    }
    if (!secretNames.has(m[1])) return line;
    rewrittenNames.add(m[1]);
    return `${m[1]}=${vaultSecretPlaceholder(m[1])}`;
  });
  const result = projectIdSeen ? rewritten.join("\n") : buildHeader(opts, /* @__PURE__ */ new Map(), []) + "\n" + rewritten.join("\n");
  fs2.writeFileSync(envPath, result);
  return { rewritten: rewrittenNames.size };
}
function buildHeader(opts, providers, manualNotes) {
  const out = [
    "# \u2500\u2500 VaultProof \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
    "# One project ID for every protected key. Safe to commit.",
    "# Generated by `vaultproof-init` \u2014 do not hand-edit values.",
    `VAULTPROOF_PROJECT_ID=${opts.projectId}`
  ];
  for (const p of providers.values()) {
    if (p.base_url_env_var && p.base_url_path_suffix) {
      out.push(`${p.base_url_env_var}=${opts.proxyBaseUrl}${p.base_url_path_suffix}`);
    } else if (p.note) {
      manualNotes.push(`${p.label}: ${p.note}`);
    } else {
      manualNotes.push(
        `${p.label}: set your client's base URL to ${opts.proxyBaseUrl}/p/${p.id}`
      );
    }
  }
  out.push("# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500");
  return out.join("\n");
}

// packages/init-cli/src/config.ts
import fs3 from "node:fs";
import path2 from "node:path";
import os4 from "node:os";
var CONFIG_DIR = path2.join(os4.homedir(), ".vaultproof");
var CONFIG_FILE = path2.join(CONFIG_DIR, "config.json");
function readConfig() {
  try {
    if (fs3.existsSync(CONFIG_FILE)) {
      return JSON.parse(fs3.readFileSync(CONFIG_FILE, "utf-8"));
    }
  } catch {
  }
  return {};
}
function getInitWorkerUrl() {
  return process.env.VAULTPROOF_INIT_URL || "https://init.vaultproof.dev";
}
function getProxyBaseUrl() {
  return process.env.VAULTPROOF_PROXY_URL || "https://init.vaultproof.dev";
}
function isJwtExpired(token) {
  try {
    const payload = JSON.parse(Buffer.from(token.split(".")[1], "base64").toString());
    return typeof payload.exp === "number" && payload.exp * 1e3 < Date.now();
  } catch {
    return true;
  }
}
function getJwt() {
  const envJwt = process.env.VAULTPROOF_JWT;
  if (envJwt) return isJwtExpired(envJwt) ? void 0 : envJwt;
  const token = readConfig().token;
  if (token && !isJwtExpired(token)) return token;
  return void 0;
}

// packages/init-cli/src/providers.ts
import fs4 from "node:fs";
import path3 from "node:path";
import { fileURLToPath } from "node:url";
var __dirname = path3.dirname(fileURLToPath(import.meta.url));
var REMOTE_URL = process.env.VAULTPROOF_PROVIDERS_URL || "https://vaultproof.dev/providers.json";
var FETCH_TIMEOUT_MS = 2e3;
async function loadProviders() {
  try {
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), FETCH_TIMEOUT_MS);
    const res = await fetch(REMOTE_URL, { signal: controller.signal });
    clearTimeout(timeout);
    if (res.ok) {
      const data = await res.json();
      if (isValidCatalog(data)) return data;
    }
  } catch {
  }
  return loadBundled();
}
function loadBundled() {
  const candidates = [
    path3.join(__dirname, "..", "providers.json"),
    path3.join(__dirname, "providers.json")
  ];
  for (const p of candidates) {
    if (fs4.existsSync(p)) {
      const data = JSON.parse(fs4.readFileSync(p, "utf-8"));
      if (isValidCatalog(data)) return data;
    }
  }
  throw new Error(
    "Could not load providers.json from remote or bundled fallback. Check your network or reinstall VaultProof init."
  );
}
function isValidCatalog(v) {
  if (!v || typeof v !== "object") return false;
  const c = v;
  return typeof c.version === "number" && Array.isArray(c.providers);
}

// packages/init-cli/src/legacy.ts
var DEFAULT_LEGACY_API = "https://api.vaultproof.dev";
function getLegacyApiUrl() {
  return process.env.VAULTPROOF_LEGACY_API_URL || DEFAULT_LEGACY_API;
}
function parseLegacyList(raw) {
  if (!raw || typeof raw !== "object" || !Array.isArray(raw.keys)) {
    return { keys: [] };
  }
  const rawKeys = raw.keys;
  const keys = [];
  for (const r of rawKeys) {
    if (!r || typeof r !== "object") continue;
    const k = r;
    if (typeof k.id !== "string" || typeof k.provider !== "string") continue;
    keys.push({
      id: k.id,
      provider: k.provider,
      label: typeof k.label === "string" ? k.label : null,
      envVar: typeof k.envVar === "string" ? k.envVar : null,
      createdAt: typeof k.createdAt === "string" ? k.createdAt : ""
    });
  }
  return { keys };
}
async function listLegacyKeys(vpLiveKey) {
  if (!vpLiveKey || !vpLiveKey.startsWith("vp_live_")) {
    return { keys: [] };
  }
  const url = `${getLegacyApiUrl()}/api/v1/sdk/keys`;
  try {
    const res = await fetch(url, {
      method: "GET",
      headers: { "X-API-Key": vpLiveKey }
    });
    if (!res.ok) return { keys: [] };
    const raw = await res.json();
    return parseLegacyList(raw);
  } catch {
    return { keys: [] };
  }
}

// packages/init-cli/src/index.ts
init_prompts();

// packages/init-cli/src/login.ts
import http from "node:http";
import fs5 from "node:fs";
import path4 from "node:path";
import os5 from "node:os";
var CONFIG_DIR2 = path4.join(os5.homedir(), ".vaultproof");
var CONFIG_FILE2 = path4.join(CONFIG_DIR2, "config.json");
var LOGIN_URL = "https://vaultproof.dev/app/login";
var TIMEOUT_MS = 12e4;
function randomState() {
  const bytes = new Uint8Array(16);
  crypto.getRandomValues(bytes);
  return Array.from(bytes).map((b) => b.toString(16).padStart(2, "0")).join("");
}
function saveConfig(token, email) {
  if (!fs5.existsSync(CONFIG_DIR2)) {
    fs5.mkdirSync(CONFIG_DIR2, { recursive: true, mode: 448 });
  }
  const existing = fs5.existsSync(CONFIG_FILE2) ? JSON.parse(fs5.readFileSync(CONFIG_FILE2, "utf-8")) : {};
  const updated = { ...existing, token, email };
  fs5.writeFileSync(CONFIG_FILE2, JSON.stringify(updated, null, 2) + "\n", { mode: 384 });
}
async function browserLogin() {
  const state = randomState();
  return new Promise((resolve) => {
    const server = http.createServer((req, res) => {
      if (!req.url?.startsWith("/callback")) {
        res.writeHead(404);
        res.end();
        return;
      }
      const params = new URL(req.url, `http://127.0.0.1`).searchParams;
      const token = params.get("token");
      const email = params.get("email") || "";
      const returnedState = params.get("state") || "";
      if (returnedState !== state) {
        res.writeHead(400, { "Content-Type": "text/html" });
        res.end('<html><body style="background:#0a0a0f;color:#ef4444;font-family:system-ui;display:flex;align-items:center;justify-content:center;height:100vh;margin:0"><div style="text-align:center"><h1>Login Failed</h1><p>State mismatch. Please try again.</p></div></body></html>');
        cleanup(null);
        return;
      }
      if (!token) {
        res.writeHead(400, { "Content-Type": "text/html" });
        res.end('<html><body style="background:#0a0a0f;color:#ef4444;font-family:system-ui;display:flex;align-items:center;justify-content:center;height:100vh;margin:0"><div style="text-align:center"><h1>Login Failed</h1><p>No token received. Please try again.</p></div></body></html>');
        cleanup(null);
        return;
      }
      saveConfig(token, email);
      res.writeHead(200, { "Content-Type": "text/html" });
      res.end('<html><body style="background:#0a0a0f;color:#22c55e;font-family:system-ui;display:flex;align-items:center;justify-content:center;height:100vh;margin:0"><div style="text-align:center"><h1 style="font-size:2rem">&#10003; Logged in</h1><p style="color:#6b7280;margin-top:0.5rem">You can close this tab and return to your terminal.</p></div></body></html>');
      cleanup({ token, email });
    });
    let timeoutHandle;
    function cleanup(result) {
      clearTimeout(timeoutHandle);
      server.close();
      resolve(result);
    }
    server.listen(0, "127.0.0.1", () => {
      const addr = server.address();
      if (!addr || typeof addr === "string") {
        cleanup(null);
        return;
      }
      const port = addr.port;
      const callbackUrl = `http://127.0.0.1:${port}/callback`;
      const loginUrl = `${LOGIN_URL}?cli_callback=${encodeURIComponent(callbackUrl)}&state=${encodeURIComponent(state)}`;
      const openCmd = process.platform === "darwin" ? "open" : process.platform === "win32" ? "start" : "xdg-open";
      import("node:child_process").then(({ exec }) => {
        exec(`${openCmd} "${loginUrl}"`);
      });
      timeoutHandle = setTimeout(() => {
        cleanup(null);
      }, TIMEOUT_MS);
    });
  });
}

// packages/init-cli/src/custom-provider.ts
var MAX_URL_LENGTH = 500;
var MAX_HOSTNAME_LENGTH = 253;
var MAX_LABEL_LENGTH = 63;
var LABEL_REGEX = /^[A-Za-z](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?$/;
var SLUG_REGEX = /^[a-z0-9][a-z0-9-]{0,31}$/;
var HEADER_NAME_REGEX = /^[A-Za-z][A-Za-z0-9-]{0,63}$/;
var ENV_VAR_REGEX = /^[A-Za-z_][A-Za-z0-9_]{0,127}$/;
var HOSTNAME_BLOCKLIST = /* @__PURE__ */ new Set([
  "localhost",
  "localhost.localdomain",
  "ip6-localhost",
  "ip6-loopback",
  "metadata.google.internal",
  "metadata.goog",
  "instance-data.ec2.internal",
  "kubernetes.default",
  "kubernetes.default.svc"
]);
var HOSTNAME_SUFFIX_BLOCKLIST = [
  ".local",
  ".internal",
  ".localhost",
  ".localdomain",
  ".arpa",
  ".workers.dev",
  ".cloudflare.com",
  ".cloudflareaccess.com",
  ".cfargotunnel.com",
  ".svc.cluster.local"
];
var RESERVED_SLUGS = /* @__PURE__ */ new Set(["api", "health", "admin", "p"]);
var SECRET_NAME_PATTERNS = [
  /(?:^|_)API_KEY$/,
  /(?:^|_)SECRET_KEY$/,
  /(?:^|_)ACCESS_TOKEN$/,
  /(?:^|_)AUTH_TOKEN$/,
  /(?:^|_)BEARER_TOKEN$/,
  /(?:^|_)SERVICE_TOKEN$/,
  /(?:^|_)PRIVATE_TOKEN$/,
  /(?:^|_)TOKEN$/
];
var PUBLIC_NAME_PATTERNS2 = [
  /^NEXT_PUBLIC_/,
  /^VITE_/,
  /^PUBLIC_/,
  /(?:^|_)PUBLIC_KEY$/,
  /(?:^|_)PUBLISHABLE_KEY$/,
  /(?:^|_)ANON_KEY$/,
  /(?:^|_)WEBHOOK_SECRET$/,
  /(?:^|_)WEBHOOK$/,
  /(?:^|_)PRICE_ID$/,
  /(?:^|_)CLIENT_ID$/,
  /(?:^|_)BASE_URL$/,
  /(?:^|_)URL$/
];
var NON_SECRET_VALUES2 = /* @__PURE__ */ new Set([
  "true",
  "false",
  "null",
  "undefined",
  "development",
  "production",
  "staging",
  "test"
]);
function isLikelyCustomSecretEntry(entry) {
  const name = entry.name.toUpperCase();
  const value = entry.value.trim();
  if (!value || value.startsWith("vp-proj-")) return false;
  if (name.startsWith("VAULTPROOF_")) return false;
  if (PUBLIC_NAME_PATTERNS2.some((pattern) => pattern.test(name))) return false;
  if (!SECRET_NAME_PATTERNS.some((pattern) => pattern.test(name))) return false;
  if (value.length < 12) return false;
  if (NON_SECRET_VALUES2.has(value.toLowerCase())) return false;
  if (/^https?:\/\//i.test(value)) return false;
  return true;
}
function deriveCustomProviderId(envVar) {
  const cleaned = envVar.toLowerCase().replace(/(?:_api_key|_secret_key|_access_token|_auth_token|_bearer_token|_service_token|_private_token|_token)$/u, "").replace(/_api$/u, "").replace(/[^a-z0-9]+/gu, "-").replace(/^-+|-+$/gu, "").slice(0, 32);
  return cleaned || "custom-api";
}
function deriveCustomLabel(slug) {
  return slug.split("-").filter(Boolean).map((part) => part.slice(0, 1).toUpperCase() + part.slice(1)).join(" ") || "Custom API";
}
function deriveBaseUrlEnvVar(envVar) {
  const upper = envVar.toUpperCase();
  if (upper.endsWith("_API_KEY")) return upper.replace(/_API_KEY$/u, "_API_BASE_URL");
  if (upper.endsWith("_SECRET_KEY")) return upper.replace(/_SECRET_KEY$/u, "_BASE_URL");
  if (upper.endsWith("_ACCESS_TOKEN")) return upper.replace(/_ACCESS_TOKEN$/u, "_BASE_URL");
  if (upper.endsWith("_AUTH_TOKEN")) return upper.replace(/_AUTH_TOKEN$/u, "_BASE_URL");
  if (upper.endsWith("_BEARER_TOKEN")) return upper.replace(/_BEARER_TOKEN$/u, "_BASE_URL");
  if (upper.endsWith("_SERVICE_TOKEN")) return upper.replace(/_SERVICE_TOKEN$/u, "_BASE_URL");
  if (upper.endsWith("_PRIVATE_TOKEN")) return upper.replace(/_PRIVATE_TOKEN$/u, "_BASE_URL");
  if (upper.endsWith("_TOKEN")) return upper.replace(/_TOKEN$/u, "_BASE_URL");
  return `${upper}_BASE_URL`;
}
function validateProviderSlug(slug) {
  if (!SLUG_REGEX.test(slug)) {
    return { ok: false, error: "Slug must be 1-32 chars: lowercase letters, numbers, and hyphens only" };
  }
  if (RESERVED_SLUGS.has(slug)) {
    return { ok: false, error: `Slug is reserved: ${slug}` };
  }
  if (slug.startsWith("vaultenv-")) {
    return { ok: false, error: "Slug prefix is reserved for VaultProof env secrets" };
  }
  return { ok: true };
}
function validateCustomUpstreamUrl(raw) {
  if (typeof raw !== "string" || raw.length === 0) {
    return { ok: false, error: "Upstream URL must be a non-empty string" };
  }
  if (raw.length > MAX_URL_LENGTH) {
    return { ok: false, error: `Upstream URL exceeds ${MAX_URL_LENGTH} characters` };
  }
  if (/[\r\n\t\0]/u.test(raw)) {
    return { ok: false, error: "Upstream URL contains control characters" };
  }
  let url;
  try {
    url = new URL(raw);
  } catch {
    return { ok: false, error: "Upstream URL is not a valid URL" };
  }
  if (url.protocol !== "https:") {
    return { ok: false, error: `Scheme not allowed: ${url.protocol} (only https: is permitted)` };
  }
  if (url.username || url.password) {
    return { ok: false, error: "Upstream URL must not contain embedded credentials" };
  }
  if (url.port && url.port !== "443") {
    return { ok: false, error: `Port not allowed: ${url.port} (only 443 is permitted)` };
  }
  const host = url.hostname.toLowerCase();
  if (host.length === 0 || host.length > MAX_HOSTNAME_LENGTH) {
    return { ok: false, error: "Hostname length out of range" };
  }
  if (host.includes(":")) {
    return { ok: false, error: "IPv6 hostnames are not allowed" };
  }
  if (/^[0-9a-fx.]+$/iu.test(host) && !/[a-wyz]/iu.test(host)) {
    return { ok: false, error: "IP literal hostnames are not allowed" };
  }
  if (!host.includes(".")) {
    return { ok: false, error: "Hostname must be a fully-qualified domain" };
  }
  if (HOSTNAME_BLOCKLIST.has(host)) {
    return { ok: false, error: `Hostname is blocked: ${host}` };
  }
  for (const suffix of HOSTNAME_SUFFIX_BLOCKLIST) {
    if (host === suffix.slice(1) || host.endsWith(suffix)) {
      return { ok: false, error: `Hostname suffix is blocked: ${suffix}` };
    }
  }
  const labels = host.split(".");
  for (const label of labels) {
    if (label.length === 0 || label.length > MAX_LABEL_LENGTH) {
      return { ok: false, error: "Hostname label length out of range" };
    }
    if (!LABEL_REGEX.test(label)) {
      return { ok: false, error: `Invalid hostname label: ${label}` };
    }
    if (label.startsWith("xn--")) {
      return { ok: false, error: "Punycode hostnames are not allowed" };
    }
  }
  const tld = labels[labels.length - 1];
  if (!/^[A-Za-z]{2,}$/u.test(tld)) {
    return { ok: false, error: "TLD must be alphabetic" };
  }
  const normalizedUrl = `https://${host}${url.pathname === "/" ? "" : url.pathname.replace(/\/+$/u, "")}`;
  return { ok: true, normalizedUrl };
}
function validateCustomHeaderName(name) {
  if (!HEADER_NAME_REGEX.test(name)) {
    return { ok: false, error: "Header name must start with a letter and contain only letters, numbers, and hyphens" };
  }
  const lower = name.toLowerCase();
  if ((/* @__PURE__ */ new Set(["host", "content-length", "transfer-encoding", "connection"])).has(lower)) {
    return { ok: false, error: `Header name is reserved: ${name}` };
  }
  return { ok: true };
}
function validateEnvVarName(name) {
  if (!ENV_VAR_REGEX.test(name)) {
    return { ok: false, error: "Environment variable name must start with a letter or underscore and contain only letters, numbers, and underscores" };
  }
  return { ok: true };
}
function validateCustomHeaderTemplate(template) {
  if (template.length === 0 || template.length > 256) {
    return { ok: false, error: "Header template must be 1-256 characters" };
  }
  if (/[\r\n\0]/u.test(template) || /[\x00-\x1F\x7F]/u.test(template)) {
    return { ok: false, error: "Header template contains control characters" };
  }
  if (!template.includes("{key}")) {
    return { ok: false, error: "Header template must contain {key}" };
  }
  return { ok: true };
}
function buildCustomProviderSpec(input) {
  return {
    id: input.id,
    label: input.label,
    upstream_base_url: input.upstreamBaseUrl,
    auth_header_name: input.authHeaderName,
    auth_header_template: input.authHeaderTemplate,
    env_var_default: input.envVar,
    base_url_env_var: input.baseUrlEnvVar,
    base_url_path_suffix: `/p/${input.id}`,
    detect: {
      regex: "^[\\s\\S]+$",
      var_hint: input.envVar
    }
  };
}

// packages/init-cli/src/netops.ts
import fs6 from "node:fs";
import path5 from "node:path";
var NETOPS_ENV_NAME_PATTERNS = [
  /^ANSIBLE_(?:PASSWORD|BECOME_PASSWORD|BECOME_PASS|SSH_PASS|NET_PASSWORD|HTTPAPI_PASSWORD)$/,
  /^ENABLE_(?:SECRET|PASSWORD)$/,
  /^DEVICE_(?:PASSWORD|SECRET)$/,
  /^NETOPS_(?:PASSWORD|SECRET|TOKEN)$/,
  /^NORNIR_(?:PASSWORD|SECRET|TOKEN)$/,
  /^NETCONF_(?:PASSWORD|SECRET|TOKEN)$/,
  /^RESTCONF_(?:PASSWORD|SECRET|TOKEN)$/,
  /^SNMP_(?:COMMUNITY|AUTH_KEY|PRIV_KEY|PRIVACY_KEY)$/,
  /^RADIUS_(?:SECRET|SHARED_SECRET)$/,
  /^TACACS_(?:SECRET|SHARED_SECRET)$/,
  /^MERAKI_DASHBOARD_API_KEY$/,
  /^(?:PALO_ALTO|PANOS|FORTINET|FORTIGATE|MERAKI|CISCO|JUNIPER|ARISTA|NETBOX)_(?:API_KEY|API_TOKEN|TOKEN|PASSWORD|SECRET)$/
];
var ANSIBLE_KEY_TO_ENV_SUFFIX = {
  ansible_password: "ANSIBLE_PASSWORD",
  ansible_ssh_pass: "ANSIBLE_PASSWORD",
  ansible_become_password: "ANSIBLE_BECOME_PASSWORD",
  ansible_become_pass: "ANSIBLE_BECOME_PASSWORD",
  ansible_net_password: "ANSIBLE_NET_PASSWORD",
  ansible_httpapi_password: "ANSIBLE_HTTPAPI_PASSWORD",
  enable_secret: "ENABLE_SECRET",
  enable_password: "ENABLE_PASSWORD",
  device_password: "DEVICE_PASSWORD",
  device_secret: "DEVICE_SECRET",
  netconf_password: "NETCONF_PASSWORD",
  restconf_password: "RESTCONF_PASSWORD",
  snmp_community: "SNMP_COMMUNITY",
  snmp_auth_key: "SNMP_AUTH_KEY",
  snmp_priv_key: "SNMP_PRIV_KEY",
  snmp_privacy_key: "SNMP_PRIVACY_KEY",
  radius_secret: "RADIUS_SHARED_SECRET",
  radius_shared_secret: "RADIUS_SHARED_SECRET",
  tacacs_secret: "TACACS_SECRET",
  tacacs_shared_secret: "TACACS_SECRET",
  firewall_api_key: "FIREWALL_API_KEY",
  panos_api_key: "PANOS_API_KEY",
  palo_alto_api_key: "PALO_ALTO_API_KEY",
  fortinet_api_token: "FORTINET_API_TOKEN",
  fortigate_api_token: "FORTIGATE_API_TOKEN",
  meraki_dashboard_api_key: "MERAKI_DASHBOARD_API_KEY"
};
var TERRAFORM_SECRET_KEY_PATTERNS = [
  /password/i,
  /secret/i,
  /token/i,
  /api_key/i,
  /community/i,
  /private_key/i,
  /shared_key/i
];
var SKIP_DIRS = /* @__PURE__ */ new Set([
  ".git",
  ".next",
  ".turbo",
  "dist",
  "build",
  "coverage",
  "node_modules",
  "vendor"
]);
function scanNetOpsSecrets(cwd) {
  const files = collectNetOpsFiles(cwd);
  const findings = [];
  for (const file of files) {
    findings.push(...scanNetOpsFile(cwd, file));
  }
  return dedupeFindings(findings);
}
function rewriteNetOpsSecretFiles(secrets) {
  const byFile = /* @__PURE__ */ new Map();
  for (const secret of secrets) {
    if (secret.rewriteMode === "none") continue;
    const list = byFile.get(secret.file) || [];
    list.push(secret);
    byFile.set(secret.file, list);
  }
  const results = [];
  for (const [file, fileSecrets] of byFile) {
    if (!fs6.existsSync(file)) continue;
    const lines = fs6.readFileSync(file, "utf-8").split("\n");
    const byLine = /* @__PURE__ */ new Map();
    for (const secret of fileSecrets) byLine.set(secret.line, secret);
    let rewritten = 0;
    const nextLines = lines.map((line, index) => {
      const secret = byLine.get(index + 1);
      if (!secret) return line;
      const replacement = rewriteLine(line, secret);
      if (replacement === line) return line;
      rewritten++;
      return replacement;
    });
    if (rewritten > 0) {
      fs6.writeFileSync(file, nextLines.join("\n"));
      results.push({ file, rewritten });
    }
  }
  return results;
}
function collectNetOpsFiles(cwd) {
  const files = [];
  walk(cwd, files);
  return files.filter((file) => isNetOpsFile(cwd, file));
}
function walk(dir, out) {
  let entries;
  try {
    entries = fs6.readdirSync(dir, { withFileTypes: true });
  } catch {
    return;
  }
  for (const entry of entries) {
    if (entry.name.startsWith(".") && entry.name !== ".env" && !entry.name.startsWith(".env.")) {
      if (entry.isDirectory()) continue;
    }
    const fullPath = path5.join(dir, entry.name);
    if (entry.isDirectory()) {
      if (SKIP_DIRS.has(entry.name)) continue;
      walk(fullPath, out);
    } else if (entry.isFile()) {
      out.push(fullPath);
    }
  }
}
function isNetOpsFile(cwd, file) {
  const relative = path5.relative(cwd, file).replace(/\\/g, "/");
  const base = path5.basename(file);
  if (base === ".env" || /^\.env\.(local|production|development|staging)$/u.test(base)) return true;
  if (/^(inventory|hosts)\.ya?ml$/u.test(base)) return true;
  if (/^(terraform|[^/]+\.auto)\.tfvars$/u.test(base)) return true;
  if (/^(group_vars|host_vars)\//u.test(relative) && /\.ya?ml$/u.test(base)) return true;
  return false;
}
function scanNetOpsFile(cwd, file) {
  const relative = path5.relative(cwd, file).replace(/\\/g, "/");
  const base = path5.basename(file);
  const raw = fs6.readFileSync(file, "utf-8");
  const lines = raw.split("\n");
  const findings = [];
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    if (!line || line.trim().startsWith("#")) continue;
    if (base === ".env" || base.startsWith(".env.")) {
      const entry = parseAssignment(line);
      if (!entry) continue;
      if (!isNetOpsEnvSecret(entry.key, entry.value)) continue;
      findings.push({
        file,
        line: i + 1,
        name: entry.key.toUpperCase(),
        value: entry.value,
        sourceKey: entry.key,
        sourceKind: "env",
        rewriteMode: "env-placeholder",
        tool: "env"
      });
      continue;
    }
    if (/\.tfvars$/u.test(base)) {
      const entry = parseAssignment(line);
      if (!entry) continue;
      if (!isTerraformSecretKey(entry.key, entry.value)) continue;
      findings.push({
        file,
        line: i + 1,
        name: `TF_VAR_${entry.key}`,
        value: entry.value,
        sourceKey: entry.key,
        sourceKind: "terraform-tfvars",
        rewriteMode: "terraform-env-var",
        tool: "terraform"
      });
      continue;
    }
    if (isAnsibleYamlPath(relative)) {
      const entry = parseYamlScalar(line);
      if (!entry) continue;
      const suffix = ANSIBLE_KEY_TO_ENV_SUFFIX[entry.key.toLowerCase()];
      if (!suffix || !isSecretValue(entry.value)) continue;
      findings.push({
        file,
        line: i + 1,
        name: deriveScopedEnvName(cwd, file, suffix),
        value: entry.value,
        sourceKey: entry.key,
        sourceKind: "ansible-yaml",
        rewriteMode: "ansible-env-lookup",
        tool: "ansible"
      });
    }
  }
  return findings;
}
function parseAssignment(line) {
  const match = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.+?)\s*$/u);
  if (!match) return null;
  const value = stripInlineComment(stripQuotes2(match[2].trim()));
  if (!isSecretValue(value)) return null;
  return { key: match[1], value };
}
function parseYamlScalar(line) {
  const match = line.match(/^(\s*)([A-Za-z_][A-Za-z0-9_-]*)\s*:\s*(.+?)\s*$/u);
  if (!match) return null;
  const rawValue = stripInlineComment(match[3].trim());
  if (!rawValue || ["|", ">", "{}", "[]"].includes(rawValue)) return null;
  const value = stripQuotes2(rawValue);
  if (!isSecretValue(value)) return null;
  return { indent: match[1], key: match[2], value };
}
function stripQuotes2(raw) {
  const trimmed = raw.trim();
  if (trimmed.startsWith('"') && trimmed.endsWith('"') || trimmed.startsWith("'") && trimmed.endsWith("'")) {
    return trimmed.slice(1, -1);
  }
  return trimmed;
}
function stripInlineComment(raw) {
  const single = raw.match(/^'[^']*'/u);
  if (single) return single[0];
  const double = raw.match(/^"[^"]*"/u);
  if (double) return double[0];
  return raw.replace(/\s+#.*$/u, "").trim();
}
function isSecretValue(value) {
  const trimmed = value.trim();
  if (trimmed.length < 4) return false;
  if (trimmed.startsWith("vp-proj-") || isVaultSecretPlaceholder(trimmed)) return false;
  if (/\{\{\s*lookup\(['"]env['"]/u.test(trimmed)) return false;
  if (/^(true|false|null|undefined|development|production|staging|test)$/iu.test(trimmed)) return false;
  return true;
}
function isNetOpsEnvSecret(name, value) {
  if (!isSecretValue(value)) return false;
  const upper = name.toUpperCase();
  return NETOPS_ENV_NAME_PATTERNS.some((pattern) => pattern.test(upper));
}
function isTerraformSecretKey(key, value) {
  if (!isSecretValue(value)) return false;
  return TERRAFORM_SECRET_KEY_PATTERNS.some((pattern) => pattern.test(key));
}
function isAnsibleYamlPath(relative) {
  const base = path5.basename(relative);
  return /^(inventory|hosts)\.ya?ml$/u.test(base) || /^(group_vars|host_vars)\//u.test(relative);
}
function deriveScopedEnvName(cwd, file, suffix) {
  const relative = path5.relative(cwd, file).replace(/\\/g, "/");
  const parts = relative.split("/");
  if (parts[0] === "host_vars" && parts[1]) {
    return `${toEnvSegment(path5.basename(parts[1], path5.extname(parts[1])))}_${suffix}`;
  }
  if (parts[0] === "group_vars" && parts[1]) {
    const group = path5.basename(parts[1], path5.extname(parts[1]));
    if (group.toLowerCase() === "all") return suffix;
    return `${toEnvSegment(group)}_${suffix}`;
  }
  return `NETOPS_${suffix}`;
}
function toEnvSegment(value) {
  return value.toUpperCase().replace(/[^A-Z0-9]+/gu, "_").replace(/^_+|_+$/gu, "") || "NETOPS";
}
function rewriteLine(line, secret) {
  if (secret.rewriteMode === "env-placeholder") {
    return line.replace(/^(\s*[A-Za-z_][A-Za-z0-9_]*\s*=\s*).+?\s*$/u, `$1${vaultSecretPlaceholder(secret.name)}`);
  }
  if (secret.rewriteMode === "ansible-env-lookup") {
    const match = line.match(/^(\s*)([A-Za-z_][A-Za-z0-9_-]*)\s*:/u);
    const indent = match?.[1] || "";
    const key = match?.[2] || secret.sourceKey;
    return `${indent}${key}: "{{ lookup('env', '${secret.name}') }}"`;
  }
  if (secret.rewriteMode === "terraform-env-var") {
    const indent = line.match(/^(\s*)/u)?.[1] || "";
    return `${indent}# ${secret.sourceKey} managed by VaultProof via ${secret.name}`;
  }
  return line;
}
function dedupeFindings(findings) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const finding of findings) {
    const key = `${finding.file}:${finding.line}:${finding.name}`;
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(finding);
  }
  return out;
}

// packages/init-cli/src/index.ts
function printBanner() {
  const lines = [
    "\u2588\u2588\u2557   \u2588\u2588\u2557 \u2588\u2588\u2588\u2588\u2588\u2557 \u2588\u2588\u2557   \u2588\u2588\u2557\u2588\u2588\u2557  \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2557\u2588\u2588\u2588\u2588\u2588\u2588\u2557 \u2588\u2588\u2588\u2588\u2588\u2588\u2557  \u2588\u2588\u2588\u2588\u2588\u2588\u2557  \u2588\u2588\u2588\u2588\u2588\u2588\u2557 \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2557",
    "\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2551  \u255A\u2550\u2550\u2588\u2588\u2554\u2550\u2550\u255D\u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2554\u2550\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2554\u2550\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2554\u2550\u2550\u2550\u2550\u255D",
    "\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2551\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2551     \u2588\u2588\u2551   \u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D\u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2588\u2588\u2588\u2557  ",
    "\u255A\u2588\u2588\u2557 \u2588\u2588\u2554\u255D\u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2551\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2551     \u2588\u2588\u2551   \u2588\u2588\u2554\u2550\u2550\u2550\u255D \u2588\u2588\u2554\u2550\u2550\u2588\u2588\u2557\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2551   \u2588\u2588\u2551\u2588\u2588\u2554\u2550\u2550\u255D  ",
    " \u255A\u2588\u2588\u2588\u2588\u2554\u255D \u2588\u2588\u2551  \u2588\u2588\u2551\u255A\u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2557\u2588\u2588\u2551   \u2588\u2588\u2551     \u2588\u2588\u2551  \u2588\u2588\u2551\u255A\u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D\u255A\u2588\u2588\u2588\u2588\u2588\u2588\u2554\u255D\u2588\u2588\u2551     ",
    "  \u255A\u2550\u2550\u2550\u255D  \u255A\u2550\u255D  \u255A\u2550\u255D \u255A\u2550\u2550\u2550\u2550\u2550\u255D \u255A\u2550\u2550\u2550\u2550\u2550\u2550\u255D\u255A\u2550\u255D   \u255A\u2550\u255D     \u255A\u2550\u255D  \u255A\u2550\u255D \u255A\u2550\u2550\u2550\u2550\u2550\u255D  \u255A\u2550\u2550\u2550\u2550\u2550\u255D \u255A\u2550\u255D     "
  ];
  const SPLIT = 50;
  const grey = source_default.rgb(180, 180, 190);
  const cyan = source_default.rgb(0, 210, 210);
  const border = source_default.rgb(80, 120, 200);
  const width = lines[0].length + 4;
  const topBar = border("\u2554" + "\u2550".repeat(width) + "\u2557");
  const emptyRow = border("\u2551") + " ".repeat(width) + border("\u2551");
  const bottomBar = border("\u255A" + "\u2550".repeat(width) + "\u255D");
  const version = source_default.dim("  v0.1.1");
  console.log("\n" + topBar);
  console.log(emptyRow);
  for (const line of lines) {
    const left = grey.bold(line.slice(0, SPLIT));
    const right = cyan.bold(line.slice(SPLIT));
    const padded = left + right;
    console.log(border("\u2551") + "  " + padded + "  " + border("\u2551"));
  }
  console.log(emptyRow);
  console.log(bottomBar);
  console.log(version + "\n");
}
function parseArgs(argv) {
  const args = argv.slice(2);
  const flags = /* @__PURE__ */ new Set();
  const positionals = [];
  const passthroughIndex = args.indexOf("--");
  const cliArgs = passthroughIndex >= 0 ? args.slice(0, passthroughIndex) : args;
  const passthrough = passthroughIndex >= 0 ? args.slice(passthroughIndex + 1) : [];
  let cmd = "init";
  for (const a of cliArgs) {
    if (a.startsWith("-")) flags.add(a);
    else if (!a.startsWith("-")) positionals.push(a);
  }
  if (positionals.length > 0) cmd = positionals[0];
  return { cmd, flags, positionals, passthrough };
}
function printUsage() {
  console.log(source_default.dim("Usage:"));
  console.log(source_default.dim("  vaultproof-init [--yes|-y] [--dry-run] [--check-legacy]"));
  console.log(source_default.dim("  vaultproof-init [--custom]"));
  console.log(source_default.dim("  vaultproof-init custom"));
  console.log(source_default.dim("  vaultproof-init secrets add"));
  console.log(source_default.dim("  vaultproof-init secrets pull [--stdout]"));
  console.log(source_default.dim("  vaultproof-init netops"));
  console.log(source_default.dim("  vaultproof-init netops run -- <command>"));
  console.log(source_default.dim("  vaultproof-init run -- <command>"));
  console.log(source_default.dim("  vaultproof-init migrate-from-legacy"));
  console.log(source_default.dim("  vaultproof-init doctor"));
}
function readLegacyVpLiveKeyFromConfig() {
  const configPath = path6.join(os6.homedir(), ".vaultproof", "config.json");
  try {
    if (!fs7.existsSync(configPath)) return null;
    const raw = JSON.parse(fs7.readFileSync(configPath, "utf-8"));
    if (raw.apiKey && raw.apiKey.startsWith("vp_live_")) return raw.apiKey;
  } catch {
  }
  return null;
}
function readLegacyVpLiveKey() {
  const fromConfig = readLegacyVpLiveKeyFromConfig();
  if (fromConfig) return fromConfig;
  if (process.env.VAULTPROOF_API_KEY?.startsWith("vp_live_")) return process.env.VAULTPROOF_API_KEY;
  return null;
}
var ENV_SCAN_FILES = [".env", ".env.local", ".env.production", ".env.development"];
function getEnvFileStatuses(cwd) {
  return ENV_SCAN_FILES.map((name) => {
    const absolutePath = path6.join(cwd, name);
    return {
      file: name,
      absolutePath,
      exists: fs7.existsSync(absolutePath)
    };
  });
}
function parseEnvEntries(filePath) {
  if (!fs7.existsSync(filePath)) return [];
  const lines = fs7.readFileSync(filePath, "utf-8").split("\n");
  const out = [];
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    if (!line || line.trim().startsWith("#")) continue;
    const m = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.+?)\s*$/);
    if (!m) continue;
    const [, name, rawValue] = m;
    const value = rawValue.trim().replace(/^["']|["']$/g, "");
    out.push({ file: filePath, line: i + 1, name, value });
  }
  return out;
}
function classifyVaultProofMarker(name, value) {
  if (name === "VAULTPROOF_PROJECT_ID" && value.startsWith("vp-proj-")) {
    return "project ID already configured";
  }
  if (name === "VAULTPROOF_API_KEY" && (value.startsWith("vp_live_") || value.startsWith("vp_test_"))) {
    return "legacy VaultProof API key configured";
  }
  if (name === "VAULTPROOF_JWT" && value.length > 20) {
    return "JWT auth token present";
  }
  if (name.endsWith("_BASE_URL") && /(?:^https:\/\/)?(?:init|api)\.vaultproof\.dev/i.test(value)) {
    return "provider base URL already routed through VaultProof";
  }
  if ((name.endsWith("_API_KEY") || name.endsWith("_SECRET_KEY") || name.endsWith("_TOKEN")) && value.startsWith("vp-proj-")) {
    return "provider key already replaced with project ID";
  }
  if (value.startsWith("vaultproof://")) {
    return "vault-only secret placeholder";
  }
  return null;
}
function collectVaultProofMarkers(cwd, statuses) {
  const markers = [];
  for (const status of statuses) {
    if (!status.exists) continue;
    const entries = parseEnvEntries(status.absolutePath);
    for (const e of entries) {
      const reason = classifyVaultProofMarker(e.name, e.value);
      if (!reason) continue;
      markers.push({
        source: "file",
        name: e.name,
        value: e.value,
        reason,
        file: status.file,
        line: e.line
      });
    }
  }
  const processEnvCandidates = ["VAULTPROOF_PROJECT_ID", "VAULTPROOF_API_KEY", "VAULTPROOF_JWT"];
  for (const key of processEnvCandidates) {
    const value = process.env[key];
    if (!value) continue;
    const reason = classifyVaultProofMarker(key, value);
    if (!reason) continue;
    markers.push({
      source: "process-env",
      name: key,
      value,
      reason
    });
  }
  const legacyVpLive = readLegacyVpLiveKeyFromConfig();
  if (legacyVpLive) {
    markers.push({
      source: "legacy-config",
      name: "VAULTPROOF_API_KEY",
      value: legacyVpLive,
      reason: "legacy VaultProof API key found in ~/.vaultproof/config.json",
      file: "~/.vaultproof/config.json"
    });
  }
  return markers;
}
function displayMarkerValue(name, value) {
  if (name === "VAULTPROOF_JWT") return "<present>";
  if (name.endsWith("_BASE_URL")) return value;
  return truncateKey(value);
}
function printScanReport(statuses, providerCount, markers) {
  console.log(source_default.bold("Scan checks:\n"));
  console.log(source_default.dim("  Files checked for env keys:"));
  for (const s of statuses) {
    const icon = s.exists ? source_default.green("\u2713") : source_default.dim("\u2022");
    const state = s.exists ? source_default.white("found") : source_default.dim("not found");
    console.log(`  ${icon} ${s.file.padEnd(18)} ${state}`);
  }
  console.log(source_default.dim("\n  Detection performed:"));
  console.log(source_default.dim(`  \u2022 API key pattern matching against ${providerCount} provider signatures`));
  console.log(source_default.dim("  \u2022 Existing VaultProof markers (project ID, legacy key, routed base URLs)"));
  if (markers.length === 0) {
    console.log(source_default.dim("\n  Existing VaultProof config: none detected\n"));
    return;
  }
  console.log(source_default.yellow("\n  Existing VaultProof config detected:"));
  for (const m of markers) {
    if (m.source === "file") {
      console.log(
        `  ${source_default.yellow("\u2022")} ${m.name}=${source_default.white(displayMarkerValue(m.name, m.value))} ` + source_default.dim(`(${m.reason}; ${m.file}:${m.line})`)
      );
    } else if (m.source === "process-env") {
      console.log(
        `  ${source_default.yellow("\u2022")} ${m.name}=${source_default.white(displayMarkerValue(m.name, m.value))} ` + source_default.dim(`(${m.reason}; process env)`)
      );
    } else {
      console.log(
        `  ${source_default.yellow("\u2022")} ${m.name}=${source_default.white(displayMarkerValue(m.name, m.value))} ` + source_default.dim(`(${m.reason}; ${m.file})`)
      );
    }
  }
  console.log();
}
function envEntryKey(entry) {
  return `${entry.file}:${entry.name}`;
}
function findingEntryKey(finding) {
  return `${finding.file}:${finding.varName}`;
}
function formatEnvEntrySource(cwd, entry) {
  const relative = path6.relative(cwd, entry.file) || path6.basename(entry.file);
  return `${relative}:${entry.line}`;
}
function allEnvEntries(statuses) {
  const entries = [];
  for (const status of statuses) {
    if (!status.exists) continue;
    entries.push(...parseEnvEntries(status.absolutePath));
  }
  return entries;
}
function readEnvValue(cwd, name) {
  const fromProcess = process.env[name];
  if (fromProcess) return fromProcess;
  for (const status of getEnvFileStatuses(cwd)) {
    if (!status.exists) continue;
    const entry = parseEnvEntries(status.absolutePath).find((item) => item.name === name);
    if (entry?.value) return entry.value;
  }
  return "";
}
function envFileValues(cwd) {
  const values = {};
  for (const status of getEnvFileStatuses(cwd)) {
    if (!status.exists) continue;
    for (const entry of parseEnvEntries(status.absolutePath)) {
      values[entry.name] = entry.value;
    }
  }
  return values;
}
async function ensureJwt() {
  let jwt = getJwt();
  if (jwt) return jwt;
  console.log(source_default.dim("\nOpening browser to log in...\n"));
  const loginSpinner = ora("Waiting for login...").start();
  const result = await browserLogin();
  if (!result) {
    loginSpinner.fail("Login timed out or was cancelled.");
    process.exit(1);
  }
  loginSpinner.succeed(`Logged in as ${source_default.bold(result.email)}`);
  jwt = result.token;
  return jwt;
}
async function listProjects(jwt, apiUrl) {
  const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
    headers: { Authorization: `Bearer ${jwt}` }
  });
  if (!res.ok) {
    const text = await res.text();
    throw new Error(`Could not list projects: ${res.status} ${text}`);
  }
  const data = await res.json();
  return data.projects || [];
}
async function createProject(jwt, apiUrl, name) {
  const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
    method: "POST",
    headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
    body: JSON.stringify({ name })
  });
  if (!res.ok) {
    const text = await res.text();
    throw new Error(`Project creation failed: ${res.status} ${text}`);
  }
  return await res.json();
}
async function chooseProject(jwt, apiUrl, opts) {
  const projects = await listProjects(jwt, apiUrl);
  if (opts.preferredProjectId) {
    const found = projects.find((project) => project.vp_proj_id === opts.preferredProjectId);
    if (found) return found;
    console.log(source_default.yellow(`Configured VAULTPROOF_PROJECT_ID ${opts.preferredProjectId} was not found on this account.`));
  }
  if (projects.length === 0) {
    return await createProject(jwt, apiUrl, opts.createName);
  }
  if (projects.length === 1 || opts.autoYes) {
    return projects[0];
  }
  console.log(source_default.bold("You have existing projects:\n"));
  projects.forEach((project, index) => {
    console.log(`  ${source_default.bold(String(index + 1))}. ${project.name || "(unnamed)"} ${source_default.dim(project.vp_proj_id)}`);
  });
  console.log(`  ${source_default.bold(String(projects.length + 1))}. ${source_default.dim("Create a new project")}`);
  console.log();
  const choice = await prompt(`Use which project? (1-${projects.length + 1}) `);
  const idx = Number.parseInt(choice, 10);
  if (Number.isInteger(idx) && idx >= 1 && idx <= projects.length) return projects[idx - 1];
  return await createProject(jwt, apiUrl, opts.createName);
}
function promptWithDefault(question, defaultValue) {
  return prompt(`${question} ${source_default.dim(`(${defaultValue})`)} `).then((answer) => answer || defaultValue);
}
async function promptValidated(question, defaultValue, validate2) {
  while (true) {
    const value = await promptWithDefault(question, defaultValue);
    const result = validate2(value);
    if (result.ok) return value;
    console.log(source_default.yellow(`  ${result.error || "Invalid value"}`));
  }
}
async function promptCustomProviderForEntry(entry, cwd, existingProviderIds) {
  console.log(source_default.bold(`
Custom provider for ${entry.name}`));
  console.log(source_default.dim(`  Source: ${formatEnvEntrySource(cwd, entry)}`));
  console.log(source_default.dim("  Upstream must be a public HTTPS hostname. Private IPs, .internal, localhost, and custom ports are blocked.\n"));
  const defaultSlug = deriveCustomProviderId(entry.name);
  let slug = "";
  while (!slug) {
    const candidate = await promptValidated("Provider slug", defaultSlug, validateProviderSlug);
    if (existingProviderIds.has(candidate)) {
      console.log(source_default.yellow(`  "${candidate}" is already in the provider catalog. Choose a unique slug for this custom API.`));
      continue;
    }
    slug = candidate;
  }
  const label = await promptWithDefault("Display name", deriveCustomLabel(slug));
  let upstreamBaseUrl = "";
  while (!upstreamBaseUrl) {
    const raw = await prompt("Upstream base URL (for example https://api.yourcompany.com) ");
    const checked = validateCustomUpstreamUrl(raw);
    if (!checked.ok || !checked.normalizedUrl) {
      console.log(source_default.yellow(`  ${checked.error || "Invalid upstream URL"}`));
      continue;
    }
    upstreamBaseUrl = checked.normalizedUrl;
  }
  const authHeaderName = await promptValidated(
    "Auth header name",
    "Authorization",
    validateCustomHeaderName
  );
  const authHeaderTemplate = await promptValidated(
    "Auth header template, using {key}",
    authHeaderName.toLowerCase() === "authorization" ? "Bearer {key}" : "{key}",
    validateCustomHeaderTemplate
  );
  const baseUrlEnvVar = await promptValidated(
    "Base URL env var to add",
    deriveBaseUrlEnvVar(entry.name),
    (value) => {
      const result = validateEnvVarName(value);
      if (!result.ok) return result;
      if (value.toUpperCase() === entry.name.toUpperCase()) {
        return { ok: false, error: "Base URL env var must be different from the API key env var" };
      }
      return { ok: true };
    }
  );
  const provider = buildCustomProviderSpec({
    id: slug,
    label,
    envVar: entry.name,
    upstreamBaseUrl,
    authHeaderName,
    authHeaderTemplate,
    baseUrlEnvVar
  });
  return {
    file: entry.file,
    line: entry.line,
    varName: entry.name,
    value: entry.value,
    provider
  };
}
async function collectCustomFindings(cwd, statuses, existingFindings, catalogProviders, opts) {
  if (!opts.forceCustom && opts.autoYes) return [];
  const promptForCustom = opts.forceCustom ? true : await confirm(
    existingFindings.length > 0 ? "Add a custom/internal API key that was not auto-detected?" : "Add a custom/internal API key from your .env?",
    false
  );
  if (!promptForCustom) return [];
  const matched = new Set(existingFindings.map(findingEntryKey));
  const selected = /* @__PURE__ */ new Set();
  const existingProviderIds = new Set(catalogProviders.map((provider) => provider.id));
  const entries = allEnvEntries(statuses);
  const customFindings = [];
  while (true) {
    const candidates = entries.filter((entry2) => !matched.has(envEntryKey(entry2)) && !selected.has(envEntryKey(entry2)) && isLikelyCustomSecretEntry({ name: entry2.name, value: entry2.value }));
    if (candidates.length === 0) {
      console.log(source_default.yellow("\nNo obvious custom/internal API key candidates found in checked .env files."));
      console.log(source_default.dim("Add a NAME=plaintext-key entry such as INTERNAL_API_KEY=... to .env, then rerun `vaultproof-init custom`."));
      break;
    }
    console.log(source_default.bold("\nPossible custom/internal keys:\n"));
    candidates.forEach((entry2, index2) => {
      console.log(
        `  ${source_default.bold(String(index2 + 1).padStart(2))}. ${entry2.name.padEnd(30)} ${source_default.dim(truncateKey(entry2.value).padEnd(18))} ${source_default.dim(formatEnvEntrySource(cwd, entry2))}`
      );
    });
    console.log();
    const answer = await prompt("Choose a key number or env var name (blank to finish): ");
    if (!answer) break;
    const index = Number.parseInt(answer, 10);
    const entry = Number.isInteger(index) && index >= 1 && index <= candidates.length ? candidates[index - 1] : candidates.find((candidate) => candidate.name.toUpperCase() === answer.toUpperCase());
    if (!entry) {
      console.log(source_default.yellow("  No matching key. Choose one of the listed numbers or names."));
      continue;
    }
    const customFinding = await promptCustomProviderForEntry(entry, cwd, existingProviderIds);
    if (customFinding) {
      customFindings.push(customFinding);
      selected.add(envEntryKey(entry));
      existingProviderIds.add(customFinding.provider.id);
    }
    if (!await confirm("Add another custom/internal API key?", false)) break;
  }
  return customFindings;
}
async function collectVaultSecretFindings(cwd, statuses, opts) {
  if (!opts.force && opts.autoYes) return [];
  const promptForVaultSecrets = opts.force ? true : await confirm("Protect vault-only secrets like DATABASE_URL, JWT_SECRET, and WEBHOOK_SECRET?", false);
  if (!promptForVaultSecrets) return [];
  const selected = /* @__PURE__ */ new Set();
  const entries = allEnvEntries(statuses);
  const vaultSecrets = [];
  while (true) {
    const candidates = entries.filter((entry2) => !selected.has(envEntryKey(entry2)) && isLikelyVaultSecretEntry({ name: entry2.name, value: entry2.value }));
    if (candidates.length === 0) {
      console.log(source_default.yellow("\nNo vault-only secret candidates found in checked .env files."));
      console.log(source_default.dim("Examples: DATABASE_URL=..., JWT_SECRET=..., SESSION_SECRET=..., WEBHOOK_SECRET=..."));
      break;
    }
    if (opts.autoYes) {
      for (const entry2 of candidates) {
        vaultSecrets.push(entry2);
        selected.add(envEntryKey(entry2));
      }
      break;
    }
    console.log(source_default.bold("\nPossible vault-only secrets:\n"));
    candidates.forEach((entry2, index2) => {
      console.log(
        `  ${source_default.bold(String(index2 + 1).padStart(2))}. ${entry2.name.padEnd(30)} ${source_default.dim(truncateKey(entry2.value).padEnd(18))} ${source_default.dim(formatEnvEntrySource(cwd, entry2))}`
      );
    });
    console.log();
    const answer = await prompt('Choose a secret number or env var name (blank to finish, "all" for all): ');
    if (!answer) break;
    if (answer.toLowerCase() === "all") {
      for (const entry2 of candidates) {
        vaultSecrets.push(entry2);
        selected.add(envEntryKey(entry2));
      }
      break;
    }
    const index = Number.parseInt(answer, 10);
    const entry = Number.isInteger(index) && index >= 1 && index <= candidates.length ? candidates[index - 1] : candidates.find((candidate) => candidate.name.toUpperCase() === answer.toUpperCase());
    if (!entry) {
      console.log(source_default.yellow("  No matching secret. Choose one of the listed numbers or names."));
      continue;
    }
    vaultSecrets.push(entry);
    selected.add(envEntryKey(entry));
    if (!await confirm("Add another vault-only secret?", false)) break;
  }
  return vaultSecrets;
}
async function uploadVaultSecret(apiUrl, jwt, projectRowId, secret) {
  const shares = splitString(secret.value, 2, 2);
  const provider = deriveVaultSecretProviderId(secret.name);
  const res = await fetch(`${apiUrl}/api/v1/init/projects/${projectRowId}/keys`, {
    method: "POST",
    headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
    body: JSON.stringify({
      provider,
      slug: provider,
      share1: serializeShare(shares[0]),
      share2: serializeShare(shares[1]),
      env_var: secret.name,
      upstream_base_url: "https://vaultproof.dev",
      auth_header_name: "Authorization",
      auth_header_template: "{key}",
      extra_headers: { "x-vaultproof-kind": "env-secret" }
    })
  });
  if (!res.ok) {
    const text = await res.text();
    throw new Error(`Upload failed for ${secret.name}: ${res.status} ${text}`);
  }
}
async function fetchVaultSecrets(apiUrl, jwt, projectRowId) {
  const res = await fetch(`${apiUrl}/api/v1/init/projects/${projectRowId}/secrets`, {
    headers: { Authorization: `Bearer ${jwt}` }
  });
  if (!res.ok) {
    const text = await res.text();
    throw new Error(`Could not fetch vault-only secrets: ${res.status} ${text}`);
  }
  const data = await res.json();
  return data.secrets || [];
}
async function runInit(opts) {
  printBanner();
  const catalog = await loadProviders();
  const envStatuses = getEnvFileStatuses(process.cwd());
  const vaultProofMarkers = collectVaultProofMarkers(process.cwd(), envStatuses);
  printScanReport(envStatuses, catalog.providers.length, vaultProofMarkers);
  const detectedFindings = opts.customOnly ? [] : scanDirectory(process.cwd(), catalog.providers);
  const customFindings = await collectCustomFindings(
    process.cwd(),
    envStatuses,
    detectedFindings,
    catalog.providers,
    {
      autoYes: opts.autoYes,
      forceCustom: Boolean(opts.forceCustom || opts.customOnly),
      customOnly: Boolean(opts.customOnly)
    }
  );
  const findings = [...detectedFindings, ...customFindings];
  const vaultSecretFindings = await collectVaultSecretFindings(
    process.cwd(),
    envStatuses,
    { autoYes: opts.autoYes, force: false }
  );
  if (findings.length === 0 && vaultSecretFindings.length === 0) {
    console.log(source_default.yellow("No plaintext API keys or vault-only secrets detected."));
    if (vaultProofMarkers.length > 0) {
      console.log(source_default.dim("This environment already appears to be configured for VaultProof."));
    } else {
      console.log(source_default.dim("No provider key patterns or vault-only secret candidates were found in checked .env files."));
    }
    console.log(source_default.dim(`Provider catalog: ${catalog.providers.length} providers, version ${catalog.version}`));
    process.exit(0);
  }
  if (findings.length > 0) {
    console.log(source_default.bold(`Found ${findings.length} proxy API key${findings.length === 1 ? "" : "s"}:
`));
    for (const f of findings) {
      console.log(
        `  ${source_default.green("\u2713")} ${f.varName.padEnd(26)} ${source_default.dim(truncateKey(f.value).padEnd(22))} ${source_default.dim("(" + f.provider.label + ")")}`
      );
    }
  }
  if (vaultSecretFindings.length > 0) {
    console.log(source_default.bold(`
Found ${vaultSecretFindings.length} vault-only secret${vaultSecretFindings.length === 1 ? "" : "s"}:
`));
    for (const secret of vaultSecretFindings) {
      console.log(
        `  ${source_default.green("\u2713")} ${secret.name.padEnd(26)} ${source_default.dim(truncateKey(secret.value).padEnd(22))} ${source_default.dim("(runtime injection)")}`
      );
    }
  }
  const skippedNotes = [];
  const envFiles = [".env", ".env.local", ".env.production", ".env.development"];
  for (const name of envFiles) {
    const filePath = path6.join(process.cwd(), name);
    if (!fs7.existsSync(filePath)) continue;
    const lines = fs7.readFileSync(filePath, "utf-8").split("\n");
    for (const line of lines) {
      const m = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.+?)\s*$/);
      if (!m) continue;
      const [, varName, rawValue] = m;
      const value = rawValue.replace(/^["']|["']$/g, "");
      if (findings.some((f) => f.varName === varName)) continue;
      if (vaultSecretFindings.some((secret) => secret.name === varName)) continue;
      if (varName.match(/WEBHOOK_SECRET|_WEBHOOK/) && value.startsWith("whsec_")) {
        skippedNotes.push(`${varName} \u2014 webhook secret (used locally for signature verification, not sent to Stripe)`);
      } else if (varName.match(/PRICE_ID|_PRICE_/) && value.startsWith("price_")) {
        skippedNotes.push(`${varName} \u2014 public price ID (not a secret)`);
      }
    }
  }
  if (skippedNotes.length > 0) {
    console.log(source_default.dim("\n  Skipped (no proxy protection needed):"));
    for (const note of skippedNotes) console.log(source_default.dim(`  \u2298 ${note}`));
  }
  console.log();
  if (opts.dryRun) {
    console.log(source_default.dim("(dry run \u2014 not uploading or rewriting)"));
    process.exit(0);
  }
  if (!opts.autoYes) {
    const total = findings.length + vaultSecretFindings.length;
    const ok = await confirm(`Protect ${total} secret${total === 1 ? "" : "s"} via Shamir splitting?`);
    if (!ok) {
      console.log(source_default.dim("Cancelled."));
      process.exit(0);
    }
  }
  let jwt = getJwt();
  if (!jwt) {
    console.log(source_default.dim("\nOpening browser to log in...\n"));
    const loginSpinner = ora("Waiting for login...").start();
    const result = await browserLogin();
    if (!result) {
      loginSpinner.fail("Login timed out or was cancelled.");
      console.log(source_default.dim("\nAlternatively, set VAULTPROOF_JWT manually:"));
      console.log(source_default.dim("  1. Log in at ") + source_default.white("https://vaultproof.dev/app/login"));
      console.log(source_default.dim("  2. Copy access_token from DevTools \u2192 Application \u2192 Local Storage"));
      console.log(source_default.dim("  3. Run: ") + source_default.white('export VAULTPROOF_JWT="<token>"'));
      process.exit(1);
    }
    loginSpinner.succeed(`Logged in as ${source_default.bold(result.email)}`);
    jwt = result.token;
  }
  const apiUrl = getInitWorkerUrl();
  const proxyBaseUrl = getProxyBaseUrl();
  let projectRowId;
  let projectId;
  try {
    const listRes = await fetch(`${apiUrl}/api/v1/init/projects`, {
      method: "GET",
      headers: { Authorization: `Bearer ${jwt}` }
    });
    const existingProjects = listRes.ok ? (await listRes.json()).projects || [] : [];
    if (existingProjects.length > 0) {
      console.log(source_default.bold("You have existing projects:\n"));
      for (let i = 0; i < existingProjects.length; i++) {
        const p = existingProjects[i];
        const label = p.name ? `${p.vp_proj_id} (${p.name})` : p.vp_proj_id;
        console.log(`  ${source_default.bold(String(i + 1))}. ${source_default.white(label)}`);
      }
      console.log(`  ${source_default.bold(String(existingProjects.length + 1))}. ${source_default.dim("Create a new project")}`);
      console.log();
      const { prompt: promptInput2 } = await Promise.resolve().then(() => (init_prompts(), prompts_exports));
      const choice = await promptInput2(`Add keys to which project? (1-${existingProjects.length + 1}) `);
      const choiceNum = parseInt(choice, 10);
      if (choiceNum >= 1 && choiceNum <= existingProjects.length) {
        const selected = existingProjects[choiceNum - 1];
        projectRowId = selected.id;
        projectId = selected.vp_proj_id;
        console.log(source_default.green(`
\u2713 Using project: ${source_default.bold(projectId)}`));
      } else {
        const createSpinner = ora("Creating new project...").start();
        const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
          method: "POST",
          headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
          body: JSON.stringify({})
        });
        if (!res.ok) {
          const text = await res.text();
          createSpinner.fail(`Project creation failed: ${res.status} ${text}`);
          process.exit(1);
        }
        const data = await res.json();
        projectRowId = data.id;
        projectId = data.vp_proj_id;
        createSpinner.succeed(`Project created: ${source_default.bold(projectId)}`);
      }
    } else {
      const createSpinner = ora("Creating VaultProof project...").start();
      const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({})
      });
      if (!res.ok) {
        const text = await res.text();
        createSpinner.fail(`Project creation failed: ${res.status} ${text}`);
        process.exit(1);
      }
      const data = await res.json();
      projectRowId = data.id;
      projectId = data.vp_proj_id;
      createSpinner.succeed(`Project created: ${source_default.bold(projectId)}`);
    }
  } catch (err) {
    console.error(source_default.red(`Could not reach VaultProof API at ${apiUrl}`));
    console.error(source_default.dim(String(err)));
    process.exit(1);
  }
  console.log(source_default.dim("\u2500".repeat(60)));
  console.log(source_default.bold("How VaultProof protects your keys:"));
  console.log(`  ${source_default.cyan("\u2192")} Your key is split into 2 shares ${source_default.bold("on this machine")}`);
  console.log(`  ${source_default.cyan("\u2192")} Each share is useless without the other`);
  console.log(`  ${source_default.cyan("\u2192")} VaultProof never receives your full key`);
  console.log(source_default.dim("\u2500".repeat(60)) + "\n");
  for (const f of findings) {
    const s = ora(`Splitting ${f.varName} (${f.provider.label})...`).start();
    let keyToSplit = f.value;
    if (f.provider.combine_format) {
      let otherVal = "";
      if (f.provider.combine_with_env) {
        otherVal = process.env[f.provider.combine_with_env] || (() => {
          try {
            const envContent = fs7.readFileSync(f.file, "utf-8");
            const match = envContent.match(new RegExp(`^${f.provider.combine_with_env}\\s*=\\s*(.+)$`, "m"));
            return match ? match[1].trim().replace(/^["']|["']$/g, "") : "";
          } catch {
            return "";
          }
        })();
        if (!otherVal && f.provider.combine_format.includes("{env}")) {
          s.fail(`${f.provider.label}: could not find ${f.provider.combine_with_env} in your .env. Add it and try again.`);
          continue;
        }
      }
      const combined = f.provider.combine_format.replace("{env}", otherVal).replace("{key}", f.value);
      keyToSplit = f.provider.combine_encoding === "base64" ? Buffer.from(combined).toString("base64") : combined;
    }
    const shares = splitString(keyToSplit, 2, 2);
    const share1 = serializeShare(shares[0]);
    const share2 = serializeShare(shares[1]);
    const readEnvVar = (varName) => {
      const fromEnv = process.env[varName] || "";
      if (fromEnv) return fromEnv;
      try {
        const envContent = fs7.readFileSync(f.file, "utf-8");
        const match = envContent.match(new RegExp(`^${varName}\\s*=\\s*(.+)$`, "m"));
        if (match) return match[1].trim().replace(/^["']|["']$/g, "");
      } catch {
      }
      return "";
    };
    let upstreamUrl = f.provider.upstream_base_url;
    if (upstreamUrl === "dynamic" && f.provider.upstream_from_env) {
      upstreamUrl = readEnvVar(f.provider.upstream_from_env);
      if (!upstreamUrl) {
        s.fail(`${f.provider.label}: could not find ${f.provider.upstream_from_env} in your .env. Add it and try again.`);
        continue;
      }
      if (f.provider.id === "algolia") {
        upstreamUrl = `https://${upstreamUrl}-dsn.algolia.net`;
      }
    }
    let resolvedExtraHeaders = f.provider.extra_headers ? { ...f.provider.extra_headers } : null;
    if (resolvedExtraHeaders) {
      for (const [k, v] of Object.entries(resolvedExtraHeaders)) {
        const envMatch = v.match(/^\{env:([A-Za-z_][A-Za-z0-9_]*)\}$/);
        if (envMatch) {
          const val = readEnvVar(envMatch[1]);
          if (val) resolvedExtraHeaders[k] = val;
          else delete resolvedExtraHeaders[k];
        }
      }
    }
    try {
      const res = await fetch(`${apiUrl}/api/v1/init/projects/${projectRowId}/keys`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({
          provider: f.provider.id,
          slug: f.provider.id,
          share1,
          share2,
          env_var: f.varName,
          upstream_base_url: upstreamUrl,
          auth_header_name: f.provider.auth_header_name,
          auth_header_template: f.provider.auth_header_template,
          extra_headers: resolvedExtraHeaders ?? null
        })
      });
      if (!res.ok) {
        const text = await res.text();
        s.fail(`Upload failed for ${f.varName}: ${res.status} ${text}`);
        process.exit(1);
      }
    } catch (err) {
      s.fail(`Network error uploading ${f.varName}: ${String(err)}`);
      process.exit(1);
    }
    s.succeed(`${source_default.bold(f.varName)} ${source_default.dim("(" + f.provider.label + ")")}`);
    console.log(source_default.dim(`    Split locally on your machine`));
    console.log(source_default.dim(`    Share 1 \u2192 VaultProof (encrypted with VaultProof's key)`));
    console.log(source_default.dim(`    Share 2 \u2192 VaultProof (encrypted with a different key)`));
    console.log(source_default.dim(`    A breach of VaultProof cannot expose this key`));
  }
  for (const secret of vaultSecretFindings) {
    const s = ora(`Splitting ${secret.name} (vault-only)...`).start();
    try {
      await uploadVaultSecret(apiUrl, jwt, projectRowId, secret);
    } catch (err) {
      s.fail(String(err));
      process.exit(1);
    }
    s.succeed(`${source_default.bold(secret.name)} ${source_default.dim("(vault-only secret)")}`);
    console.log(source_default.dim("    Stored for authenticated CLI retrieval and runtime injection"));
    console.log(source_default.dim(`    Use: vaultproof-init run -- <command>`));
  }
  console.log();
  const filesToRewrite = Array.from(new Set(findings.map((f) => f.file)));
  const allManualNotes = [];
  for (const file of filesToRewrite) {
    const { rewritten, manualNotes } = rewriteEnvFile(file, findings, { projectId, proxyBaseUrl });
    if (rewritten > 0) {
      console.log(`
${source_default.green("\u2713")} Rewrote ${source_default.bold(file)} (${rewritten} key${rewritten === 1 ? "" : "s"})`);
    }
    allManualNotes.push(...manualNotes);
  }
  const vaultSecretFilesToRewrite = Array.from(new Set(vaultSecretFindings.map((secret) => secret.file)));
  for (const file of vaultSecretFilesToRewrite) {
    const { rewritten } = rewriteEnvFileForVaultSecrets(file, vaultSecretFindings, { projectId, proxyBaseUrl });
    if (rewritten > 0) {
      console.log(`
${source_default.green("\u2713")} Rewrote ${source_default.bold(file)} (${rewritten} vault-only secret${rewritten === 1 ? "" : "s"})`);
    }
  }
  console.log(`
${source_default.bold.green("Done.")} Your .env uses one project ID for everything:`);
  console.log(`  ${source_default.bold(projectId)}
`);
  const hasStripe = findings.some((f) => f.provider.id === "stripe");
  const hasSupabase = findings.some((f) => f.provider.id === "supabase");
  const hasGoogle = findings.some((f) => f.provider.id === "google");
  if (hasStripe || hasSupabase || hasGoogle) {
    const configLines = [
      "// Generated by vaultproof-init \u2014 do not edit manually.",
      "// Import from this file instead of constructing clients directly.",
      ""
    ];
    const importHints = [];
    if (hasGoogle) {
      configLines.push(
        `// Google / Gemini`,
        `// Supports both @google/genai (new) and @google/generative-ai (legacy)`,
        `const GOOGLE_PROXY = 'https://init.vaultproof.dev/p/google';`,
        `const GOOGLE_KEY = process.env.GOOGLE_API_KEY || process.env.GEMINI_API_KEY || '';`,
        ``,
        `// @google/genai (new SDK, recommended)`,
        `let genai: any = null;`,
        `try {`,
        `  const { GoogleGenAI } = await import('@google/genai');`,
        `  genai = new GoogleGenAI({ apiKey: GOOGLE_KEY, httpOptions: { baseUrl: GOOGLE_PROXY } });`,
        `} catch {}`,
        `export { genai };`,
        ``,
        `// @google/generative-ai (legacy SDK)`,
        `let GoogleGenerativeAI: any = null;`,
        `let genAI: any = null;`,
        `try {`,
        `  const m = await import('@google/generative-ai');`,
        `  GoogleGenerativeAI = m.GoogleGenerativeAI;`,
        `  genAI = new GoogleGenerativeAI(GOOGLE_KEY, { baseUrl: GOOGLE_PROXY });`,
        `} catch {}`,
        `export { genAI };`,
        ``
      );
      importHints.push(`    import { genai } from './vaultproof.config'; // @google/genai`);
      importHints.push(`    import { genAI } from './vaultproof.config'; // @google/generative-ai`);
    }
    if (hasStripe) {
      configLines.push(
        `import Stripe from 'stripe';`,
        ``,
        `export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {`,
        `  host: 'init.vaultproof.dev',`,
        `  apiVersion: '2023-10-16',`,
        `});`,
        ``
      );
      importHints.push(`    import { stripe } from './vaultproof.config';`);
    }
    if (hasSupabase) {
      configLines.push(
        `import { createClient } from '@supabase/supabase-js';`,
        ``,
        `export const supabaseService = createClient(`,
        `  process.env.SUPABASE_PROXY_URL || process.env.NEXT_PUBLIC_SUPABASE_URL!,`,
        `  process.env.SUPABASE_SERVICE_ROLE_KEY!,`,
        `  { auth: { autoRefreshToken: false, persistSession: false } }`,
        `);`,
        ``
      );
      importHints.push(`    import { supabaseService } from './vaultproof.config';`);
    }
    const hasTsConfig = fs7.existsSync(path6.join(process.cwd(), "tsconfig.json"));
    const ext = hasTsConfig ? "ts" : "js";
    const configPath = path6.join(process.cwd(), `vaultproof.config.${ext}`);
    if (!fs7.existsSync(configPath)) {
      fs7.writeFileSync(configPath, configLines.join("\n"));
      console.log(`
${source_default.green("\u2713")} Created ${source_default.bold(`vaultproof.config.${ext}`)}`);
      console.log(source_default.dim("  Import from this file instead of constructing clients directly:"));
      for (const hint of importHints) console.log(source_default.dim(hint));
    } else {
      console.log(source_default.dim(`
  vaultproof.config.${ext} already exists \u2014 skipped.`));
    }
    console.log();
  }
  const deployVars = [
    { name: "VAULTPROOF_PROJECT_ID", value: projectId }
  ];
  for (const f of findings) {
    deployVars.push({ name: f.varName, value: projectId });
    if (f.provider.base_url_env_var && f.provider.base_url_path_suffix) {
      deployVars.push({
        name: f.provider.base_url_env_var,
        value: `${proxyBaseUrl}${f.provider.base_url_path_suffix}`
      });
    }
  }
  for (const secret of vaultSecretFindings) {
    deployVars.push({ name: secret.name, value: vaultSecretPlaceholder(secret.name) });
  }
  console.log(source_default.bold("\n\u2500\u2500 Deploy to production \u2500\u2500\n"));
  console.log(source_default.dim("Set these env vars on your hosting platform (Vercel, Railway, Netlify, etc.):\n"));
  for (const v of deployVars) {
    console.log(`  ${source_default.white(v.name)}=${source_default.dim(v.value)}`);
  }
  console.log(source_default.dim("\nThese are identifiers or VaultProof placeholders, not plaintext secrets.\n"));
  if (vaultSecretFindings.length > 0) {
    console.log(source_default.dim("For vault-only secrets, start your app with:"));
    console.log(source_default.white("  vaultproof-init run -- <your start command>\n"));
  }
  const { prompt: promptInput } = await Promise.resolve().then(() => (init_prompts(), prompts_exports));
  const deployed = await confirm("Have you set these on your hosting platform?");
  if (deployed) {
    console.log();
    for (const f of findings) {
      const testSpinner = ora(`Testing ${f.provider.label} proxy...`).start();
      try {
        const testUrl = f.provider.id === "stripe" ? `${proxyBaseUrl}/v1/customers?limit=1` : `${proxyBaseUrl}/p/${f.provider.id}/`;
        const testRes = await fetch(testUrl, {
          method: "GET",
          headers: { Authorization: `Bearer ${projectId}` }
        });
        if (testRes.status === 401 || testRes.status === 200) {
          testSpinner.succeed(`${f.provider.label} proxy: ${source_default.green("connected")}`);
        } else if (testRes.status === 404 || testRes.status === 405) {
          testSpinner.succeed(`${f.provider.label} proxy: ${source_default.green("connected")}`);
        } else {
          testSpinner.warn(`${f.provider.label} proxy: status ${testRes.status} \u2014 check your deployment`);
        }
      } catch (err) {
        testSpinner.fail(`${f.provider.label} proxy: ${source_default.red("could not connect")}`);
      }
    }
  }
  console.log(source_default.bold("\n\u2713 All done.\n"));
  console.log(`  ${source_default.green("\u2022")} Dashboard: ${source_default.white("https://vaultproof.dev/app")}`);
  console.log(`  ${source_default.green("\u2022")} Docs: ${source_default.white("https://vaultproof.dev/docs")}`);
}
async function runCheckLegacy() {
  console.log(source_default.bold("\nVaultProof \u2014 legacy audit & migration\n"));
  const vpLive = readLegacyVpLiveKey();
  if (!vpLive) {
    console.log(source_default.yellow("No legacy vp_live_ key found in ~/.vaultproof/config.json."));
    console.log(source_default.dim("If you never used the legacy system, just run: vaultproof-init"));
    process.exit(0);
  }
  let jwt = getJwt();
  if (!jwt) {
    console.log(source_default.dim("\nOpening browser to log in...\n"));
    const loginSpinner = ora("Waiting for login...").start();
    const result = await browserLogin();
    if (!result) {
      loginSpinner.fail("Login timed out or was cancelled.");
      process.exit(1);
    }
    loginSpinner.succeed(`Logged in as ${source_default.bold(result.email)}`);
    jwt = result.token;
  }
  const listSpinner = ora("Fetching legacy key list...").start();
  const legacy = await listLegacyKeys(vpLive);
  listSpinner.stop();
  if (legacy.keys.length === 0) {
    console.log(source_default.yellow("No keys found in the legacy system."));
    console.log(source_default.dim("Nothing to migrate. Run `vaultproof-init` on your current .env if you have plaintext keys there."));
    process.exit(0);
  }
  const catalog = await loadProviders();
  const providerById = /* @__PURE__ */ new Map();
  for (const p of catalog.providers) providerById.set(p.id, p);
  const entries = legacy.keys.map((k) => {
    const provider = providerById.get(k.provider.toLowerCase()) || null;
    const defaultEnvVar = k.envVar || provider?.env_var_default || `${k.provider.toUpperCase()}_API_KEY`;
    return { legacy: k, provider, defaultEnvVar };
  });
  console.log(source_default.bold(`Found ${entries.length} key${entries.length === 1 ? "" : "s"} in the legacy system:
`));
  for (const e of entries) {
    const label = e.provider ? e.provider.label : source_default.yellow(`${e.legacy.provider} (unsupported)`);
    const when = e.legacy.createdAt ? source_default.dim(`(stored ${e.legacy.createdAt.slice(0, 10)})`) : "";
    console.log(`  \u2022 ${e.defaultEnvVar.padEnd(26)} ${source_default.dim(label.padEnd(18))} ${when}`);
  }
  console.log();
  console.log(source_default.dim("Plaintext values are not recoverable from the legacy system \u2014 by design."));
  console.log(source_default.dim("For each key you want to migrate, you will paste a freshly ROTATED value."));
  console.log(source_default.dim("If you haven't rotated yet, exit now, rotate in the provider dashboard, then re-run."));
  console.log();
  const proceed = await confirm("Start migration?");
  if (!proceed) {
    console.log(source_default.dim("Cancelled."));
    process.exit(0);
  }
  const apiUrl = getInitWorkerUrl();
  const proxyBaseUrl = getProxyBaseUrl();
  const createSpinner = ora("Creating VaultProof project...").start();
  let projectRowId;
  let projectId;
  try {
    const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
      method: "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
      body: JSON.stringify({ name: "migrated-from-legacy" })
    });
    if (!res.ok) {
      const text = await res.text();
      createSpinner.fail(`Project creation failed: ${res.status} ${text}`);
      process.exit(1);
    }
    const data = await res.json();
    projectRowId = data.id;
    projectId = data.vp_proj_id;
  } catch (err) {
    createSpinner.fail(`Could not reach VaultProof API at ${apiUrl}`);
    console.error(source_default.dim(String(err)));
    process.exit(1);
  }
  createSpinner.succeed(`Project created: ${source_default.bold(projectId)}`);
  console.log();
  const migrated = [];
  const skipped = [];
  for (const e of entries) {
    if (!e.provider) {
      console.log(source_default.yellow(`\u2298 Skipping ${e.defaultEnvVar} \u2014 provider "${e.legacy.provider}" not supported by the new system yet.`));
      skipped.push(e.defaultEnvVar);
      continue;
    }
    console.log(source_default.bold(`
${e.defaultEnvVar} (${e.provider.label})`));
    const doThisOne = await confirm("  Migrate this key?", true);
    if (!doThisOne) {
      skipped.push(e.defaultEnvVar);
      continue;
    }
    const newKey = await promptHidden(`  Paste freshly rotated ${e.provider.label} key: `);
    if (!newKey) {
      console.log(source_default.yellow("  Empty input \u2014 skipping."));
      skipped.push(e.defaultEnvVar);
      continue;
    }
    let re;
    try {
      re = new RegExp(e.provider.detect.regex);
    } catch {
      re = /.*/;
    }
    if (!re.test(newKey)) {
      console.log(source_default.yellow(`  Warning: key does not match ${e.provider.label} pattern.`));
      const keepGoing = await confirm("  Upload it anyway?", false);
      if (!keepGoing) {
        skipped.push(e.defaultEnvVar);
        continue;
      }
    }
    const s = ora(`  Splitting + uploading ${e.defaultEnvVar}...`).start();
    const shares = splitString(newKey, 2, 2);
    try {
      const res = await fetch(`${apiUrl}/api/v1/init/projects/${projectRowId}/keys`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${jwt}` },
        body: JSON.stringify({
          provider: e.provider.id,
          slug: e.provider.id,
          share1: serializeShare(shares[0]),
          share2: serializeShare(shares[1]),
          env_var: e.defaultEnvVar,
          upstream_base_url: e.provider.upstream_base_url,
          auth_header_name: e.provider.auth_header_name,
          auth_header_template: e.provider.auth_header_template,
          extra_headers: e.provider.extra_headers ?? null
        })
      });
      if (!res.ok) {
        const text = await res.text();
        s.fail(`  Upload failed: ${res.status} ${text}`);
        skipped.push(e.defaultEnvVar);
        continue;
      }
    } catch (err) {
      s.fail(`  Network error: ${String(err)}`);
      skipped.push(e.defaultEnvVar);
      continue;
    }
    s.succeed(`  Protected ${source_default.bold(e.defaultEnvVar)}`);
    migrated.push({ envVar: e.defaultEnvVar, provider: e.provider });
  }
  if (migrated.length > 0) {
    const envPath = path6.join(process.cwd(), ".env");
    const { written, manualNotes } = rewriteEnvFileForMigration(envPath, migrated, { projectId, proxyBaseUrl });
    console.log(`
${source_default.green("\u2713")} Wrote ${written} entries to ${source_default.bold(envPath)}`);
    if (manualNotes.length > 0) {
      console.log(source_default.bold("\nA few providers need a one-line client change:"));
      for (const n of manualNotes) console.log(`  ${source_default.yellow("\u2022")} ${n}`);
    }
  }
  console.log(`
${source_default.bold.green("Migration complete.")}`);
  console.log(`  ${source_default.green(migrated.length)} migrated, ${source_default.dim(skipped.length + " skipped")}`);
  console.log(`  Project ID: ${source_default.bold(projectId)}`);
  if (skipped.length > 0) {
    console.log(source_default.dim("\nSkipped keys still live in the legacy system."));
    console.log(source_default.dim("When you are ready, rotate them and re-run `vaultproof-init --check-legacy`."));
  }
  process.exit(0);
}
async function runSecretsAdd(opts) {
  printBanner();
  const catalog = await loadProviders();
  const cwd = process.cwd();
  const envStatuses = getEnvFileStatuses(cwd);
  const markers = collectVaultProofMarkers(cwd, envStatuses);
  printScanReport(envStatuses, catalog.providers.length, markers);
  const vaultSecrets = await collectVaultSecretFindings(cwd, envStatuses, {
    autoYes: opts.autoYes,
    force: true
  });
  if (vaultSecrets.length === 0) {
    console.log(source_default.yellow("No vault-only secrets selected."));
    process.exit(0);
  }
  console.log(source_default.bold(`
Selected ${vaultSecrets.length} vault-only secret${vaultSecrets.length === 1 ? "" : "s"}:
`));
  for (const secret of vaultSecrets) {
    console.log(`  ${source_default.green("\u2713")} ${secret.name.padEnd(26)} ${source_default.dim(truncateKey(secret.value).padEnd(22))} ${source_default.dim(formatEnvEntrySource(cwd, secret))}`);
  }
  if (opts.dryRun) {
    console.log(source_default.dim("\n(dry run \u2014 not uploading or rewriting)"));
    process.exit(0);
  }
  if (!opts.autoYes) {
    const ok = await confirm(`Protect ${vaultSecrets.length} vault-only secret${vaultSecrets.length === 1 ? "" : "s"}?`);
    if (!ok) {
      console.log(source_default.dim("Cancelled."));
      process.exit(0);
    }
  }
  const jwt = await ensureJwt();
  const apiUrl = getInitWorkerUrl();
  const preferredProjectId = readEnvValue(cwd, "VAULTPROOF_PROJECT_ID");
  const project = await chooseProject(jwt, apiUrl, {
    autoYes: opts.autoYes,
    preferredProjectId: preferredProjectId || void 0,
    createName: path6.basename(cwd) || "vaultproof-project"
  });
  for (const secret of vaultSecrets) {
    const spinner = ora(`Splitting + uploading ${secret.name}...`).start();
    try {
      await uploadVaultSecret(apiUrl, jwt, project.id, secret);
      spinner.succeed(`Protected ${source_default.bold(secret.name)}`);
    } catch (err) {
      spinner.fail(String(err));
      process.exit(1);
    }
  }
  const proxyBaseUrl = getProxyBaseUrl();
  for (const file of Array.from(new Set(vaultSecrets.map((secret) => secret.file)))) {
    const { rewritten } = rewriteEnvFileForVaultSecrets(file, vaultSecrets, {
      projectId: project.vp_proj_id,
      proxyBaseUrl
    });
    if (rewritten > 0) {
      console.log(`${source_default.green("\u2713")} Rewrote ${source_default.bold(file)} (${rewritten} vault-only secret${rewritten === 1 ? "" : "s"})`);
    }
  }
  console.log(`
${source_default.bold.green("Done.")} Vault-only secrets are stored for project ${source_default.bold(project.vp_proj_id)}.`);
  console.log(source_default.dim("Run with injected secrets:"));
  console.log(source_default.white("  vaultproof-init run -- <your start command>"));
}
async function selectNetOpsSecrets(cwd, candidates, opts) {
  if (opts.autoYes) return candidates;
  const selected = /* @__PURE__ */ new Set();
  const out = [];
  while (true) {
    const remaining = candidates.filter((entry2) => !selected.has(`${entry2.file}:${entry2.line}:${entry2.name}`));
    if (remaining.length === 0) break;
    console.log(source_default.bold("\nPossible NetOps secrets:\n"));
    remaining.forEach((entry2, index2) => {
      const label = `${entry2.sourceKey} \u2192 ${entry2.name}`;
      console.log(
        `  ${source_default.bold(String(index2 + 1).padStart(2))}. ${label.padEnd(44)} ${source_default.dim(truncateKey(entry2.value).padEnd(18))} ${source_default.dim(formatEnvEntrySource(cwd, entry2))}`
      );
    });
    console.log();
    const answer = await prompt('Choose a secret number (blank to finish, "all" for all): ');
    if (!answer) break;
    if (answer.toLowerCase() === "all") {
      for (const entry2 of remaining) {
        out.push(entry2);
        selected.add(`${entry2.file}:${entry2.line}:${entry2.name}`);
      }
      break;
    }
    const index = Number.parseInt(answer, 10);
    const entry = Number.isInteger(index) && index >= 1 && index <= remaining.length ? remaining[index - 1] : null;
    if (!entry) {
      console.log(source_default.yellow("  No matching secret. Choose one of the listed numbers."));
      continue;
    }
    out.push(entry);
    selected.add(`${entry.file}:${entry.line}:${entry.name}`);
    if (!await confirm("Add another NetOps secret?", false)) break;
  }
  return out;
}
async function runNetOpsAdd(opts) {
  printBanner();
  const cwd = process.cwd();
  console.log(source_default.bold("VaultProof NetOps \u2014 Ansible and Terraform secret cleanup\n"));
  console.log(source_default.dim("Scanning .env, inventory.yml, hosts.yml, group_vars, host_vars, terraform.tfvars, and *.auto.tfvars.\n"));
  const candidates = scanNetOpsSecrets(cwd);
  if (candidates.length === 0) {
    console.log(source_default.yellow("No NetOps secret candidates found."));
    console.log(source_default.dim("Try names like ANSIBLE_PASSWORD, ansible_password, enable_secret, snmp_community, or device_password."));
    process.exit(0);
  }
  const selectedSecrets = await selectNetOpsSecrets(cwd, candidates, opts);
  if (selectedSecrets.length === 0) {
    console.log(source_default.yellow("No NetOps secrets selected."));
    process.exit(0);
  }
  console.log(source_default.bold(`
Selected ${selectedSecrets.length} NetOps secret${selectedSecrets.length === 1 ? "" : "s"}:
`));
  for (const secret of selectedSecrets) {
    console.log(`  ${source_default.green("\u2713")} ${secret.name.padEnd(34)} ${source_default.dim(secret.tool.padEnd(10))} ${source_default.dim(formatEnvEntrySource(cwd, secret))}`);
  }
  if (opts.dryRun) {
    console.log(source_default.dim("\n(dry run \u2014 not uploading or rewriting)"));
    process.exit(0);
  }
  if (!opts.autoYes) {
    const ok = await confirm(`Protect ${selectedSecrets.length} NetOps secret${selectedSecrets.length === 1 ? "" : "s"}?`);
    if (!ok) {
      console.log(source_default.dim("Cancelled."));
      process.exit(0);
    }
  }
  const jwt = await ensureJwt();
  const apiUrl = getInitWorkerUrl();
  const preferredProjectId = readEnvValue(cwd, "VAULTPROOF_PROJECT_ID");
  const project = await chooseProject(jwt, apiUrl, {
    autoYes: opts.autoYes,
    preferredProjectId: preferredProjectId || void 0,
    createName: `${path6.basename(cwd) || "vaultproof"}-netops`
  });
  for (const secret of selectedSecrets) {
    const spinner = ora(`Splitting + uploading ${secret.name}...`).start();
    try {
      await uploadVaultSecret(apiUrl, jwt, project.id, secret);
      spinner.succeed(`Protected ${source_default.bold(secret.name)}`);
    } catch (err) {
      spinner.fail(String(err));
      process.exit(1);
    }
  }
  const rewriteResults = rewriteNetOpsSecretFiles(selectedSecrets);
  for (const result of rewriteResults) {
    console.log(`${source_default.green("\u2713")} Rewrote ${source_default.bold(result.file)} (${result.rewritten} secret${result.rewritten === 1 ? "" : "s"})`);
  }
  if (rewriteResults.length === 0) {
    console.log(source_default.dim("No files were rewritten. Secrets are stored and can be injected with netops run."));
  }
  console.log(`
${source_default.bold.green("Done.")} NetOps secrets are stored for project ${source_default.bold(project.vp_proj_id)}.`);
  console.log(source_default.dim("Run automation with injected secrets:"));
  console.log(source_default.white("  vaultproof-init netops run -- ansible-playbook site.yml"));
  console.log(source_default.white("  vaultproof-init netops run -- terraform plan"));
}
async function resolveProjectForVaultSecretRead(jwt, apiUrl, opts) {
  const preferredProjectId = readEnvValue(process.cwd(), "VAULTPROOF_PROJECT_ID");
  return await chooseProject(jwt, apiUrl, {
    autoYes: opts.autoYes,
    preferredProjectId: preferredProjectId || void 0,
    createName: path6.basename(process.cwd()) || "vaultproof-project"
  });
}
async function runSecretsPull(opts) {
  const jwt = await ensureJwt();
  const apiUrl = getInitWorkerUrl();
  const project = await resolveProjectForVaultSecretRead(jwt, apiUrl, opts);
  const secrets = await fetchVaultSecrets(apiUrl, jwt, project.id);
  if (secrets.length === 0) {
    console.log(source_default.yellow("No vault-only secrets found for this project."));
    process.exit(0);
  }
  const lines = secrets.sort((a, b) => a.env_var.localeCompare(b.env_var)).map((secret) => `${secret.env_var}=${formatDotEnvValue(secret.value)}`);
  if (opts.stdout) {
    console.log(lines.join("\n"));
    return;
  }
  const outPath = path6.join(process.cwd(), ".env.vaultproof.local");
  fs7.writeFileSync(outPath, lines.join("\n") + "\n", { mode: 384 });
  try {
    fs7.chmodSync(outPath, 384);
  } catch {
  }
  console.log(`${source_default.green("\u2713")} Wrote ${secrets.length} secret${secrets.length === 1 ? "" : "s"} to ${source_default.bold(outPath)}`);
  console.log(source_default.dim("This file contains plaintext secrets. Keep it out of git."));
}
async function runWithVaultSecrets(opts) {
  if (opts.passthrough.length === 0) {
    console.error(source_default.red("Missing command. Usage: vaultproof-init run -- <command>"));
    process.exit(1);
  }
  const jwt = await ensureJwt();
  const apiUrl = getInitWorkerUrl();
  const project = await resolveProjectForVaultSecretRead(jwt, apiUrl, opts);
  const secrets = await fetchVaultSecrets(apiUrl, jwt, project.id);
  const localEnv = envFileValues(process.cwd());
  const secretEnv = {};
  for (const secret of secrets) secretEnv[secret.env_var] = secret.value;
  const childEnv = {
    ...localEnv,
    ...process.env,
    ...secretEnv,
    VAULTPROOF_PROJECT_ID: project.vp_proj_id
  };
  console.log(source_default.dim(`Injecting ${secrets.length} vault-only secret${secrets.length === 1 ? "" : "s"} for ${project.vp_proj_id}.`));
  const child = spawn(opts.passthrough[0], opts.passthrough.slice(1), {
    cwd: process.cwd(),
    env: childEnv,
    stdio: "inherit",
    shell: process.platform === "win32"
  });
  child.on("exit", (code, signal) => {
    if (signal) {
      process.kill(process.pid, signal);
      return;
    }
    process.exit(code ?? 0);
  });
  child.on("error", (err) => {
    console.error(source_default.red(`Failed to start command: ${err.message}`));
    process.exit(1);
  });
}
async function runDoctor() {
  printBanner();
  console.log(source_default.bold("VaultProof \u2014 health check\n"));
  const apiUrl = getInitWorkerUrl();
  const proxyBaseUrl = getProxyBaseUrl();
  const TIMEOUT_MS2 = 5e3;
  let issues = 0;
  async function check(label, fn) {
    const spinner = ora(label).start();
    try {
      const start = Date.now();
      const result = await Promise.race([
        fn(),
        new Promise(
          (_, reject) => setTimeout(() => reject(new Error("timeout")), TIMEOUT_MS2)
        )
      ]);
      const ms = Date.now() - start;
      if (result.ok) {
        spinner.succeed(`${label.padEnd(30)} ${source_default.dim(result.detail)} ${source_default.dim(`(${ms}ms)`)}`);
      } else {
        spinner.fail(`${label.padEnd(30)} ${source_default.red(result.detail)}`);
        issues++;
      }
    } catch (err) {
      spinner.fail(`${label.padEnd(30)} ${source_default.red(err?.message === "timeout" ? "timeout (5s)" : String(err))}`);
      issues++;
    }
  }
  await check("Worker reachability", async () => {
    const res = await fetch(`${apiUrl}/health`);
    return res.ok ? { ok: true, detail: "connected" } : { ok: false, detail: `HTTP ${res.status}` };
  });
  const jwt = getJwt();
  await check("Auth validity", async () => {
    if (!jwt) return { ok: false, detail: "not logged in \u2014 run vaultproof-init first" };
    const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
      headers: { Authorization: `Bearer ${jwt}` }
    });
    if (!res.ok) return { ok: false, detail: `HTTP ${res.status}` };
    const email = (() => {
      try {
        return JSON.parse(Buffer.from(jwt.split(".")[1], "base64").toString()).email || "";
      } catch {
        return "";
      }
    })();
    return { ok: true, detail: email || "valid" };
  });
  let projectId = null;
  await check("Share integrity", async () => {
    if (!jwt) return { ok: false, detail: "skipped (not logged in)" };
    const res = await fetch(`${apiUrl}/api/v1/init/projects`, {
      headers: { Authorization: `Bearer ${jwt}` }
    });
    if (!res.ok) return { ok: false, detail: `HTTP ${res.status}` };
    const data = await res.json();
    const projects = data.projects || [];
    if (projects.length === 0) return { ok: false, detail: "no projects found" };
    projectId = projects[0].vp_proj_id;
    return { ok: true, detail: `${projects.length} project${projects.length === 1 ? "" : "s"} found` };
  });
  if (projectId) {
    await check("Proxy reachability", async () => {
      const testUrl = `${proxyBaseUrl}/p/openai/`;
      const res = await fetch(testUrl, {
        method: "GET",
        headers: { Authorization: `Bearer ${projectId}` }
      });
      if ([200, 401, 404, 405].includes(res.status)) {
        return { ok: true, detail: "proxy chain connected" };
      }
      return { ok: false, detail: `HTTP ${res.status}` };
    });
  } else {
    console.log(source_default.dim("  Proxy test skipped \u2014 no projects found"));
  }
  console.log();
  if (issues === 0) {
    console.log(source_default.bold.green("\u2713 All checks passed."));
  } else {
    console.log(source_default.bold.red(`\u2717 ${issues} issue${issues === 1 ? "" : "s"} found.`));
    console.log(source_default.dim("  See https://vaultproof.dev/status for live uptime data."));
  }
  console.log();
  process.exit(issues > 0 ? 1 : 0);
}
async function main2() {
  const { cmd, flags, positionals, passthrough } = parseArgs(process.argv);
  const autoYes = flags.has("--yes") || flags.has("-y");
  const dryRun = flags.has("--dry-run");
  const checkLegacy = flags.has("--check-legacy");
  const forceCustom = flags.has("--custom");
  const showHelp = flags.has("--help") || flags.has("-h") || cmd === "help";
  if (showHelp) {
    printBanner();
    printUsage();
    return;
  }
  if (cmd === "doctor") {
    await runDoctor();
    return;
  }
  if (cmd === "migrate-from-legacy") {
    await runCheckLegacy();
    return;
  }
  if (cmd === "custom") {
    await runInit({ autoYes, dryRun, forceCustom: true, customOnly: true });
    return;
  }
  if (cmd === "secrets") {
    const subcommand = positionals[1] || "add";
    if (subcommand === "add") {
      await runSecretsAdd({ autoYes, dryRun });
      return;
    }
    if (subcommand === "pull") {
      await runSecretsPull({ autoYes, stdout: flags.has("--stdout") });
      return;
    }
    console.error(source_default.red(`Unknown secrets command: ${subcommand}`));
    printUsage();
    process.exit(1);
  }
  if (cmd === "netops") {
    const subcommand = positionals[1] || "add";
    if (subcommand === "add") {
      await runNetOpsAdd({ autoYes, dryRun });
      return;
    }
    if (subcommand === "run") {
      await runWithVaultSecrets({ autoYes, passthrough });
      return;
    }
    if (subcommand === "pull") {
      await runSecretsPull({ autoYes, stdout: flags.has("--stdout") });
      return;
    }
    console.error(source_default.red(`Unknown netops command: ${subcommand}`));
    printUsage();
    process.exit(1);
  }
  if (cmd === "run") {
    await runWithVaultSecrets({ autoYes, passthrough });
    return;
  }
  if (cmd !== "init") {
    console.error(source_default.red(`Unknown command: ${cmd}`));
    printUsage();
    process.exit(1);
  }
  if (checkLegacy) {
    await runCheckLegacy();
    return;
  }
  await runInit({ autoYes, dryRun, forceCustom });
}
main2().catch((err) => {
  console.error(source_default.red(`Unexpected error: ${String(err)}`));
  process.exit(1);
});
